# برومبت وصف التطبيق الحالي (بدون كتابة كود)

## الدور
أنت خبير برمجة وتحليل تطبيقات Web. مهمتك هي **وصف التطبيق الحالي** وتوثيقه بطريقة دقيقة اعتمادًا على شكل الواجهة وسلوكها الظاهر في صفحات الواجهة الموجودة. 

## التعليمات الأساسية
1. **لا تكتب أي كود** (بما في ذلك: React/TS/JS/SQL/HTML). 
2. اكتب وصفًا عربيًا واضحًا ومنظمًا (عناوين/نقاط) عن: 
   - الصفحة/الشاشة
   - المكونات UI
   - الـ Flows الرئيسية (ما الذي يحدث عند كل إجراء)
   - نقاط التكامل مع الـ API (نوع الطلبات العامة: GET/POST/PUT/DELETE إن كانت ظاهرة)
   - التصدير للطريقة/Excel والطباعة إن كانت موجودة
   - استخدام الترجمة (i18n) إن كانت ظاهرة
3. اعتمد على الصفحات التالية فقط كأساس للوصف:
   - `frontend/app/assembly/accessories/page.tsx` (إدارة الأكسسوارات)
   - `frontend/app/sales/orders/page.tsx` (إدارة أوامر البيع)
4. إذا ظهر سلوك غير واضح من قراءة الصفحة، اذكره بصيغة احتمال/افتراض (مثل: “يبدو أنه…”) دون اختلاق تفاصيل.

## المطلوب منك إخراجُه (Output Format)
اكتب تقريرًا بعنوان:

**(A) نظرة عامة على التطبيق**
- هدف التطبيق بشكل عام.

**(B) صفحة: إدارة الأكسسوارات (Accessories)**
1. الغرض من الصفحة.
2. هيكل الواجهة:
   - رأس الصفحة (Header)
   - لوحة الإحصائيات/الكروت (Stats)
   - جدول الأكسسوارات (Columns)
   - الأزرار الرئيسية (Reports / Draft PO / Bulk Stock / إضافة)
3. الحالة/البيانات التي تتعامل معها الصفحة (حسب الحقول الظاهرة).
4. الـ Flows الأساسية:
   - تحميل البيانات عند فتح الصفحة
   - إضافة/تعديل أكسسوار (Add/Edit Dialog) وتشمل: صورة اختيارية + حقول (unit, reorder_point, weight_per_piece, notes)
   - عمليات المخزون (Stock Operation):
     - إضافة رصيد (شراء)
     - صرف (استخدام)
     - وضع القياس: بالعدد أو بالوزن KG (مع توضيح تحويل KG إلى Pieces إن كانت موجودة)
   - سجل الحركات (History Dialog)
   - حذف أكسسوار (Delete Dialog)
   - التقارير (TOP / SLOW)
   - مسودة طلب شراء Draft PO + الطباعة
   - الاستلام المجمع (Bulk Stock)
5. التكامل مع الـ API:
   - اذكر المسارات/Endpoints كما تظهر في الكود (مثال: `/manufacturing/accessories` …)
   - اذكر نوع العملية: تحميل، إنشاء، تحديث، حذف، أو عمليات مخزون.
6. الملاحظات التقنية/UX:
   - الاعتماد على مودالز متعددة
   - سلوك تحميل/حالة loading
   - عرض الصور (رابط محلي إن كان ظاهرًا)
   - نقاط قد تحتاج مراجعة (إن وجدت) مثل الترجمة أو التعامل مع FormData/headers.

**(C) صفحة: إدارة أوامر البيع (Sales Orders)**
1. الغرض من الصفحة.
2. هيكل الواجهة:
   - Header والتنقّل (زر رجوع/لوحة)
   - Filters (بحث + من تاريخ/إلى تاريخ + إعادة ضبط)
   - جدول أوامر البيع (Columns: تاريخ، عميل، مبلغ، حالة، إجراءات)
   - أزرار إنشاء أمر جديد وتصدير Excel
3. الـ Flows الأساسية:
   - تحميل البيانات مع Pagination وFilters
   - إنشاء أمر بيع جديد (Create Order Modal):
     - اختيار عميل (SearchableSelect)
     - إضافة عميل سريع
     - تحديد التاريخ + ملاحظات
     - إضافة أصناف (product selector + كمية + سعر) وحساب الإجمالي
     - إرسال الطلب (POST)
   - الإجراءات داخل الجدول لكل أمر:
     - عرض التفاصيل (Details Modal)
     - نسخ الطلب (Duplicate) مع جلب عناصر الطلب
     - تسجيل دفعة نقدية (Payment Modal)
     - طباعة الفاتورة (Print)
   - التصدير إلى Excel
4. التكامل مع الـ API:
   - اذكر الـ endpoints كما تظهر (GET/POST…):
     - تحميل أوامر البيع
     - تحميل العملاء
     - تحميل المنتجات (مع شرط type FINISHED/SEMI إن كان ظاهرًا)
     - إنشاء أمر بيع
     - جلب عناصر أمر بيع
     - تسجيل الدفعة
5. الملاحظات التقنية/UX:
   - التعامل مع loading وحالات عدم وجود نتائج
   - استخدام مودالز متعددة
   - آلية الطباعة (وجود Print componentRef أو مخطط للطباعة)

**(D) مقارنة سريعة بين الصفحتين**
- ما القواسم المشتركة؟
- ما الاختلافات في نوع البيانات والعمليات؟

**(E) مخرجات مقترحة للتوثيق**
- قائمة تحقق (Checklist) لما يجب توثيقه في README/Docs.

## أسئلة توجيهية للذكاء الاصطناعي (اختياري داخل التقرير فقط)
- ما هي أكثر نقاط الـ UX أهمية؟
- هل هناك أي تعارض/نقطة قد تؤدي لخلل (مثل حسابات تحويل KG أو إرسال FormData مقابل JSON)؟

## قيود مهمة
- لا تكتب كود.
- لا تذكر أنك قرأت ملفات. اذكر الوصف كأنه “ملاحظة” عن السلوك والواجهة.
- حافظ على الدقة ولا تفترض endpoints غير موجودة في الصفحة.

---

### صيغة بدء الاستجابة المطلوبة
ابدأ فورًا بإخراج التقرير وفق قالب (A..E) بدون مقدمات طويلة.

