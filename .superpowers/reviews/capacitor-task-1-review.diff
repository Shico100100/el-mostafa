 frontend/app/inventory2/products/[id]/client.tsx   | 121 +++++
 .../inventory2/products/[id]/movements/client.tsx  | 210 +++++++++
 .../inventory2/products/[id]/movements/page.tsx    | 211 +--------
 frontend/app/inventory2/products/[id]/page.tsx     | 123 +----
 .../app/inventory2/semi-finished/[id]/client.tsx   | 230 +++++++++
 .../app/inventory2/semi-finished/[id]/page.tsx     | 236 +---------
 frontend/app/inventory2/warehouses/[id]/client.tsx | 246 ++++++++++
 frontend/app/inventory2/warehouses/[id]/page.tsx   | 247 +---------
 .../app/manufacturing/kiosk/[machineId]/client.tsx | 253 ++++++++++
 .../app/manufacturing/kiosk/[machineId]/page.tsx   | 267 +----------
 .../app/manufacturing/machines/[id]/client.tsx     |  94 ++++
 .../machines/[id]/maintenance/client.tsx           | 231 +++++++++
 .../machines/[id]/maintenance/page.tsx             | 232 +--------
 frontend/app/manufacturing/machines/[id]/page.tsx  |  95 +---
 frontend/app/manufacturing/molds/[id]/client.tsx   |  99 ++++
 frontend/app/manufacturing/molds/[id]/page.tsx     | 100 +---
 .../raw-materials/[id]/add-stock/client.tsx        | 194 ++++++++
 .../raw-materials/[id]/add-stock/page.tsx          | 199 +-------
 .../manufacturing/raw-materials/[id]/client.tsx    | 514 ++++++++++++++++++++
 .../app/manufacturing/raw-materials/[id]/page.tsx  | 523 +--------------------
 .../app/manufacturing/traceability/[id]/client.tsx | 242 ++++++++++
 .../app/manufacturing/traceability/[id]/page.tsx   | 246 +---------
 .../purchases/suppliers/statement/[id]/client.tsx  | 145 ++++++
 .../purchases/suppliers/statement/[id]/page.tsx    | 155 +-----
 .../app/sales/customers/statement/[id]/client.tsx  | 161 +++++++
 .../app/sales/customers/statement/[id]/page.tsx    | 169 +------
 frontend/next.config.ts                            |  26 +-
 27 files changed, 2809 insertions(+), 2760 deletions(-)
diff --git a/frontend/app/inventory2/products/[id]/client.tsx b/frontend/app/inventory2/products/[id]/client.tsx
new file mode 100644
index 0000000..e68c7de
--- /dev/null
+++ b/frontend/app/inventory2/products/[id]/client.tsx
@@ -0,0 +1,121 @@
+'use client';
+
+import { useEffect, useState } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+import { toast } from 'sonner';
+import { Package, DollarSign, BarChart3, Warehouse, Tag } from 'lucide-react';
+import PageShell from '@/components/inventory2/PageShell';
+import InfoCard, { DetailRow } from '@/components/inventory2/InfoCard';
+import { TypeBadge, StockBadge } from '@/components/inventory2/Badge';
+
+interface Category { id: number; name: string; }
+interface Warehouse { id: number; name: string; }
+interface ProductDetail {
+  id: number; name: string; type: string; unit: string;
+  cost_price: string; selling_price: string; stock_quantity: string;
+  min_stock?: string; warehouse_id?: number; warehouse?: Warehouse;
+  sku?: string; barcode?: string; category?: Category; category_id?: number;
+  description?: string; image_path?: string; weight_grams?: string;
+  created_at: string; updated_at: string;
+}
+
+export default function ProductDetailPage() {
+  const router = useRouter();
+  const params = useParams();
+  const productId = params.id as string;
+
+  const [product, setProduct] = useState<ProductDetail | null>(null);
+  const [loading, setLoading] = useState(true);
+
+  useEffect(() => {
+    const fetchProduct = async () => {
+      try {
+        const data = await api.fetchWithAuth<ProductDetail>(`/inventory/products/${productId}`);
+        setProduct(data);
+      } catch {
+        toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪ذ┘è╪د┘╪د╪ز ╪د┘┘à┘╪ز╪ش');
+      } finally { setLoading(false); }
+    };
+    fetchProduct();
+  }, [productId]);
+
+  if (loading) return (
+    <PageShell title="..." backHref="/inventory2/products">
+      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
+        <div className="bg-white/5 border border-white/10 rounded-2xl p-6 h-48 animate-pulse" />
+        <div className="bg-white/5 border border-white/10 rounded-2xl p-6 h-48 animate-pulse" />
+        <div className="bg-white/5 border border-white/10 rounded-2xl p-6 h-48 animate-pulse" />
+      </div>
+    </PageShell>
+  );
+
+  if (!product) return (
+    <PageShell title="╪د┘┘à┘╪ز╪ش ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»" backHref="/inventory2/products">
+      <p className="text-center text-slate-400 py-20">╪د┘┘à┘╪ز╪ش ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</p>
+    </PageShell>
+  );
+
+  const cost = Number(product.cost_price);
+  const sell = Number(product.selling_price);
+  const qty = Number(product.stock_quantity);
+  const margin = cost > 0 ? { value: sell - cost, pct: ((sell - cost) / cost * 100) } : { value: 0, pct: 0 };
+
+  return (
+    <PageShell
+      title={product.name}
+      subtitle={product.sku ? `SKU: ${product.sku}` : undefined}
+      backHref="/inventory2/products"
+      actions={
+        <>
+          <button
+            onClick={() => router.push(`/inventory2/products/${product.id}/movements`)}
+            className="px-4 py-2.5 bg-blue-600/10 hover:bg-blue-600/20 text-blue-400 rounded-xl border border-blue-500/20 transition flex items-center gap-2 text-sm"
+          >
+            <BarChart3 className="w-4 h-4" /> ╪│╪ش┘ ╪د┘╪ص╪▒┘â╪د╪ز
+          </button>
+        </>
+      }
+    >
+      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
+        {product.image_path && (
+          <div className="lg:col-span-3">
+            <div className="bg-white/5 border border-white/10 rounded-2xl p-4 backdrop-blur-xl flex justify-center">
+              <img src={product.image_path} alt={product.name} className="max-h-48 object-contain rounded-xl" />
+            </div>
+          </div>
+        )}
+
+        <InfoCard title="┘à╪╣┘┘ê┘à╪د╪ز ╪ث╪│╪د╪│┘è╪ر" icon={<Package className="w-5 h-5 text-blue-400" />}>
+          <DetailRow label="╪د┘╪د╪│┘à" value={product.name} />
+          <DetailRow label="╪د┘┘â┘ê╪» (SKU)" value={product.sku || 'ظ¤'} />
+          <DetailRow label="╪د┘╪ذ╪د╪▒┘â┘ê╪»" value={product.barcode || 'ظ¤'} />
+          <DetailRow label="╪د┘┘┘ê╪╣" value={<TypeBadge type={product.type} />} />
+          <DetailRow label="╪د┘╪ز╪╡┘┘è┘" value={product.category?.name || 'ظ¤'} />
+          <DetailRow label="╪د┘┘ê╪ص╪»╪ر" value={product.unit} />
+          {product.weight_grams && <DetailRow label="╪د┘┘ê╪▓┘" value={`${Number(product.weight_grams).toLocaleString()} ╪ش╪▒╪د┘à`} />}
+        </InfoCard>
+
+        <InfoCard title="╪د┘┘à╪«╪▓┘ê┘" icon={<Warehouse className="w-5 h-5 text-amber-400" />}>
+          <DetailRow label="╪د┘┘â┘à┘è╪ر ╪د┘╪ص╪د┘┘è╪ر" value={<span className="flex items-center gap-2">{qty.toLocaleString()} <StockBadge quantity={qty} /></span>} />
+          <DetailRow label="╪د┘╪ص╪» ╪د┘╪ث╪»┘┘ë" value={product.min_stock ? Number(product.min_stock).toLocaleString() : 'ظ¤'} />
+          <DetailRow label="╪د┘┘à╪«╪▓┘" value={product.warehouse?.name || 'ظ¤'} />
+        </InfoCard>
+
+        <InfoCard title="╪د┘╪ز╪│╪╣┘è╪▒" icon={<DollarSign className="w-5 h-5 text-emerald-400" />}>
+          <DetailRow label="╪│╪╣╪▒ ╪د┘╪ز┘â┘┘╪ر" value={`${cost.toFixed(2)} ╪ش.┘à`} valueClass="text-green-400" />
+          <DetailRow label="╪│╪╣╪▒ ╪د┘╪ذ┘è╪╣" value={`${sell.toFixed(2)} ╪ش.┘à`} valueClass="text-blue-400" />
+          <DetailRow label="╪د┘┘ç╪د┘à╪┤" value={`${margin.value.toFixed(2)} ╪ش.┘à (${margin.pct.toFixed(1)}%)`} valueClass="text-purple-400" />
+        </InfoCard>
+
+        {product.description && (
+          <div className="lg:col-span-3">
+            <InfoCard title="┘ê╪╡┘" icon={<Tag className="w-5 h-5 text-slate-400" />}>
+              <p className="text-slate-300 text-sm leading-relaxed">{product.description}</p>
+            </InfoCard>
+          </div>
+        )}
+      </div>
+    </PageShell>
+  );
+}
diff --git a/frontend/app/inventory2/products/[id]/movements/client.tsx b/frontend/app/inventory2/products/[id]/movements/client.tsx
new file mode 100644
index 0000000..a68f64b
--- /dev/null
+++ b/frontend/app/inventory2/products/[id]/movements/client.tsx
@@ -0,0 +1,210 @@
+'use client';
+
+import { useEffect, useState, useCallback } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+import { toast } from 'sonner';
+import { BarChart3, Package, TrendingUp, ArrowLeft, Edit3, Trash2, X } from 'lucide-react';
+import StatCards from '@/components/inventory2/StatCards';
+import { Modal } from '@/components/inventory2/Modal';
+
+interface ProductData {
+  id: number; name: string; stock_quantity: string; selling_price: string;
+}
+
+interface Movement {
+  id: number; date: string; type: string; quantity: string;
+  notes?: string; warehouse?: { name: string };
+}
+
+export default function ProductMovementsPage() {
+  const router = useRouter();
+  const params = useParams();
+  const productId = params.id as string;
+
+  const [product, setProduct] = useState<ProductData | null>(null);
+  const [movements, setMovements] = useState<Movement[]>([]);
+  const [loading, setLoading] = useState(true);
+
+  const [showEditModal, setShowEditModal] = useState(false);
+  const [editingMovement, setEditingMovement] = useState<Movement | null>(null);
+  const [editForm, setEditForm] = useState({ quantity: '', type: 'IN', notes: '' });
+
+  const loadData = useCallback(async () => {
+    try {
+      const [productData, movementsData] = await Promise.all([
+        api.fetchWithAuth<ProductData>(`/inventory/products/${productId}`),
+        api.fetchWithAuth<Movement[]>(`/inventory/products/${productId}/movements`),
+      ]);
+      setProduct(productData);
+      setMovements(movementsData || []);
+    } catch {
+      toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪د┘╪ذ┘è╪د┘╪د╪ز');
+    } finally { setLoading(false); }
+  }, [productId]);
+
+  useEffect(() => {
+    loadData();
+  }, [loadData]);
+
+  const openEditModal = (movement: Movement) => {
+    setEditingMovement(movement);
+    setEditForm({ quantity: movement.quantity, type: movement.type, notes: movement.notes || '' });
+    setShowEditModal(true);
+  };
+
+  const handleEditSubmit = async (e: React.FormEvent) => {
+    e.preventDefault();
+    if (!editingMovement) return;
+    try {
+      await api.fetchWithAuth(`/inventory/stock/movements/${editingMovement.id}`, {
+        method: 'PUT',
+        body: JSON.stringify({ quantity: Number(editForm.quantity), type: editForm.type, notes: editForm.notes }),
+      });
+      await api.createNotification({
+        title: '╪╖┘╪ذ ╪د┘┘à┘ê╪د┘┘é╪ر ╪╣┘┘ë ╪ز╪╣╪»┘è┘ ╪ص╪▒┘â╪ر ┘à╪«╪▓┘ê┘',
+        message: `╪ز┘à ╪╖┘╪ذ ╪ز╪╣╪»┘è┘ ╪ص╪▒┘â╪ر ╪د┘┘à╪«╪▓┘ê┘ ╪▒┘é┘à ${editingMovement.id} ┘┘┘à┘╪ز╪ش ${product?.name}`,
+      });
+      toast.success('╪ز┘à ╪ح╪▒╪│╪د┘ ╪╖┘╪ذ ╪د┘╪ز╪╣╪»┘è┘ ┘┘┘à╪»┘è╪▒');
+      setShowEditModal(false);
+      loadData();
+    } catch {
+      toast.error('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪د┘╪ز╪╣╪»┘è┘');
+    }
+  };
+
+  const handleDeleteMovement = async (movement: Movement) => {
+    if (!product) return;
+    try {
+      await api.createNotification({
+        title: '╪╖┘╪ذ ╪ص╪░┘ ╪ص╪▒┘â╪ر ┘à╪«╪▓┘ê┘',
+        message: `╪ز┘à ╪╖┘╪ذ ╪ص╪░┘ ╪ص╪▒┘â╪ر ╪د┘┘à╪«╪▓┘ê┘ ╪▒┘é┘à ${movement.id} ┘┘┘à┘╪ز╪ش ${product.name} - ╪د┘┘â┘à┘è╪ر: ${movement.quantity}`,
+        actionType: 'delete_movement',
+        actionData: { movementId: movement.id, productId: product.id },
+      });
+      toast.success('╪ز┘à ╪ح╪▒╪│╪د┘ ╪╖┘╪ذ ╪د┘╪ص╪░┘ ┘┘┘à╪»┘è╪▒');
+    } catch {
+      toast.error('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪ح╪▒╪│╪د┘ ╪د┘╪╖┘╪ذ');
+    }
+  };
+
+  if (loading) {
+    return (
+      <div className="flex items-center justify-center min-h-[50vh]">
+        <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
+      </div>
+    );
+  }
+
+  const qty = Number(product?.stock_quantity || 0);
+
+  const statsCards = [
+    { label: '╪د┘┘â┘à┘è╪ر ╪د┘╪ص╪د┘┘è╪ر', value: qty.toLocaleString(), icon: <Package className="w-6 h-6 text-blue-400" />, color: 'bg-blue-500/20' },
+    { label: '╪ح╪ش┘à╪د┘┘è ╪د┘╪ص╪▒┘â╪د╪ز', value: movements.length, icon: <BarChart3 className="w-6 h-6 text-purple-400" />, color: 'bg-purple-500/20' },
+    { label: '╪│╪╣╪▒ ╪د┘╪ذ┘è╪╣', value: product ? `${Number(product.selling_price).toFixed(2)} ╪ش.┘à` : 'ظ¤', icon: <TrendingUp className="w-6 h-6 text-green-400" />, color: 'bg-green-500/20' },
+  ];
+
+  return (
+    <>
+      <header className="bg-white/5 backdrop-blur-xl border-b border-white/10 sticky top-0 z-40">
+        <div className="px-8 py-5 flex justify-between items-center">
+          <div className="flex items-center gap-4">
+            <button onClick={() => router.push(`/inventory2/products/${productId}`)} className="p-2 hover:bg-white/5 rounded-full transition text-white/50 hover:text-white">
+              <ArrowLeft className="w-6 h-6" />
+            </button>
+            <h1 className="text-2xl font-black text-white">╪│╪ش┘ ╪ص╪▒┘â╪د╪ز: {product?.name}</h1>
+          </div>
+        </div>
+      </header>
+
+      <main className="px-8 py-8">
+        <StatCards cards={statsCards} />
+
+        <div className="bg-white/5 backdrop-blur-xl rounded-2xl border border-white/10 overflow-hidden">
+          <table className="w-full">
+            <thead className="bg-white/5">
+              <tr>
+                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘╪ز╪د╪▒┘è╪«</th>
+                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘┘ê╪╣</th>
+                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘â┘à┘è╪ر</th>
+                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘à╪«╪▓┘</th>
+                <th className="px-6 py-4 text-right text-white font-semibold text-sm">┘à┘╪د╪ص╪╕╪د╪ز</th>
+                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘╪ح╪ش╪▒╪د╪ة╪د╪ز</th>
+              </tr>
+            </thead>
+            <tbody>
+              {movements.map((movement) => (
+                <tr key={movement.id} className="border-t border-white/5 hover:bg-white/5 transition">
+                  <td className="px-6 py-4 text-slate-300 text-sm">
+                    {new Date(movement.date).toLocaleDateString('ar-EG')}
+                  </td>
+                  <td className="px-6 py-4">
+                    <span className={`px-3 py-1 rounded-full text-xs font-medium border ${
+                      movement.type === 'IN' ? 'bg-emerald-500/20 text-emerald-300 border-emerald-500/30' :
+                      movement.type === 'OUT' ? 'bg-red-500/20 text-red-300 border-red-500/30' :
+                      'bg-amber-500/20 text-amber-300 border-amber-500/30'
+                    }`}>
+                      {movement.type === 'IN' ? '╪ح╪»╪«╪د┘' : movement.type === 'OUT' ? '╪ح╪«╪▒╪د╪ش' : '╪ز╪╣╪»┘è┘'}
+                    </span>
+                  </td>
+                  <td className={`px-6 py-4 font-semibold text-sm ${
+                    movement.type === 'IN' ? 'text-emerald-400' :
+                    movement.type === 'OUT' ? 'text-red-400' : 'text-amber-400'
+                  }`}>{Number(movement.quantity).toLocaleString()}</td>
+                  <td className="px-6 py-4 text-slate-400 text-sm">{movement.warehouse?.name || 'ظ¤'}</td>
+                  <td className="px-6 py-4 text-slate-400 text-sm">{movement.notes || 'ظ¤'}</td>
+                  <td className="px-6 py-4">
+                    <div className="flex gap-1.5">
+                      <button onClick={() => openEditModal(movement)} className="p-1.5 bg-blue-500/20 hover:bg-blue-500/40 text-blue-200 rounded-lg transition" title="╪ز╪╣╪»┘è┘"><Edit3 className="w-3.5 h-3.5" /></button>
+                      <button onClick={() => handleDeleteMovement(movement)} className="p-1.5 bg-red-500/20 hover:bg-red-500/40 text-red-200 rounded-lg transition" title="╪ص╪░┘"><Trash2 className="w-3.5 h-3.5" /></button>
+                    </div>
+                  </td>
+                </tr>
+              ))}
+              {movements.length === 0 && (
+                <tr>
+                  <td colSpan={6} className="px-6 py-16 text-center text-slate-500">
+                    <BarChart3 className="w-12 h-12 mx-auto mb-3 text-slate-600" />
+                    <p>┘╪د ╪ز┘ê╪ش╪» ╪ص╪▒┘â╪د╪ز ┘┘ç╪░╪د ╪د┘┘à┘╪ز╪ش</p>
+                  </td>
+                </tr>
+              )}
+            </tbody>
+          </table>
+        </div>
+      </main>
+
+      <Modal isOpen={showEditModal} onClose={() => setShowEditModal(false)} title="╪ز╪╣╪»┘è┘ ╪ص╪▒┘â╪ر ╪د┘┘à╪«╪▓┘ê┘" icon={<Edit3 className="w-6 h-6 text-blue-400" />}>
+        <form onSubmit={handleEditSubmit} className="space-y-4">
+          <div>
+            <label className="block text-sm font-medium text-slate-300 mb-1.5">╪د┘┘â┘à┘è╪ر</label>
+            <input type="number" required min="0" step="0.01"
+              value={editForm.quantity}
+              onChange={(e) => setEditForm({ ...editForm, quantity: e.target.value })}
+              className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none" />
+          </div>
+          <div>
+            <label className="block text-sm font-medium text-slate-300 mb-1.5">╪د┘┘┘ê╪╣</label>
+            <select value={editForm.type}
+              onChange={(e) => setEditForm({ ...editForm, type: e.target.value })}
+              className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none">
+              <option value="IN">╪ح╪»╪«╪د┘</option>
+              <option value="OUT">╪ح╪«╪▒╪د╪ش</option>
+              <option value="ADJUST">╪ز╪╣╪»┘è┘</option>
+            </select>
+          </div>
+          <div>
+            <label className="block text-sm font-medium text-slate-300 mb-1.5">┘à┘╪د╪ص╪╕╪د╪ز</label>
+            <textarea rows={3} value={editForm.notes}
+              onChange={(e) => setEditForm({ ...editForm, notes: e.target.value })}
+              className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none" />
+          </div>
+          <div className="flex gap-3 justify-end pt-4 border-t border-white/10">
+            <button type="button" onClick={() => setShowEditModal(false)} className="px-5 py-2.5 bg-slate-700/50 text-slate-200 rounded-xl hover:bg-slate-700 transition">╪ح┘╪║╪د╪ة</button>
+            <button type="submit" className="px-6 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-xl font-bold hover:from-blue-700 hover:to-indigo-700 transition shadow-lg shadow-blue-900/20">╪ص┘╪╕ ╪د┘╪ز╪╣╪»┘è┘</button>
+          </div>
+        </form>
+      </Modal>
+    </>
+  );
+}
diff --git a/frontend/app/inventory2/products/[id]/movements/page.tsx b/frontend/app/inventory2/products/[id]/movements/page.tsx
index a68f64b..38cde4c 100644
--- a/frontend/app/inventory2/products/[id]/movements/page.tsx
+++ b/frontend/app/inventory2/products/[id]/movements/page.tsx
@@ -1,210 +1,9 @@
-'use client';
+import MovementsClientPage from './client';
 
-import { useEffect, useState, useCallback } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-import { toast } from 'sonner';
-import { BarChart3, Package, TrendingUp, ArrowLeft, Edit3, Trash2, X } from 'lucide-react';
-import StatCards from '@/components/inventory2/StatCards';
-import { Modal } from '@/components/inventory2/Modal';
-
-interface ProductData {
-  id: number; name: string; stock_quantity: string; selling_price: string;
-}
-
-interface Movement {
-  id: number; date: string; type: string; quantity: string;
-  notes?: string; warehouse?: { name: string };
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function ProductMovementsPage() {
-  const router = useRouter();
-  const params = useParams();
-  const productId = params.id as string;
-
-  const [product, setProduct] = useState<ProductData | null>(null);
-  const [movements, setMovements] = useState<Movement[]>([]);
-  const [loading, setLoading] = useState(true);
-
-  const [showEditModal, setShowEditModal] = useState(false);
-  const [editingMovement, setEditingMovement] = useState<Movement | null>(null);
-  const [editForm, setEditForm] = useState({ quantity: '', type: 'IN', notes: '' });
-
-  const loadData = useCallback(async () => {
-    try {
-      const [productData, movementsData] = await Promise.all([
-        api.fetchWithAuth<ProductData>(`/inventory/products/${productId}`),
-        api.fetchWithAuth<Movement[]>(`/inventory/products/${productId}/movements`),
-      ]);
-      setProduct(productData);
-      setMovements(movementsData || []);
-    } catch {
-      toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪د┘╪ذ┘è╪د┘╪د╪ز');
-    } finally { setLoading(false); }
-  }, [productId]);
-
-  useEffect(() => {
-    loadData();
-  }, [loadData]);
-
-  const openEditModal = (movement: Movement) => {
-    setEditingMovement(movement);
-    setEditForm({ quantity: movement.quantity, type: movement.type, notes: movement.notes || '' });
-    setShowEditModal(true);
-  };
-
-  const handleEditSubmit = async (e: React.FormEvent) => {
-    e.preventDefault();
-    if (!editingMovement) return;
-    try {
-      await api.fetchWithAuth(`/inventory/stock/movements/${editingMovement.id}`, {
-        method: 'PUT',
-        body: JSON.stringify({ quantity: Number(editForm.quantity), type: editForm.type, notes: editForm.notes }),
-      });
-      await api.createNotification({
-        title: '╪╖┘╪ذ ╪د┘┘à┘ê╪د┘┘é╪ر ╪╣┘┘ë ╪ز╪╣╪»┘è┘ ╪ص╪▒┘â╪ر ┘à╪«╪▓┘ê┘',
-        message: `╪ز┘à ╪╖┘╪ذ ╪ز╪╣╪»┘è┘ ╪ص╪▒┘â╪ر ╪د┘┘à╪«╪▓┘ê┘ ╪▒┘é┘à ${editingMovement.id} ┘┘┘à┘╪ز╪ش ${product?.name}`,
-      });
-      toast.success('╪ز┘à ╪ح╪▒╪│╪د┘ ╪╖┘╪ذ ╪د┘╪ز╪╣╪»┘è┘ ┘┘┘à╪»┘è╪▒');
-      setShowEditModal(false);
-      loadData();
-    } catch {
-      toast.error('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪د┘╪ز╪╣╪»┘è┘');
-    }
-  };
-
-  const handleDeleteMovement = async (movement: Movement) => {
-    if (!product) return;
-    try {
-      await api.createNotification({
-        title: '╪╖┘╪ذ ╪ص╪░┘ ╪ص╪▒┘â╪ر ┘à╪«╪▓┘ê┘',
-        message: `╪ز┘à ╪╖┘╪ذ ╪ص╪░┘ ╪ص╪▒┘â╪ر ╪د┘┘à╪«╪▓┘ê┘ ╪▒┘é┘à ${movement.id} ┘┘┘à┘╪ز╪ش ${product.name} - ╪د┘┘â┘à┘è╪ر: ${movement.quantity}`,
-        actionType: 'delete_movement',
-        actionData: { movementId: movement.id, productId: product.id },
-      });
-      toast.success('╪ز┘à ╪ح╪▒╪│╪د┘ ╪╖┘╪ذ ╪د┘╪ص╪░┘ ┘┘┘à╪»┘è╪▒');
-    } catch {
-      toast.error('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪ح╪▒╪│╪د┘ ╪د┘╪╖┘╪ذ');
-    }
-  };
-
-  if (loading) {
-    return (
-      <div className="flex items-center justify-center min-h-[50vh]">
-        <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
-      </div>
-    );
-  }
-
-  const qty = Number(product?.stock_quantity || 0);
-
-  const statsCards = [
-    { label: '╪د┘┘â┘à┘è╪ر ╪د┘╪ص╪د┘┘è╪ر', value: qty.toLocaleString(), icon: <Package className="w-6 h-6 text-blue-400" />, color: 'bg-blue-500/20' },
-    { label: '╪ح╪ش┘à╪د┘┘è ╪د┘╪ص╪▒┘â╪د╪ز', value: movements.length, icon: <BarChart3 className="w-6 h-6 text-purple-400" />, color: 'bg-purple-500/20' },
-    { label: '╪│╪╣╪▒ ╪د┘╪ذ┘è╪╣', value: product ? `${Number(product.selling_price).toFixed(2)} ╪ش.┘à` : 'ظ¤', icon: <TrendingUp className="w-6 h-6 text-green-400" />, color: 'bg-green-500/20' },
-  ];
-
-  return (
-    <>
-      <header className="bg-white/5 backdrop-blur-xl border-b border-white/10 sticky top-0 z-40">
-        <div className="px-8 py-5 flex justify-between items-center">
-          <div className="flex items-center gap-4">
-            <button onClick={() => router.push(`/inventory2/products/${productId}`)} className="p-2 hover:bg-white/5 rounded-full transition text-white/50 hover:text-white">
-              <ArrowLeft className="w-6 h-6" />
-            </button>
-            <h1 className="text-2xl font-black text-white">╪│╪ش┘ ╪ص╪▒┘â╪د╪ز: {product?.name}</h1>
-          </div>
-        </div>
-      </header>
-
-      <main className="px-8 py-8">
-        <StatCards cards={statsCards} />
-
-        <div className="bg-white/5 backdrop-blur-xl rounded-2xl border border-white/10 overflow-hidden">
-          <table className="w-full">
-            <thead className="bg-white/5">
-              <tr>
-                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘╪ز╪د╪▒┘è╪«</th>
-                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘┘ê╪╣</th>
-                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘â┘à┘è╪ر</th>
-                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘à╪«╪▓┘</th>
-                <th className="px-6 py-4 text-right text-white font-semibold text-sm">┘à┘╪د╪ص╪╕╪د╪ز</th>
-                <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘╪ح╪ش╪▒╪د╪ة╪د╪ز</th>
-              </tr>
-            </thead>
-            <tbody>
-              {movements.map((movement) => (
-                <tr key={movement.id} className="border-t border-white/5 hover:bg-white/5 transition">
-                  <td className="px-6 py-4 text-slate-300 text-sm">
-                    {new Date(movement.date).toLocaleDateString('ar-EG')}
-                  </td>
-                  <td className="px-6 py-4">
-                    <span className={`px-3 py-1 rounded-full text-xs font-medium border ${
-                      movement.type === 'IN' ? 'bg-emerald-500/20 text-emerald-300 border-emerald-500/30' :
-                      movement.type === 'OUT' ? 'bg-red-500/20 text-red-300 border-red-500/30' :
-                      'bg-amber-500/20 text-amber-300 border-amber-500/30'
-                    }`}>
-                      {movement.type === 'IN' ? '╪ح╪»╪«╪د┘' : movement.type === 'OUT' ? '╪ح╪«╪▒╪د╪ش' : '╪ز╪╣╪»┘è┘'}
-                    </span>
-                  </td>
-                  <td className={`px-6 py-4 font-semibold text-sm ${
-                    movement.type === 'IN' ? 'text-emerald-400' :
-                    movement.type === 'OUT' ? 'text-red-400' : 'text-amber-400'
-                  }`}>{Number(movement.quantity).toLocaleString()}</td>
-                  <td className="px-6 py-4 text-slate-400 text-sm">{movement.warehouse?.name || 'ظ¤'}</td>
-                  <td className="px-6 py-4 text-slate-400 text-sm">{movement.notes || 'ظ¤'}</td>
-                  <td className="px-6 py-4">
-                    <div className="flex gap-1.5">
-                      <button onClick={() => openEditModal(movement)} className="p-1.5 bg-blue-500/20 hover:bg-blue-500/40 text-blue-200 rounded-lg transition" title="╪ز╪╣╪»┘è┘"><Edit3 className="w-3.5 h-3.5" /></button>
-                      <button onClick={() => handleDeleteMovement(movement)} className="p-1.5 bg-red-500/20 hover:bg-red-500/40 text-red-200 rounded-lg transition" title="╪ص╪░┘"><Trash2 className="w-3.5 h-3.5" /></button>
-                    </div>
-                  </td>
-                </tr>
-              ))}
-              {movements.length === 0 && (
-                <tr>
-                  <td colSpan={6} className="px-6 py-16 text-center text-slate-500">
-                    <BarChart3 className="w-12 h-12 mx-auto mb-3 text-slate-600" />
-                    <p>┘╪د ╪ز┘ê╪ش╪» ╪ص╪▒┘â╪د╪ز ┘┘ç╪░╪د ╪د┘┘à┘╪ز╪ش</p>
-                  </td>
-                </tr>
-              )}
-            </tbody>
-          </table>
-        </div>
-      </main>
-
-      <Modal isOpen={showEditModal} onClose={() => setShowEditModal(false)} title="╪ز╪╣╪»┘è┘ ╪ص╪▒┘â╪ر ╪د┘┘à╪«╪▓┘ê┘" icon={<Edit3 className="w-6 h-6 text-blue-400" />}>
-        <form onSubmit={handleEditSubmit} className="space-y-4">
-          <div>
-            <label className="block text-sm font-medium text-slate-300 mb-1.5">╪د┘┘â┘à┘è╪ر</label>
-            <input type="number" required min="0" step="0.01"
-              value={editForm.quantity}
-              onChange={(e) => setEditForm({ ...editForm, quantity: e.target.value })}
-              className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none" />
-          </div>
-          <div>
-            <label className="block text-sm font-medium text-slate-300 mb-1.5">╪د┘┘┘ê╪╣</label>
-            <select value={editForm.type}
-              onChange={(e) => setEditForm({ ...editForm, type: e.target.value })}
-              className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none">
-              <option value="IN">╪ح╪»╪«╪د┘</option>
-              <option value="OUT">╪ح╪«╪▒╪د╪ش</option>
-              <option value="ADJUST">╪ز╪╣╪»┘è┘</option>
-            </select>
-          </div>
-          <div>
-            <label className="block text-sm font-medium text-slate-300 mb-1.5">┘à┘╪د╪ص╪╕╪د╪ز</label>
-            <textarea rows={3} value={editForm.notes}
-              onChange={(e) => setEditForm({ ...editForm, notes: e.target.value })}
-              className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none" />
-          </div>
-          <div className="flex gap-3 justify-end pt-4 border-t border-white/10">
-            <button type="button" onClick={() => setShowEditModal(false)} className="px-5 py-2.5 bg-slate-700/50 text-slate-200 rounded-xl hover:bg-slate-700 transition">╪ح┘╪║╪د╪ة</button>
-            <button type="submit" className="px-6 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-xl font-bold hover:from-blue-700 hover:to-indigo-700 transition shadow-lg shadow-blue-900/20">╪ص┘╪╕ ╪د┘╪ز╪╣╪»┘è┘</button>
-          </div>
-        </form>
-      </Modal>
-    </>
-  );
+export default function Page() {
+  return <MovementsClientPage />;
 }
diff --git a/frontend/app/inventory2/products/[id]/page.tsx b/frontend/app/inventory2/products/[id]/page.tsx
index 9dd3890..9a82aa9 100644
--- a/frontend/app/inventory2/products/[id]/page.tsx
+++ b/frontend/app/inventory2/products/[id]/page.tsx
@@ -1,122 +1,9 @@
-'use client';
+import ProductClientPage from './client';
 
-import { useEffect, useState } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-import { toast } from 'sonner';
-import { Package, DollarSign, BarChart3, Warehouse, Tag } from 'lucide-react';
-import PageShell from '@/components/inventory2/PageShell';
-import InfoCard, { DetailRow } from '@/components/inventory2/InfoCard';
-import { TypeBadge, StockBadge } from '@/components/inventory2/Badge';
-
-interface Category { id: number; name: string; }
-interface Warehouse { id: number; name: string; }
-interface ProductDetail {
-  id: number; name: string; type: string; unit: string;
-  cost_price: string; selling_price: string; stock_quantity: string;
-  min_stock?: string; warehouse_id?: number; warehouse?: Warehouse;
-  sku?: string; barcode?: string; category?: Category; category_id?: number;
-  description?: string; image_path?: string; weight_grams?: string;
-  created_at: string; updated_at: string;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function ProductDetailPage() {
-  const router = useRouter();
-  const params = useParams();
-  const productId = params.id as string;
-
-  const [product, setProduct] = useState<ProductDetail | null>(null);
-  const [loading, setLoading] = useState(true);
-
-  useEffect(() => {
-    const fetchProduct = async () => {
-      try {
-        const data = await api.fetchWithAuth<ProductDetail>(`/inventory/products/${productId}`);
-        setProduct(data);
-      } catch {
-        toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪ذ┘è╪د┘╪د╪ز ╪د┘┘à┘╪ز╪ش');
-      } finally { setLoading(false); }
-    };
-    fetchProduct();
-  }, [productId]);
-
-  if (loading) return (
-    <PageShell title="..." backHref="/inventory2/products">
-      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
-        <div className="bg-white/5 border border-white/10 rounded-2xl p-6 h-48 animate-pulse" />
-        <div className="bg-white/5 border border-white/10 rounded-2xl p-6 h-48 animate-pulse" />
-        <div className="bg-white/5 border border-white/10 rounded-2xl p-6 h-48 animate-pulse" />
-      </div>
-    </PageShell>
-  );
-
-  if (!product) return (
-    <PageShell title="╪د┘┘à┘╪ز╪ش ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»" backHref="/inventory2/products">
-      <p className="text-center text-slate-400 py-20">╪د┘┘à┘╪ز╪ش ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</p>
-    </PageShell>
-  );
-
-  const cost = Number(product.cost_price);
-  const sell = Number(product.selling_price);
-  const qty = Number(product.stock_quantity);
-  const margin = cost > 0 ? { value: sell - cost, pct: ((sell - cost) / cost * 100) } : { value: 0, pct: 0 };
-
-  return (
-    <PageShell
-      title={product.name}
-      subtitle={product.sku ? `SKU: ${product.sku}` : undefined}
-      backHref="/inventory2/products"
-      actions={
-        <>
-          <button
-            onClick={() => router.push(`/inventory2/products/${product.id}/movements`)}
-            className="px-4 py-2.5 bg-blue-600/10 hover:bg-blue-600/20 text-blue-400 rounded-xl border border-blue-500/20 transition flex items-center gap-2 text-sm"
-          >
-            <BarChart3 className="w-4 h-4" /> ╪│╪ش┘ ╪د┘╪ص╪▒┘â╪د╪ز
-          </button>
-        </>
-      }
-    >
-      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
-        {/* Image */}
-        {product.image_path && (
-          <div className="lg:col-span-3">
-            <div className="bg-white/5 border border-white/10 rounded-2xl p-4 backdrop-blur-xl flex justify-center">
-              <img src={product.image_path} alt={product.name} className="max-h-48 object-contain rounded-xl" />
-            </div>
-          </div>
-        )}
-
-        <InfoCard title="┘à╪╣┘┘ê┘à╪د╪ز ╪ث╪│╪د╪│┘è╪ر" icon={<Package className="w-5 h-5 text-blue-400" />}>
-          <DetailRow label="╪د┘╪د╪│┘à" value={product.name} />
-          <DetailRow label="╪د┘┘â┘ê╪» (SKU)" value={product.sku || 'ظ¤'} />
-          <DetailRow label="╪د┘╪ذ╪د╪▒┘â┘ê╪»" value={product.barcode || 'ظ¤'} />
-          <DetailRow label="╪د┘┘┘ê╪╣" value={<TypeBadge type={product.type} />} />
-          <DetailRow label="╪د┘╪ز╪╡┘┘è┘" value={product.category?.name || 'ظ¤'} />
-          <DetailRow label="╪د┘┘ê╪ص╪»╪ر" value={product.unit} />
-          {product.weight_grams && <DetailRow label="╪د┘┘ê╪▓┘" value={`${Number(product.weight_grams).toLocaleString()} ╪ش╪▒╪د┘à`} />}
-        </InfoCard>
-
-        <InfoCard title="╪د┘┘à╪«╪▓┘ê┘" icon={<Warehouse className="w-5 h-5 text-amber-400" />}>
-          <DetailRow label="╪د┘┘â┘à┘è╪ر ╪د┘╪ص╪د┘┘è╪ر" value={<span className="flex items-center gap-2">{qty.toLocaleString()} <StockBadge quantity={qty} /></span>} />
-          <DetailRow label="╪د┘╪ص╪» ╪د┘╪ث╪»┘┘ë" value={product.min_stock ? Number(product.min_stock).toLocaleString() : 'ظ¤'} />
-          <DetailRow label="╪د┘┘à╪«╪▓┘" value={product.warehouse?.name || 'ظ¤'} />
-        </InfoCard>
-
-        <InfoCard title="╪د┘╪ز╪│╪╣┘è╪▒" icon={<DollarSign className="w-5 h-5 text-emerald-400" />}>
-          <DetailRow label="╪│╪╣╪▒ ╪د┘╪ز┘â┘┘╪ر" value={`${cost.toFixed(2)} ╪ش.┘à`} valueClass="text-green-400" />
-          <DetailRow label="╪│╪╣╪▒ ╪د┘╪ذ┘è╪╣" value={`${sell.toFixed(2)} ╪ش.┘à`} valueClass="text-blue-400" />
-          <DetailRow label="╪د┘┘ç╪د┘à╪┤" value={`${margin.value.toFixed(2)} ╪ش.┘à (${margin.pct.toFixed(1)}%)`} valueClass="text-purple-400" />
-        </InfoCard>
-
-        {product.description && (
-          <div className="lg:col-span-3">
-            <InfoCard title="┘ê╪╡┘" icon={<Tag className="w-5 h-5 text-slate-400" />}>
-              <p className="text-slate-300 text-sm leading-relaxed">{product.description}</p>
-            </InfoCard>
-          </div>
-        )}
-      </div>
-    </PageShell>
-  );
+export default function Page() {
+  return <ProductClientPage />;
 }
diff --git a/frontend/app/inventory2/semi-finished/[id]/client.tsx b/frontend/app/inventory2/semi-finished/[id]/client.tsx
new file mode 100644
index 0000000..2997c3f
--- /dev/null
+++ b/frontend/app/inventory2/semi-finished/[id]/client.tsx
@@ -0,0 +1,230 @@
+'use client';
+
+import { useEffect, useState } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+import { toast } from 'sonner';
+import { Factory, Package, Target, Cpu, DollarSign, ArrowLeft, Scale } from 'lucide-react';
+import PageShell from '@/components/inventory2/PageShell';
+import InfoCard, { DetailRow } from '@/components/inventory2/InfoCard';
+
+interface ProductInfo {
+  id: number; name: string; cost_price: number; selling_price: number;
+  stock_quantity: number; unit: string;
+}
+
+interface MoldInfo {
+  id: number; name: string; price: number; product_weight: number;
+  cavities: number; current_shots: number; max_shots: number;
+  status: string; life_cycle_status: string;
+}
+
+interface BestMachine {
+  id: number; name: string; price: number; useful_life_years: number;
+  power_consumption: number; runs: number; total_pieces: number;
+}
+
+interface CostBreakdown {
+  rawMaterialPrice: number; pieceWeightGrams: number; rawCostPerPiece: number;
+  nonElectricHourly: number; machinePowerKw: number;
+  electricityPerMachineHour: number; electricityHourly: number;
+  machineDepreciationHourly: number; machineHourly: number;
+  hoursWorked: number; estimatedPieces: number; fixedPerPiece: number;
+  moldAmortizationPerPiece: number; totalPerPiece: number;
+  avgTotalFixedCost: number; avgTotalElectricity: number; avgActiveDays: number;
+  monthStr: string; monthDetails: { month: string; amount: number }[];
+}
+
+interface Details {
+  product: ProductInfo; mold: MoldInfo | null;
+  bestMachine: BestMachine | null; costBreakdown: CostBreakdown | null;
+}
+
+function MoldStatusBadge({ status }: { status: string }) {
+  const colors: Record<string, string> = {
+    GOOD: 'text-emerald-400',
+    NEEDS_REPAIR: 'text-amber-400',
+    BROKEN: 'text-red-400',
+    MAINTENANCE: 'text-yellow-400',
+  };
+  const labels: Record<string, string> = {
+    GOOD: '╪ش┘è╪»', NEEDS_REPAIR: '┘è╪ص╪ز╪د╪ش ╪╡┘è╪د┘╪ر', BROKEN: '╪╣╪╖┘╪د┘', MAINTENANCE: '╪╡┘è╪د┘╪ر',
+  };
+  return <span className={`font-bold ${colors[status] || 'text-slate-400'}`}>{labels[status] || status}</span>;
+}
+
+function LifeCycleBadge({ status }: { status: string }) {
+  const colors: Record<string, string> = {
+    good: 'text-emerald-400', warning: 'text-yellow-400', danger: 'text-red-400',
+  };
+  const labels: Record<string, string> = { good: '╪ش┘è╪»', warning: '╪ز╪ص╪░┘è╪▒', danger: '╪«╪╖┘è╪▒' };
+  return <span className={`font-bold ${colors[status] || 'text-slate-400'}`}>{labels[status] || status}</span>;
+}
+
+export default function SemiFinishedDetailPage() {
+  const router = useRouter();
+  const params = useParams();
+  const productId = params.id as string;
+
+  const [details, setDetails] = useState<Details | null>(null);
+  const [loading, setLoading] = useState(true);
+
+  useEffect(() => {
+    const fetchDetails = async () => {
+      try {
+        const data = await api.fetchWithAuth<Details>(`/v1/manufacturing/semi-finished-products/${productId}/details`);
+        setDetails(data);
+      } catch {
+        toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪د┘╪ز┘╪د╪╡┘è┘');
+      } finally { setLoading(false); }
+    };
+    fetchDetails();
+  }, [productId]);
+
+  if (loading) return <div className="flex items-center justify-center min-h-[50vh] text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
+  if (!details) return <div className="flex items-center justify-center min-h-[50vh] text-white text-xl">╪د┘┘à┘╪ز╪ش ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>;
+
+  const { product, mold, bestMachine, costBreakdown } = details;
+
+  return (
+    <>
+      <header className="bg-white/5 backdrop-blur-xl border-b border-white/10 sticky top-0 z-40">
+        <div className="px-8 py-5 flex justify-between items-center">
+          <div className="flex items-center gap-4">
+            <button onClick={() => router.push('/inventory2/semi-finished')} className="p-2 hover:bg-white/5 rounded-full transition text-white/50 hover:text-white">
+              <ArrowLeft className="w-6 h-6" />
+            </button>
+            <Factory className="w-7 h-7 text-amber-400" />
+            <h1 className="text-2xl font-black text-white">{product.name}</h1>
+          </div>
+        </div>
+      </header>
+
+      <main className="px-8 py-8">
+        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
+          <InfoCard title="┘à╪╣┘┘ê┘à╪د╪ز ╪د┘┘à┘╪ز╪ش" icon={<Package className="w-5 h-5 text-blue-400" />}>
+            <DetailRow label="╪د┘╪د╪│┘à" value={product.name} />
+            <DetailRow label="╪د┘┘ê╪ص╪»╪ر" value={product.unit} />
+            <DetailRow label="┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر" value={`${product.cost_price.toFixed(2)} ╪ش.┘à`} valueClass="text-green-400" />
+            <DetailRow label="╪│╪╣╪▒ ╪د┘╪ذ┘è╪╣" value={`${product.selling_price.toFixed(2)} ╪ش.┘à`} valueClass="text-blue-400" />
+            <DetailRow label="╪د┘┘à╪«╪▓┘ê┘ ╪د┘╪ص╪د┘┘è" value={`${product.stock_quantity?.toLocaleString() || 0} ${product.unit || '┘é╪╖╪╣╪ر'}`} valueClass="text-amber-400" />
+          </InfoCard>
+
+          <InfoCard title="┘à╪╣┘┘ê┘à╪د╪ز ╪د┘┘é╪د┘╪ذ (╪د┘╪ح╪│╪╖┘à╪ذ╪ر)" icon={<Target className="w-5 h-5 text-purple-400" />}>
+            {mold ? (
+              <>
+                <DetailRow label="╪د╪│┘à ╪د┘┘é╪د┘╪ذ" value={mold.name} />
+                <DetailRow label="┘ê╪▓┘ ╪د┘┘é╪╖╪╣╪ر" value={`${mold.product_weight} ╪ش╪▒╪د┘à`} />
+                <DetailRow label="╪╣╪»╪» ╪د┘╪ز╪ش╪د┘ê┘è┘" value={String(mold.cavities)} />
+                <DetailRow label="╪د┘╪ص╪د┘╪ر" value={<MoldStatusBadge status={mold.status} />} />
+                <DetailRow label="╪»┘ê╪▒╪ر ╪د┘╪ص┘è╪د╪ر" value={<LifeCycleBadge status={mold.life_cycle_status} />} />
+                <DetailRow label="╪╣╪»╪» ╪د┘╪╖┘┘é╪د╪ز" value={`${mold.current_shots?.toLocaleString() || 0} / ${mold.max_shots?.toLocaleString() || 'ظêئ'}`} />
+              </>
+            ) : (
+              <p className="text-slate-500">┘╪د ┘è┘ê╪ش╪» ┘é╪د┘╪ذ ┘à╪▒╪ز╪ذ╪╖</p>
+            )}
+          </InfoCard>
+
+          <InfoCard title="╪ث┘╪╢┘ ┘à╪د┘â┘è┘╪ر ┘┘╪ز╪┤╪║┘è┘" icon={<Cpu className="w-5 h-5 text-cyan-400" />}>
+            {bestMachine ? (
+              <>
+                <DetailRow label="╪د╪│┘à ╪د┘┘à╪د┘â┘è┘╪ر" value={bestMachine.name} />
+                <DetailRow label="┘é╪»╪▒╪ر ╪د┘┘à╪د┘â┘è┘╪ر" value={`${bestMachine.power_consumption} kW`} />
+                <DetailRow label="╪╣╪»╪» ┘à╪▒╪د╪ز ╪د┘╪ز╪┤╪║┘è┘" value={`${bestMachine.runs} ┘à╪▒╪ر`} />
+                <DetailRow label="╪ح╪ش┘à╪د┘┘è ╪د┘┘é╪╖╪╣ ╪د┘┘à┘╪ز╪ش╪ر" value={`${bestMachine.total_pieces?.toLocaleString() || 0} ┘é╪╖╪╣╪ر`} />
+              </>
+            ) : (
+              <p className="text-slate-500">┘╪د ╪ز┘ê╪ش╪» ╪ذ┘è╪د┘╪د╪ز ┘â╪د┘┘è╪ر</p>
+            )}
+          </InfoCard>
+
+          {costBreakdown && (
+            <div className="lg:col-span-2">
+              <InfoCard title="╪ز╪ص┘┘è┘ ╪د┘╪ز┘â┘┘╪ر" icon={<DollarSign className="w-5 h-5 text-emerald-400" />}>
+                <div className="space-y-6">
+                  <div className="bg-blue-500/5 border border-blue-500/20 rounded-xl p-4">
+                    <p className="text-blue-200 font-bold mb-2">┘à╪ز┘ê╪│╪╖ ╪ت╪«╪▒ {costBreakdown.monthDetails.length} ╪ث╪┤┘ç╪▒ (┘é╪ذ┘ {costBreakdown.monthStr}):</p>
+                    <div className="flex gap-2 flex-wrap mb-2">
+                      {costBreakdown.monthDetails.map((m) => (
+                        <span key={m.month} className="bg-blue-500/20 px-2.5 py-1 rounded-lg text-sm text-slate-300">{m.month}: {m.amount.toFixed(0)} ╪ش.┘à</span>
+                      ))}
+                    </div>
+                    <div className="text-sm text-slate-400 space-y-0.5">
+                      <p>┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â╪د┘┘è┘ ╪د┘╪س╪د╪ذ╪ز╪ر: <span className="text-white">{costBreakdown.avgTotalFixedCost.toFixed(0)} ╪ش.┘à/╪┤┘ç╪▒</span></p>
+                      <p>┘à╪ز┘ê╪│╪╖ ┘╪د╪ز┘ê╪▒╪ر ╪د┘┘â┘ç╪▒╪ذ╪د╪ة: <span className="text-white">{costBreakdown.avgTotalElectricity.toFixed(0)} ╪ش.┘à/╪┤┘ç╪▒</span></p>
+                      <p>┘à╪ز┘ê╪│╪╖ ╪ث┘è╪د┘à ╪د┘╪╣┘à┘: <span className="text-white">{costBreakdown.avgActiveDays.toFixed(0)} ┘è┘ê┘à/╪┤┘ç╪▒</span></p>
+                    </div>
+                  </div>
+
+                  <div className="bg-slate-800/50 rounded-xl p-4 border border-slate-700">
+                    <h3 className="text-lg font-bold text-blue-300 mb-3">1. ╪ز┘â┘┘╪ر ╪د┘╪«╪د┘à</h3>
+                    <div className="text-slate-300 text-sm space-y-1">
+                      <p>╪│╪╣╪▒ ╪د┘┘à╪د╪»╪ر ╪د┘╪«╪د┘à: <span className="text-white font-bold">{costBreakdown.rawMaterialPrice.toFixed(2)} ╪ش.┘à / ┘â╪ش┘à</span></p>
+                      <p>┘ê╪▓┘ ╪د┘┘é╪╖╪╣╪ر: <span className="text-white font-bold">{costBreakdown.pieceWeightGrams} ╪ش╪▒╪د┘à</span></p>
+                      <div className="bg-black/20 rounded-lg p-3 mt-2 font-mono text-sm text-emerald-300">
+                        ╪ز┘â┘┘╪ر ╪د┘╪«╪د┘à = ({costBreakdown.rawMaterialPrice.toFixed(2)} ├╖ 1000) ├ù {costBreakdown.pieceWeightGrams}
+                        <br />
+                        = <span className="text-yellow-300 font-bold">{costBreakdown.rawCostPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
+                      </div>
+                    </div>
+                  </div>
+
+                  <div className="bg-slate-800/50 rounded-xl p-4 border border-slate-700">
+                    <h3 className="text-lg font-bold text-blue-300 mb-3">2. ╪د┘╪ز┘â╪د┘┘è┘ ╪د┘╪س╪د╪ذ╪ز╪ر ┘ê╪د┘┘â┘ç╪▒╪ذ╪د╪ة ┘ê╪د┘╪ح┘ç┘╪د┘â</h3>
+                    <div className="text-slate-300 text-sm space-y-1">
+                      <p>┘à╪╣╪»┘ ╪س╪د╪ذ╪ز (╪║┘è╪▒ ╪د┘┘â┘ç╪▒╪ذ╪د╪ة): <span className="text-white font-bold">{costBreakdown.nonElectricHourly.toFixed(4)} ╪ش.┘à / ╪│╪د╪╣╪ر</span></p>
+                      <p>┘é╪»╪▒╪ر ╪د┘┘à╪د┘â┘è┘╪ر: <span className="text-white font-bold">{costBreakdown.machinePowerKw} kW</span></p>
+                      <p>╪ز┘â┘┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة/╪│╪د╪╣╪ر ╪ز╪┤╪║┘è┘: <span className="text-white font-bold">{costBreakdown.electricityHourly.toFixed(4)} ╪ش.┘à</span></p>
+                      {costBreakdown.machineDepreciationHourly > 0 && (
+                        <p>╪ح┘ç┘╪د┘â ╪د┘┘à╪د┘â┘è┘╪ر/╪│╪د╪╣╪ر: <span className="text-white font-bold">{costBreakdown.machineDepreciationHourly.toFixed(4)} ╪ش.┘à</span></p>
+                      )}
+                      <p>╪ز┘â┘┘╪ر ╪د┘╪│╪د╪╣╪ر ╪د┘┘â┘┘è╪ر ┘┘┘à╪د┘â┘è┘╪ر: <span className="text-white font-bold">{costBreakdown.machineHourly.toFixed(4)} ╪ش.┘à</span></p>
+                      <p>╪│╪د╪╣╪د╪ز ╪د┘╪ز╪┤╪║┘è┘: <span className="text-white font-bold">{costBreakdown.hoursWorked} ╪│╪د╪╣╪د╪ز</span></p>
+                      <p>╪د┘┘é╪╖╪╣ ╪د┘┘à┘é╪»╪▒╪ر: <span className="text-white font-bold">{costBreakdown.estimatedPieces?.toLocaleString() || 0} ┘é╪╖╪╣╪ر</span></p>
+                      <div className="bg-black/20 rounded-lg p-3 mt-2 font-mono text-sm text-emerald-300">
+                        ╪ز┘â┘┘╪ر ╪س╪د╪ذ╪ز╪ر ┘┘┘é╪╖╪╣╪ر = ({costBreakdown.machineHourly.toFixed(4)} ├ù {costBreakdown.hoursWorked}) ├╖ {costBreakdown.estimatedPieces}
+                        <br />
+                        = <span className="text-yellow-300 font-bold">{costBreakdown.fixedPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
+                      </div>
+                    </div>
+                  </div>
+
+                  {costBreakdown.moldAmortizationPerPiece > 0 && (
+                    <div className="bg-purple-500/5 border border-purple-500/20 rounded-xl p-4">
+                      <h3 className="text-lg font-bold text-purple-300 mb-3">3. ╪ح┘ç┘╪د┘â ╪د┘┘é╪د┘╪ذ</h3>
+                      <div className="text-slate-300 text-sm space-y-1">
+                        <p>╪│╪╣╪▒ ╪د┘┘é╪د┘╪ذ: <span className="text-white font-bold">{details.mold?.price.toLocaleString()} ╪ش.┘à</span></p>
+                        <p>╪د┘╪╣┘à╪▒ ╪د┘╪د┘╪ز╪▒╪د╪╢┘è: <span className="text-white font-bold">{details.mold?.max_shots?.toLocaleString()} ╪┤┘ê╪ز</span></p>
+                        <div className="bg-black/20 rounded-lg p-3 mt-2 font-mono text-sm text-emerald-300">
+                          ╪ح┘ç┘╪د┘â ╪د┘┘é╪د┘╪ذ ┘┘┘é╪╖╪╣╪ر = ({details.mold?.price?.toLocaleString()} ├ù 1.5) ├╖ {details.mold?.max_shots?.toLocaleString()}
+                          <br />
+                          = <span className="text-yellow-300 font-bold">{costBreakdown.moldAmortizationPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
+                        </div>
+                      </div>
+                    </div>
+                  )}
+
+                  <div className="bg-emerald-500/5 border border-emerald-500/20 rounded-xl p-4">
+                    <h3 className="text-lg font-bold text-emerald-300 mb-3">
+                      {costBreakdown.moldAmortizationPerPiece > 0 ? '4' : '3'}. ╪ح╪ش┘à╪د┘┘è ╪د┘╪ز┘â┘┘╪ر ╪د┘╪ز┘é╪»┘è╪▒┘è╪ر ┘┘┘é╪╖╪╣╪ر
+                    </h3>
+                    <div className="bg-black/20 rounded-lg p-4 font-mono text-sm">
+                      <span className="text-slate-400">╪د┘╪ز┘â┘┘╪ر ╪د┘┘â┘┘è╪ر</span>
+                      <br />
+                      = {costBreakdown.rawCostPerPiece.toFixed(4)} (╪«╪د┘à) + {costBreakdown.fixedPerPiece.toFixed(4)} (╪س╪د╪ذ╪ز + ┘â┘ç╪▒╪ذ╪د╪ة{costBreakdown.machineDepreciationHourly > 0 ? ' + ╪ح┘ç┘╪د┘â ┘à╪د┘â┘è┘╪ر' : ''}){costBreakdown.moldAmortizationPerPiece > 0 ? ` + ${costBreakdown.moldAmortizationPerPiece.toFixed(4)} (╪ح┘ç┘╪د┘â ┘é╪د┘╪ذ)` : ''}
+                      <br />
+                      = <span className="text-yellow-300 font-bold text-lg">{costBreakdown.totalPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
+                    </div>
+                    <div className="mt-3 text-slate-500 text-xs">
+                      * ┘à┘╪د╪ص╪╕╪ر: ┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر ╪د┘┘╪╣┘┘è ┘┘┘à┘╪ز╪ش ({product.cost_price.toFixed(2)} ╪ش.┘à) ┘ç┘ê WAC ╪╣╪ذ╪▒ ┘â┘ ╪»┘╪╣╪د╪ز ╪د┘╪ح┘╪ز╪د╪ش ╪د┘╪│╪د╪ذ┘é╪ر╪î ┘ê┘é╪» ┘è╪«╪ز┘┘ ╪╣┘ ╪د┘╪ز┘é╪»┘è╪▒ ╪د┘╪ص╪د┘┘è.
+                    </div>
+                  </div>
+                </div>
+              </InfoCard>
+            </div>
+          )}
+        </div>
+      </main>
+    </>
+  );
+}
diff --git a/frontend/app/inventory2/semi-finished/[id]/page.tsx b/frontend/app/inventory2/semi-finished/[id]/page.tsx
index 023c736..46c0b80 100644
--- a/frontend/app/inventory2/semi-finished/[id]/page.tsx
+++ b/frontend/app/inventory2/semi-finished/[id]/page.tsx
@@ -1,235 +1,9 @@
-'use client';
+import SemiFinishedClientPage from './client';
 
-import { useEffect, useState } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-import { toast } from 'sonner';
-import { Factory, Package, Target, Cpu, DollarSign, ArrowLeft, Scale } from 'lucide-react';
-import PageShell from '@/components/inventory2/PageShell';
-import InfoCard, { DetailRow } from '@/components/inventory2/InfoCard';
-
-interface ProductInfo {
-  id: number; name: string; cost_price: number; selling_price: number;
-  stock_quantity: number; unit: string;
-}
-
-interface MoldInfo {
-  id: number; name: string; price: number; product_weight: number;
-  cavities: number; current_shots: number; max_shots: number;
-  status: string; life_cycle_status: string;
-}
-
-interface BestMachine {
-  id: number; name: string; price: number; useful_life_years: number;
-  power_consumption: number; runs: number; total_pieces: number;
-}
-
-interface CostBreakdown {
-  rawMaterialPrice: number; pieceWeightGrams: number; rawCostPerPiece: number;
-  nonElectricHourly: number; machinePowerKw: number;
-  electricityPerMachineHour: number; electricityHourly: number;
-  machineDepreciationHourly: number; machineHourly: number;
-  hoursWorked: number; estimatedPieces: number; fixedPerPiece: number;
-  moldAmortizationPerPiece: number; totalPerPiece: number;
-  avgTotalFixedCost: number; avgTotalElectricity: number; avgActiveDays: number;
-  monthStr: string; monthDetails: { month: string; amount: number }[];
-}
-
-interface Details {
-  product: ProductInfo; mold: MoldInfo | null;
-  bestMachine: BestMachine | null; costBreakdown: CostBreakdown | null;
-}
-
-function MoldStatusBadge({ status }: { status: string }) {
-  const colors: Record<string, string> = {
-    GOOD: 'text-emerald-400',
-    NEEDS_REPAIR: 'text-amber-400',
-    BROKEN: 'text-red-400',
-    MAINTENANCE: 'text-yellow-400',
-  };
-  const labels: Record<string, string> = {
-    GOOD: '╪ش┘è╪»', NEEDS_REPAIR: '┘è╪ص╪ز╪د╪ش ╪╡┘è╪د┘╪ر', BROKEN: '╪╣╪╖┘╪د┘', MAINTENANCE: '╪╡┘è╪د┘╪ر',
-  };
-  return <span className={`font-bold ${colors[status] || 'text-slate-400'}`}>{labels[status] || status}</span>;
-}
-
-function LifeCycleBadge({ status }: { status: string }) {
-  const colors: Record<string, string> = {
-    good: 'text-emerald-400', warning: 'text-yellow-400', danger: 'text-red-400',
-  };
-  const labels: Record<string, string> = { good: '╪ش┘è╪»', warning: '╪ز╪ص╪░┘è╪▒', danger: '╪«╪╖┘è╪▒' };
-  return <span className={`font-bold ${colors[status] || 'text-slate-400'}`}>{labels[status] || status}</span>;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function SemiFinishedDetailPage() {
-  const router = useRouter();
-  const params = useParams();
-  const productId = params.id as string;
-
-  const [details, setDetails] = useState<Details | null>(null);
-  const [loading, setLoading] = useState(true);
-
-  useEffect(() => {
-    const fetchDetails = async () => {
-      try {
-        const data = await api.fetchWithAuth<Details>(`/v1/manufacturing/semi-finished-products/${productId}/details`);
-        setDetails(data);
-      } catch {
-        toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪د┘╪ز┘╪د╪╡┘è┘');
-      } finally { setLoading(false); }
-    };
-    fetchDetails();
-  }, [productId]);
-
-  if (loading) return <div className="flex items-center justify-center min-h-[50vh] text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
-  if (!details) return <div className="flex items-center justify-center min-h-[50vh] text-white text-xl">╪د┘┘à┘╪ز╪ش ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>;
-
-  const { product, mold, bestMachine, costBreakdown } = details;
-
-  return (
-    <>
-      <header className="bg-white/5 backdrop-blur-xl border-b border-white/10 sticky top-0 z-40">
-        <div className="px-8 py-5 flex justify-between items-center">
-          <div className="flex items-center gap-4">
-            <button onClick={() => router.push('/inventory2/semi-finished')} className="p-2 hover:bg-white/5 rounded-full transition text-white/50 hover:text-white">
-              <ArrowLeft className="w-6 h-6" />
-            </button>
-            <Factory className="w-7 h-7 text-amber-400" />
-            <h1 className="text-2xl font-black text-white">{product.name}</h1>
-          </div>
-        </div>
-      </header>
-
-      <main className="px-8 py-8">
-        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
-          <InfoCard title="┘à╪╣┘┘ê┘à╪د╪ز ╪د┘┘à┘╪ز╪ش" icon={<Package className="w-5 h-5 text-blue-400" />}>
-            <DetailRow label="╪د┘╪د╪│┘à" value={product.name} />
-            <DetailRow label="╪د┘┘ê╪ص╪»╪ر" value={product.unit} />
-            <DetailRow label="┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر" value={`${product.cost_price.toFixed(2)} ╪ش.┘à`} valueClass="text-green-400" />
-            <DetailRow label="╪│╪╣╪▒ ╪د┘╪ذ┘è╪╣" value={`${product.selling_price.toFixed(2)} ╪ش.┘à`} valueClass="text-blue-400" />
-            <DetailRow label="╪د┘┘à╪«╪▓┘ê┘ ╪د┘╪ص╪د┘┘è" value={`${product.stock_quantity?.toLocaleString() || 0} ${product.unit || '┘é╪╖╪╣╪ر'}`} valueClass="text-amber-400" />
-          </InfoCard>
-
-          <InfoCard title="┘à╪╣┘┘ê┘à╪د╪ز ╪د┘┘é╪د┘╪ذ (╪د┘╪ح╪│╪╖┘à╪ذ╪ر)" icon={<Target className="w-5 h-5 text-purple-400" />}>
-            {mold ? (
-              <>
-                <DetailRow label="╪د╪│┘à ╪د┘┘é╪د┘╪ذ" value={mold.name} />
-                <DetailRow label="┘ê╪▓┘ ╪د┘┘é╪╖╪╣╪ر" value={`${mold.product_weight} ╪ش╪▒╪د┘à`} />
-                <DetailRow label="╪╣╪»╪» ╪د┘╪ز╪ش╪د┘ê┘è┘" value={String(mold.cavities)} />
-                <DetailRow label="╪د┘╪ص╪د┘╪ر" value={<MoldStatusBadge status={mold.status} />} />
-                <DetailRow label="╪»┘ê╪▒╪ر ╪د┘╪ص┘è╪د╪ر" value={<LifeCycleBadge status={mold.life_cycle_status} />} />
-                <DetailRow label="╪╣╪»╪» ╪د┘╪╖┘┘é╪د╪ز" value={`${mold.current_shots?.toLocaleString() || 0} / ${mold.max_shots?.toLocaleString() || 'ظêئ'}`} />
-              </>
-            ) : (
-              <p className="text-slate-500">┘╪د ┘è┘ê╪ش╪» ┘é╪د┘╪ذ ┘à╪▒╪ز╪ذ╪╖</p>
-            )}
-          </InfoCard>
-
-          <InfoCard title="╪ث┘╪╢┘ ┘à╪د┘â┘è┘╪ر ┘┘╪ز╪┤╪║┘è┘" icon={<Cpu className="w-5 h-5 text-cyan-400" />}>
-            {bestMachine ? (
-              <>
-                <DetailRow label="╪د╪│┘à ╪د┘┘à╪د┘â┘è┘╪ر" value={bestMachine.name} />
-                <DetailRow label="┘é╪»╪▒╪ر ╪د┘┘à╪د┘â┘è┘╪ر" value={`${bestMachine.power_consumption} kW`} />
-                <DetailRow label="╪╣╪»╪» ┘à╪▒╪د╪ز ╪د┘╪ز╪┤╪║┘è┘" value={`${bestMachine.runs} ┘à╪▒╪ر`} />
-                <DetailRow label="╪ح╪ش┘à╪د┘┘è ╪د┘┘é╪╖╪╣ ╪د┘┘à┘╪ز╪ش╪ر" value={`${bestMachine.total_pieces?.toLocaleString() || 0} ┘é╪╖╪╣╪ر`} />
-              </>
-            ) : (
-              <p className="text-slate-500">┘╪د ╪ز┘ê╪ش╪» ╪ذ┘è╪د┘╪د╪ز ┘â╪د┘┘è╪ر</p>
-            )}
-          </InfoCard>
-
-          {costBreakdown && (
-            <div className="lg:col-span-2">
-              <InfoCard title="╪ز╪ص┘┘è┘ ╪د┘╪ز┘â┘┘╪ر" icon={<DollarSign className="w-5 h-5 text-emerald-400" />}>
-                <div className="space-y-6">
-                  {/* Months Info */}
-                  <div className="bg-blue-500/5 border border-blue-500/20 rounded-xl p-4">
-                    <p className="text-blue-200 font-bold mb-2">┘à╪ز┘ê╪│╪╖ ╪ت╪«╪▒ {costBreakdown.monthDetails.length} ╪ث╪┤┘ç╪▒ (┘é╪ذ┘ {costBreakdown.monthStr}):</p>
-                    <div className="flex gap-2 flex-wrap mb-2">
-                      {costBreakdown.monthDetails.map((m) => (
-                        <span key={m.month} className="bg-blue-500/20 px-2.5 py-1 rounded-lg text-sm text-slate-300">{m.month}: {m.amount.toFixed(0)} ╪ش.┘à</span>
-                      ))}
-                    </div>
-                    <div className="text-sm text-slate-400 space-y-0.5">
-                      <p>┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â╪د┘┘è┘ ╪د┘╪س╪د╪ذ╪ز╪ر: <span className="text-white">{costBreakdown.avgTotalFixedCost.toFixed(0)} ╪ش.┘à/╪┤┘ç╪▒</span></p>
-                      <p>┘à╪ز┘ê╪│╪╖ ┘╪د╪ز┘ê╪▒╪ر ╪د┘┘â┘ç╪▒╪ذ╪د╪ة: <span className="text-white">{costBreakdown.avgTotalElectricity.toFixed(0)} ╪ش.┘à/╪┤┘ç╪▒</span></p>
-                      <p>┘à╪ز┘ê╪│╪╖ ╪ث┘è╪د┘à ╪د┘╪╣┘à┘: <span className="text-white">{costBreakdown.avgActiveDays.toFixed(0)} ┘è┘ê┘à/╪┤┘ç╪▒</span></p>
-                    </div>
-                  </div>
-
-                  {/* Raw Material */}
-                  <div className="bg-slate-800/50 rounded-xl p-4 border border-slate-700">
-                    <h3 className="text-lg font-bold text-blue-300 mb-3">1. ╪ز┘â┘┘╪ر ╪د┘╪«╪د┘à</h3>
-                    <div className="text-slate-300 text-sm space-y-1">
-                      <p>╪│╪╣╪▒ ╪د┘┘à╪د╪»╪ر ╪د┘╪«╪د┘à: <span className="text-white font-bold">{costBreakdown.rawMaterialPrice.toFixed(2)} ╪ش.┘à / ┘â╪ش┘à</span></p>
-                      <p>┘ê╪▓┘ ╪د┘┘é╪╖╪╣╪ر: <span className="text-white font-bold">{costBreakdown.pieceWeightGrams} ╪ش╪▒╪د┘à</span></p>
-                      <div className="bg-black/20 rounded-lg p-3 mt-2 font-mono text-sm text-emerald-300">
-                        ╪ز┘â┘┘╪ر ╪د┘╪«╪د┘à = ({costBreakdown.rawMaterialPrice.toFixed(2)} ├╖ 1000) ├ù {costBreakdown.pieceWeightGrams}
-                        <br />
-                        = <span className="text-yellow-300 font-bold">{costBreakdown.rawCostPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
-                      </div>
-                    </div>
-                  </div>
-
-                  {/* Fixed + Electricity + Depreciation */}
-                  <div className="bg-slate-800/50 rounded-xl p-4 border border-slate-700">
-                    <h3 className="text-lg font-bold text-blue-300 mb-3">2. ╪د┘╪ز┘â╪د┘┘è┘ ╪د┘╪س╪د╪ذ╪ز╪ر ┘ê╪د┘┘â┘ç╪▒╪ذ╪د╪ة ┘ê╪د┘╪ح┘ç┘╪د┘â</h3>
-                    <div className="text-slate-300 text-sm space-y-1">
-                      <p>┘à╪╣╪»┘ ╪س╪د╪ذ╪ز (╪║┘è╪▒ ╪د┘┘â┘ç╪▒╪ذ╪د╪ة): <span className="text-white font-bold">{costBreakdown.nonElectricHourly.toFixed(4)} ╪ش.┘à / ╪│╪د╪╣╪ر</span></p>
-                      <p>┘é╪»╪▒╪ر ╪د┘┘à╪د┘â┘è┘╪ر: <span className="text-white font-bold">{costBreakdown.machinePowerKw} kW</span></p>
-                      <p>╪ز┘â┘┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة/╪│╪د╪╣╪ر ╪ز╪┤╪║┘è┘: <span className="text-white font-bold">{costBreakdown.electricityHourly.toFixed(4)} ╪ش.┘à</span></p>
-                      {costBreakdown.machineDepreciationHourly > 0 && (
-                        <p>╪ح┘ç┘╪د┘â ╪د┘┘à╪د┘â┘è┘╪ر/╪│╪د╪╣╪ر: <span className="text-white font-bold">{costBreakdown.machineDepreciationHourly.toFixed(4)} ╪ش.┘à</span></p>
-                      )}
-                      <p>╪ز┘â┘┘╪ر ╪د┘╪│╪د╪╣╪ر ╪د┘┘â┘┘è╪ر ┘┘┘à╪د┘â┘è┘╪ر: <span className="text-white font-bold">{costBreakdown.machineHourly.toFixed(4)} ╪ش.┘à</span></p>
-                      <p>╪│╪د╪╣╪د╪ز ╪د┘╪ز╪┤╪║┘è┘: <span className="text-white font-bold">{costBreakdown.hoursWorked} ╪│╪د╪╣╪د╪ز</span></p>
-                      <p>╪د┘┘é╪╖╪╣ ╪د┘┘à┘é╪»╪▒╪ر: <span className="text-white font-bold">{costBreakdown.estimatedPieces?.toLocaleString() || 0} ┘é╪╖╪╣╪ر</span></p>
-                      <div className="bg-black/20 rounded-lg p-3 mt-2 font-mono text-sm text-emerald-300">
-                        ╪ز┘â┘┘╪ر ╪س╪د╪ذ╪ز╪ر ┘┘┘é╪╖╪╣╪ر = ({costBreakdown.machineHourly.toFixed(4)} ├ù {costBreakdown.hoursWorked}) ├╖ {costBreakdown.estimatedPieces}
-                        <br />
-                        = <span className="text-yellow-300 font-bold">{costBreakdown.fixedPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
-                      </div>
-                    </div>
-                  </div>
-
-                  {/* Mold Amortization */}
-                  {costBreakdown.moldAmortizationPerPiece > 0 && (
-                    <div className="bg-purple-500/5 border border-purple-500/20 rounded-xl p-4">
-                      <h3 className="text-lg font-bold text-purple-300 mb-3">3. ╪ح┘ç┘╪د┘â ╪د┘┘é╪د┘╪ذ</h3>
-                      <div className="text-slate-300 text-sm space-y-1">
-                        <p>╪│╪╣╪▒ ╪د┘┘é╪د┘╪ذ: <span className="text-white font-bold">{details.mold?.price.toLocaleString()} ╪ش.┘à</span></p>
-                        <p>╪د┘╪╣┘à╪▒ ╪د┘╪د┘╪ز╪▒╪د╪╢┘è: <span className="text-white font-bold">{details.mold?.max_shots?.toLocaleString()} ╪┤┘ê╪ز</span></p>
-                        <div className="bg-black/20 rounded-lg p-3 mt-2 font-mono text-sm text-emerald-300">
-                          ╪ح┘ç┘╪د┘â ╪د┘┘é╪د┘╪ذ ┘┘┘é╪╖╪╣╪ر = ({details.mold?.price?.toLocaleString()} ├ù 1.5) ├╖ {details.mold?.max_shots?.toLocaleString()}
-                          <br />
-                          = <span className="text-yellow-300 font-bold">{costBreakdown.moldAmortizationPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
-                        </div>
-                      </div>
-                    </div>
-                  )}
-
-                  {/* Total */}
-                  <div className="bg-emerald-500/5 border border-emerald-500/20 rounded-xl p-4">
-                    <h3 className="text-lg font-bold text-emerald-300 mb-3">
-                      {costBreakdown.moldAmortizationPerPiece > 0 ? '4' : '3'}. ╪ح╪ش┘à╪د┘┘è ╪د┘╪ز┘â┘┘╪ر ╪د┘╪ز┘é╪»┘è╪▒┘è╪ر ┘┘┘é╪╖╪╣╪ر
-                    </h3>
-                    <div className="bg-black/20 rounded-lg p-4 font-mono text-sm">
-                      <span className="text-slate-400">╪د┘╪ز┘â┘┘╪ر ╪د┘┘â┘┘è╪ر</span>
-                      <br />
-                      = {costBreakdown.rawCostPerPiece.toFixed(4)} (╪«╪د┘à) + {costBreakdown.fixedPerPiece.toFixed(4)} (╪س╪د╪ذ╪ز + ┘â┘ç╪▒╪ذ╪د╪ة{costBreakdown.machineDepreciationHourly > 0 ? ' + ╪ح┘ç┘╪د┘â ┘à╪د┘â┘è┘╪ر' : ''}){costBreakdown.moldAmortizationPerPiece > 0 ? ` + ${costBreakdown.moldAmortizationPerPiece.toFixed(4)} (╪ح┘ç┘╪د┘â ┘é╪د┘╪ذ)` : ''}
-                      <br />
-                      = <span className="text-yellow-300 font-bold text-lg">{costBreakdown.totalPerPiece.toFixed(4)} ╪ش.┘à / ┘é╪╖╪╣╪ر</span>
-                    </div>
-                    <div className="mt-3 text-slate-500 text-xs">
-                      * ┘à┘╪د╪ص╪╕╪ر: ┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر ╪د┘┘╪╣┘┘è ┘┘┘à┘╪ز╪ش ({product.cost_price.toFixed(2)} ╪ش.┘à) ┘ç┘ê WAC ╪╣╪ذ╪▒ ┘â┘ ╪»┘╪╣╪د╪ز ╪د┘╪ح┘╪ز╪د╪ش ╪د┘╪│╪د╪ذ┘é╪ر╪î ┘ê┘é╪» ┘è╪«╪ز┘┘ ╪╣┘ ╪د┘╪ز┘é╪»┘è╪▒ ╪د┘╪ص╪د┘┘è.
-                    </div>
-                  </div>
-                </div>
-              </InfoCard>
-            </div>
-          )}
-        </div>
-      </main>
-    </>
-  );
+export default function Page() {
+  return <SemiFinishedClientPage />;
 }
diff --git a/frontend/app/inventory2/warehouses/[id]/client.tsx b/frontend/app/inventory2/warehouses/[id]/client.tsx
new file mode 100644
index 0000000..ff77e5c
--- /dev/null
+++ b/frontend/app/inventory2/warehouses/[id]/client.tsx
@@ -0,0 +1,246 @@
+'use client';
+
+import { useEffect, useState, useCallback } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+import { toast } from 'sonner';
+import { Package, ShoppingCart, Truck, ArrowRightLeft, Search, X } from 'lucide-react';
+import StatCards from '@/components/inventory2/StatCards';
+import { TypeBadge } from '@/components/inventory2/Badge';
+import { Modal } from '@/components/inventory2/Modal';
+
+interface StockItem {
+  product_id: number;
+  product_name: string;
+  product_sku?: string;
+  product_type: string;
+  quantity: string;
+  unit: string;
+}
+
+interface WarehouseDetail {
+  id: number;
+  name: string;
+  location?: string;
+  is_active: boolean;
+}
+
+export default function WarehouseDetailPage() {
+  const router = useRouter();
+  const params = useParams();
+  const warehouseId = Number(params.id);
+
+  const [warehouse, setWarehouse] = useState<WarehouseDetail | null>(null);
+  const [stock, setStock] = useState<StockItem[]>([]);
+  const [otherWarehouses, setOtherWarehouses] = useState<WarehouseDetail[]>([]);
+  const [loading, setLoading] = useState(true);
+  const [search, setSearch] = useState('');
+
+  const [transferProduct, setTransferProduct] = useState<StockItem | null>(null);
+  const [transferForm, setTransferForm] = useState({ toWarehouseId: '', notes: '' });
+  const [showTransfer, setShowTransfer] = useState(false);
+
+  const loadData = useCallback(async () => {
+    setLoading(true);
+    try {
+      const [warehouseData, stockData, allWarehouses] = await Promise.all([
+        api.fetchWithAuth<WarehouseDetail>(`/inventory/warehouses/${warehouseId}`),
+        api.fetchWithAuth<StockItem[]>(`/inventory/warehouses/${warehouseId}/stock`),
+        api.fetchWithAuth<WarehouseDetail[]>('/inventory/warehouses'),
+      ]);
+      setWarehouse(warehouseData);
+      setStock(stockData || []);
+      setOtherWarehouses((allWarehouses || []).filter((w) => w.id !== warehouseId));
+    } catch {
+      toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪ذ┘è╪د┘╪د╪ز ╪د┘┘à╪«╪▓┘');
+    } finally { setLoading(false); }
+  }, [warehouseId]);
+
+  useEffect(() => {
+    loadData();
+  }, [loadData]);
+
+  const filteredStock = stock.filter((s) =>
+    !search || s.product_name.toLowerCase().includes(search.toLowerCase()) ||
+    (s.product_sku && s.product_sku.toLowerCase().includes(search.toLowerCase()))
+  );
+
+  const totalItems = stock.reduce((sum, s) => sum + Number(s.quantity), 0);
+
+  const openTransfer = (item: StockItem) => {
+    setTransferProduct(item);
+    setTransferForm({ toWarehouseId: '', notes: '' });
+    setShowTransfer(true);
+  };
+
+  const handleTransfer = async () => {
+    if (!transferProduct || !transferForm.toWarehouseId) {
+      toast.error('╪د┘╪▒╪ش╪د╪ة ╪د╪«╪ز┘è╪د╪▒ ╪د┘┘à╪«╪▓┘ ╪د┘┘ê╪ش┘ç╪ر');
+      return;
+    }
+    try {
+      await api.fetchWithAuth('/inventory/stock/transfer', {
+        method: 'POST',
+        body: JSON.stringify({
+          product_id: transferProduct.product_id,
+          from_warehouse_id: warehouseId,
+          to_warehouse_id: parseInt(transferForm.toWarehouseId),
+          notes: transferForm.notes || `┘┘é┘ ${transferProduct.product_name}`,
+        }),
+      });
+      toast.success(`╪ز┘à ┘┘é┘ "${transferProduct.product_name}" ╪ذ┘╪ش╪د╪ص`);
+      setShowTransfer(false);
+      loadData();
+    } catch {
+      toast.error('┘╪┤┘ ┘┘é┘ ╪د┘┘à┘╪ز╪ش');
+    }
+  };
+
+  if (loading) {
+    return (
+      <div className="flex items-center justify-center min-h-screen bg-slate-900">
+        <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
+      </div>
+    );
+  }
+
+  if (!warehouse) {
+    return (
+      <div className="flex items-center justify-center min-h-screen bg-slate-900">
+        <div className="text-slate-400 text-xl">╪د┘┘à╪«╪▓┘ ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>
+      </div>
+    );
+  }
+
+  const statsCards = [
+    { label: '╪╣╪»╪» ╪د┘╪ث╪╡┘╪د┘', value: stock.length, icon: <Package className="w-6 h-6 text-blue-400" />, color: 'bg-blue-500/20' },
+    { label: '╪ح╪ش┘à╪د┘┘è ╪د┘┘é╪╖╪╣', value: totalItems.toLocaleString(), icon: <ShoppingCart className="w-6 h-6 text-green-400" />, color: 'bg-green-500/20' },
+    { label: '┘à╪«╪د╪▓┘ ╪ث╪«╪▒┘ë', value: otherWarehouses.length, icon: <Truck className="w-6 h-6 text-purple-400" />, color: 'bg-purple-500/20' },
+  ];
+
+  return (
+    <>
+      <header className="bg-white/5 backdrop-blur-xl border-b border-white/10 sticky top-0 z-40">
+        <div className="px-8 py-5 flex justify-between items-center">
+          <div className="flex items-center gap-4">
+            <button onClick={() => router.push('/inventory2/warehouses')} className="p-2 hover:bg-white/5 rounded-full transition text-white/50 hover:text-white">
+              <ArrowRightLeft className="w-6 h-6 rotate-180" />
+            </button>
+            <div>
+              <h1 className="text-2xl font-black text-white">{warehouse.name}</h1>
+              {warehouse.location && <p className="text-sm text-slate-400">{warehouse.location}</p>}
+            </div>
+          </div>
+        </div>
+      </header>
+
+      <main className="px-8 py-8">
+        <StatCards cards={statsCards} />
+
+        <div className="flex flex-col sm:flex-row gap-4 mb-6 justify-between items-start sm:items-center">
+          <div className="relative w-full sm:w-72">
+            <Search className="absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-500" />
+            <input type="text" placeholder="╪ذ╪ص╪س ┘┘è ╪د┘┘à╪«╪▓┘..." value={search}
+              onChange={(e) => setSearch(e.target.value)}
+              className="w-full pr-10 pl-4 py-2.5 bg-slate-900/50 border border-white/10 rounded-xl text-white placeholder-slate-500 focus:outline-none focus:border-blue-500" />
+          </div>
+          <div className="text-sm text-slate-400">╪╣╪▒╪╢ {filteredStock.length} ┘à┘ {stock.length} ╪╡┘┘</div>
+        </div>
+
+        <div className="bg-white/5 backdrop-blur-xl rounded-2xl border border-white/10 overflow-hidden shadow-xl">
+          <div className="overflow-x-auto">
+            <table className="w-full">
+              <thead className="bg-white/5">
+                <tr>
+                  <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘à┘╪ز╪ش</th>
+                  <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘┘ê╪╣</th>
+                  <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘â┘à┘è╪ر</th>
+                  <th className="px-6 py-4 text-center text-white font-semibold text-sm">╪د┘╪ح╪ش╪▒╪د╪ة╪د╪ز</th>
+                </tr>
+              </thead>
+              <tbody className="divide-y divide-white/5">
+                {filteredStock.map((item) => {
+                  const qty = Number(item.quantity);
+                  return (
+                    <tr key={item.product_id} className="hover:bg-white/5 transition">
+                      <td className="px-6 py-4">
+                        <div className="text-white font-medium">{item.product_name}</div>
+                        {item.product_sku && <div className="text-xs text-slate-500 mt-0.5">SKU: {item.product_sku}</div>}
+                      </td>
+                      <td className="px-6 py-4"><TypeBadge type={item.product_type} /></td>
+                      <td className="px-6 py-4">
+                        <span className={`text-lg font-bold ${qty > 0 ? 'text-emerald-400' : 'text-red-400'}`}>
+                          {qty.toLocaleString()}
+                        </span>
+                        <span className="text-xs text-slate-500 mr-1">{item.unit || '┘é╪╖╪╣╪ر'}</span>
+                      </td>
+                      <td className="px-6 py-4 text-center">
+                        {otherWarehouses.length > 0 && (
+                          <button onClick={() => openTransfer(item)}
+                            className="px-3 py-1.5 bg-amber-500/10 hover:bg-amber-500/20 text-amber-300 rounded-lg text-sm transition inline-flex items-center gap-1.5">
+                            <ArrowRightLeft className="w-3.5 h-3.5" /> ╪ز╪ص┘ê┘è┘
+                          </button>
+                        )}
+                      </td>
+                    </tr>
+                  );
+                })}
+                {filteredStock.length === 0 && (
+                  <tr>
+                    <td colSpan={4} className="px-6 py-12 text-center text-slate-500">
+                      <Package className="w-10 h-10 mx-auto mb-2 text-slate-600" />
+                      <p>{search ? '┘╪د ╪ز┘ê╪ش╪» ┘╪ز╪د╪خ╪ش ┘┘╪ذ╪ص╪س' : '┘ç╪░╪د ╪د┘┘à╪«╪▓┘ ┘╪د╪▒╪║'}</p>
+                    </td>
+                  </tr>
+                )}
+              </tbody>
+            </table>
+          </div>
+        </div>
+      </main>
+
+      {showTransfer && transferProduct && (
+        <div className="fixed inset-0 bg-black/60 backdrop-blur-sm flex items-center justify-center z-50 p-4" onClick={() => setShowTransfer(false)}>
+          <div className="bg-slate-800 p-6 rounded-2xl w-full max-w-md border border-white/20 shadow-2xl" onClick={(e) => e.stopPropagation()}>
+            <div className="flex items-center gap-3 mb-6">
+              <ArrowRightLeft className="w-6 h-6 text-amber-400" />
+              <h3 className="text-xl font-bold text-white">╪ز╪ص┘ê┘è┘ ┘à╪«╪▓┘ê┘</h3>
+            </div>
+
+            <div className="bg-white/5 rounded-xl p-4 mb-6 border border-white/10">
+              <div className="text-white font-medium">{transferProduct.product_name}</div>
+              <div className="text-sm text-slate-400 mt-1">
+                ╪د┘┘â┘à┘è╪ر: <span className="text-emerald-400 font-bold">{Number(transferProduct.quantity).toLocaleString()}</span> {transferProduct.unit || '┘é╪╖╪╣╪ر'}
+              </div>
+              <div className="text-xs text-slate-500 mt-1">╪│┘è╪ز┘à ┘┘é┘ ┘â╪د┘à┘ ╪د┘┘â┘à┘è╪ر ╪ح┘┘ë ╪د┘┘à╪«╪▓┘ ╪د┘╪ش╪»┘è╪»</div>
+            </div>
+
+            <div className="space-y-4">
+              <div>
+                <label className="block text-sm font-medium text-slate-300 mb-2">╪د┘┘à╪«╪▓┘ ╪د┘┘ê╪ش┘ç╪ر</label>
+                <select value={transferForm.toWarehouseId}
+                  onChange={(e) => setTransferForm({ ...transferForm, toWarehouseId: e.target.value })}
+                  className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none" autoFocus>
+                  <option value="">╪د╪«╪ز╪▒ ╪د┘┘à╪«╪▓┘...</option>
+                  {otherWarehouses.map((w) => (<option key={w.id} value={w.id}>{w.name}</option>))}
+                </select>
+              </div>
+              <div>
+                <label className="block text-sm font-medium text-slate-300 mb-2">┘à┘╪د╪ص╪╕╪د╪ز (╪د╪«╪ز┘è╪د╪▒┘è)</label>
+                <input type="text" value={transferForm.notes}
+                  onChange={(e) => setTransferForm({ ...transferForm, notes: e.target.value })}
+                  className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none"
+                  placeholder="╪│╪ذ╪ذ ╪د┘╪ز╪ص┘ê┘è┘..." />
+              </div>
+
+              <div className="flex gap-3 justify-end pt-4 border-t border-white/10">
+                <button onClick={() => setShowTransfer(false)} className="px-4 py-2 bg-slate-700/50 text-slate-200 rounded-xl hover:bg-slate-700 transition">╪ح┘╪║╪د╪ة</button>
+                <button onClick={handleTransfer} className="px-6 py-2.5 bg-gradient-to-r from-amber-600 to-orange-600 text-white rounded-xl hover:from-amber-700 hover:to-orange-700 font-bold shadow-lg shadow-amber-900/20">╪ز╪ث┘â┘è╪» ╪د┘╪ز╪ص┘ê┘è┘</button>
+              </div>
+            </div>
+          </div>
+        </div>
+      )}
+    </>
+  );
+}
diff --git a/frontend/app/inventory2/warehouses/[id]/page.tsx b/frontend/app/inventory2/warehouses/[id]/page.tsx
index ff77e5c..7d5381f 100644
--- a/frontend/app/inventory2/warehouses/[id]/page.tsx
+++ b/frontend/app/inventory2/warehouses/[id]/page.tsx
@@ -1,246 +1,9 @@
-'use client';
+import WarehouseClientPage from './client';
 
-import { useEffect, useState, useCallback } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-import { toast } from 'sonner';
-import { Package, ShoppingCart, Truck, ArrowRightLeft, Search, X } from 'lucide-react';
-import StatCards from '@/components/inventory2/StatCards';
-import { TypeBadge } from '@/components/inventory2/Badge';
-import { Modal } from '@/components/inventory2/Modal';
-
-interface StockItem {
-  product_id: number;
-  product_name: string;
-  product_sku?: string;
-  product_type: string;
-  quantity: string;
-  unit: string;
-}
-
-interface WarehouseDetail {
-  id: number;
-  name: string;
-  location?: string;
-  is_active: boolean;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function WarehouseDetailPage() {
-  const router = useRouter();
-  const params = useParams();
-  const warehouseId = Number(params.id);
-
-  const [warehouse, setWarehouse] = useState<WarehouseDetail | null>(null);
-  const [stock, setStock] = useState<StockItem[]>([]);
-  const [otherWarehouses, setOtherWarehouses] = useState<WarehouseDetail[]>([]);
-  const [loading, setLoading] = useState(true);
-  const [search, setSearch] = useState('');
-
-  const [transferProduct, setTransferProduct] = useState<StockItem | null>(null);
-  const [transferForm, setTransferForm] = useState({ toWarehouseId: '', notes: '' });
-  const [showTransfer, setShowTransfer] = useState(false);
-
-  const loadData = useCallback(async () => {
-    setLoading(true);
-    try {
-      const [warehouseData, stockData, allWarehouses] = await Promise.all([
-        api.fetchWithAuth<WarehouseDetail>(`/inventory/warehouses/${warehouseId}`),
-        api.fetchWithAuth<StockItem[]>(`/inventory/warehouses/${warehouseId}/stock`),
-        api.fetchWithAuth<WarehouseDetail[]>('/inventory/warehouses'),
-      ]);
-      setWarehouse(warehouseData);
-      setStock(stockData || []);
-      setOtherWarehouses((allWarehouses || []).filter((w) => w.id !== warehouseId));
-    } catch {
-      toast.error('┘╪┤┘ ╪ز╪ص┘à┘è┘ ╪ذ┘è╪د┘╪د╪ز ╪د┘┘à╪«╪▓┘');
-    } finally { setLoading(false); }
-  }, [warehouseId]);
-
-  useEffect(() => {
-    loadData();
-  }, [loadData]);
-
-  const filteredStock = stock.filter((s) =>
-    !search || s.product_name.toLowerCase().includes(search.toLowerCase()) ||
-    (s.product_sku && s.product_sku.toLowerCase().includes(search.toLowerCase()))
-  );
-
-  const totalItems = stock.reduce((sum, s) => sum + Number(s.quantity), 0);
-
-  const openTransfer = (item: StockItem) => {
-    setTransferProduct(item);
-    setTransferForm({ toWarehouseId: '', notes: '' });
-    setShowTransfer(true);
-  };
-
-  const handleTransfer = async () => {
-    if (!transferProduct || !transferForm.toWarehouseId) {
-      toast.error('╪د┘╪▒╪ش╪د╪ة ╪د╪«╪ز┘è╪د╪▒ ╪د┘┘à╪«╪▓┘ ╪د┘┘ê╪ش┘ç╪ر');
-      return;
-    }
-    try {
-      await api.fetchWithAuth('/inventory/stock/transfer', {
-        method: 'POST',
-        body: JSON.stringify({
-          product_id: transferProduct.product_id,
-          from_warehouse_id: warehouseId,
-          to_warehouse_id: parseInt(transferForm.toWarehouseId),
-          notes: transferForm.notes || `┘┘é┘ ${transferProduct.product_name}`,
-        }),
-      });
-      toast.success(`╪ز┘à ┘┘é┘ "${transferProduct.product_name}" ╪ذ┘╪ش╪د╪ص`);
-      setShowTransfer(false);
-      loadData();
-    } catch {
-      toast.error('┘╪┤┘ ┘┘é┘ ╪د┘┘à┘╪ز╪ش');
-    }
-  };
-
-  if (loading) {
-    return (
-      <div className="flex items-center justify-center min-h-screen bg-slate-900">
-        <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
-      </div>
-    );
-  }
-
-  if (!warehouse) {
-    return (
-      <div className="flex items-center justify-center min-h-screen bg-slate-900">
-        <div className="text-slate-400 text-xl">╪د┘┘à╪«╪▓┘ ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>
-      </div>
-    );
-  }
-
-  const statsCards = [
-    { label: '╪╣╪»╪» ╪د┘╪ث╪╡┘╪د┘', value: stock.length, icon: <Package className="w-6 h-6 text-blue-400" />, color: 'bg-blue-500/20' },
-    { label: '╪ح╪ش┘à╪د┘┘è ╪د┘┘é╪╖╪╣', value: totalItems.toLocaleString(), icon: <ShoppingCart className="w-6 h-6 text-green-400" />, color: 'bg-green-500/20' },
-    { label: '┘à╪«╪د╪▓┘ ╪ث╪«╪▒┘ë', value: otherWarehouses.length, icon: <Truck className="w-6 h-6 text-purple-400" />, color: 'bg-purple-500/20' },
-  ];
-
-  return (
-    <>
-      <header className="bg-white/5 backdrop-blur-xl border-b border-white/10 sticky top-0 z-40">
-        <div className="px-8 py-5 flex justify-between items-center">
-          <div className="flex items-center gap-4">
-            <button onClick={() => router.push('/inventory2/warehouses')} className="p-2 hover:bg-white/5 rounded-full transition text-white/50 hover:text-white">
-              <ArrowRightLeft className="w-6 h-6 rotate-180" />
-            </button>
-            <div>
-              <h1 className="text-2xl font-black text-white">{warehouse.name}</h1>
-              {warehouse.location && <p className="text-sm text-slate-400">{warehouse.location}</p>}
-            </div>
-          </div>
-        </div>
-      </header>
-
-      <main className="px-8 py-8">
-        <StatCards cards={statsCards} />
-
-        <div className="flex flex-col sm:flex-row gap-4 mb-6 justify-between items-start sm:items-center">
-          <div className="relative w-full sm:w-72">
-            <Search className="absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-500" />
-            <input type="text" placeholder="╪ذ╪ص╪س ┘┘è ╪د┘┘à╪«╪▓┘..." value={search}
-              onChange={(e) => setSearch(e.target.value)}
-              className="w-full pr-10 pl-4 py-2.5 bg-slate-900/50 border border-white/10 rounded-xl text-white placeholder-slate-500 focus:outline-none focus:border-blue-500" />
-          </div>
-          <div className="text-sm text-slate-400">╪╣╪▒╪╢ {filteredStock.length} ┘à┘ {stock.length} ╪╡┘┘</div>
-        </div>
-
-        <div className="bg-white/5 backdrop-blur-xl rounded-2xl border border-white/10 overflow-hidden shadow-xl">
-          <div className="overflow-x-auto">
-            <table className="w-full">
-              <thead className="bg-white/5">
-                <tr>
-                  <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘à┘╪ز╪ش</th>
-                  <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘┘ê╪╣</th>
-                  <th className="px-6 py-4 text-right text-white font-semibold text-sm">╪د┘┘â┘à┘è╪ر</th>
-                  <th className="px-6 py-4 text-center text-white font-semibold text-sm">╪د┘╪ح╪ش╪▒╪د╪ة╪د╪ز</th>
-                </tr>
-              </thead>
-              <tbody className="divide-y divide-white/5">
-                {filteredStock.map((item) => {
-                  const qty = Number(item.quantity);
-                  return (
-                    <tr key={item.product_id} className="hover:bg-white/5 transition">
-                      <td className="px-6 py-4">
-                        <div className="text-white font-medium">{item.product_name}</div>
-                        {item.product_sku && <div className="text-xs text-slate-500 mt-0.5">SKU: {item.product_sku}</div>}
-                      </td>
-                      <td className="px-6 py-4"><TypeBadge type={item.product_type} /></td>
-                      <td className="px-6 py-4">
-                        <span className={`text-lg font-bold ${qty > 0 ? 'text-emerald-400' : 'text-red-400'}`}>
-                          {qty.toLocaleString()}
-                        </span>
-                        <span className="text-xs text-slate-500 mr-1">{item.unit || '┘é╪╖╪╣╪ر'}</span>
-                      </td>
-                      <td className="px-6 py-4 text-center">
-                        {otherWarehouses.length > 0 && (
-                          <button onClick={() => openTransfer(item)}
-                            className="px-3 py-1.5 bg-amber-500/10 hover:bg-amber-500/20 text-amber-300 rounded-lg text-sm transition inline-flex items-center gap-1.5">
-                            <ArrowRightLeft className="w-3.5 h-3.5" /> ╪ز╪ص┘ê┘è┘
-                          </button>
-                        )}
-                      </td>
-                    </tr>
-                  );
-                })}
-                {filteredStock.length === 0 && (
-                  <tr>
-                    <td colSpan={4} className="px-6 py-12 text-center text-slate-500">
-                      <Package className="w-10 h-10 mx-auto mb-2 text-slate-600" />
-                      <p>{search ? '┘╪د ╪ز┘ê╪ش╪» ┘╪ز╪د╪خ╪ش ┘┘╪ذ╪ص╪س' : '┘ç╪░╪د ╪د┘┘à╪«╪▓┘ ┘╪د╪▒╪║'}</p>
-                    </td>
-                  </tr>
-                )}
-              </tbody>
-            </table>
-          </div>
-        </div>
-      </main>
-
-      {showTransfer && transferProduct && (
-        <div className="fixed inset-0 bg-black/60 backdrop-blur-sm flex items-center justify-center z-50 p-4" onClick={() => setShowTransfer(false)}>
-          <div className="bg-slate-800 p-6 rounded-2xl w-full max-w-md border border-white/20 shadow-2xl" onClick={(e) => e.stopPropagation()}>
-            <div className="flex items-center gap-3 mb-6">
-              <ArrowRightLeft className="w-6 h-6 text-amber-400" />
-              <h3 className="text-xl font-bold text-white">╪ز╪ص┘ê┘è┘ ┘à╪«╪▓┘ê┘</h3>
-            </div>
-
-            <div className="bg-white/5 rounded-xl p-4 mb-6 border border-white/10">
-              <div className="text-white font-medium">{transferProduct.product_name}</div>
-              <div className="text-sm text-slate-400 mt-1">
-                ╪د┘┘â┘à┘è╪ر: <span className="text-emerald-400 font-bold">{Number(transferProduct.quantity).toLocaleString()}</span> {transferProduct.unit || '┘é╪╖╪╣╪ر'}
-              </div>
-              <div className="text-xs text-slate-500 mt-1">╪│┘è╪ز┘à ┘┘é┘ ┘â╪د┘à┘ ╪د┘┘â┘à┘è╪ر ╪ح┘┘ë ╪د┘┘à╪«╪▓┘ ╪د┘╪ش╪»┘è╪»</div>
-            </div>
-
-            <div className="space-y-4">
-              <div>
-                <label className="block text-sm font-medium text-slate-300 mb-2">╪د┘┘à╪«╪▓┘ ╪د┘┘ê╪ش┘ç╪ر</label>
-                <select value={transferForm.toWarehouseId}
-                  onChange={(e) => setTransferForm({ ...transferForm, toWarehouseId: e.target.value })}
-                  className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none" autoFocus>
-                  <option value="">╪د╪«╪ز╪▒ ╪د┘┘à╪«╪▓┘...</option>
-                  {otherWarehouses.map((w) => (<option key={w.id} value={w.id}>{w.name}</option>))}
-                </select>
-              </div>
-              <div>
-                <label className="block text-sm font-medium text-slate-300 mb-2">┘à┘╪د╪ص╪╕╪د╪ز (╪د╪«╪ز┘è╪د╪▒┘è)</label>
-                <input type="text" value={transferForm.notes}
-                  onChange={(e) => setTransferForm({ ...transferForm, notes: e.target.value })}
-                  className="w-full px-4 py-2.5 bg-slate-900/50 border border-white/20 rounded-xl text-white focus:border-blue-500 focus:outline-none"
-                  placeholder="╪│╪ذ╪ذ ╪د┘╪ز╪ص┘ê┘è┘..." />
-              </div>
-
-              <div className="flex gap-3 justify-end pt-4 border-t border-white/10">
-                <button onClick={() => setShowTransfer(false)} className="px-4 py-2 bg-slate-700/50 text-slate-200 rounded-xl hover:bg-slate-700 transition">╪ح┘╪║╪د╪ة</button>
-                <button onClick={handleTransfer} className="px-6 py-2.5 bg-gradient-to-r from-amber-600 to-orange-600 text-white rounded-xl hover:from-amber-700 hover:to-orange-700 font-bold shadow-lg shadow-amber-900/20">╪ز╪ث┘â┘è╪» ╪د┘╪ز╪ص┘ê┘è┘</button>
-              </div>
-            </div>
-          </div>
-        </div>
-      )}
-    </>
-  );
+export default function Page() {
+  return <WarehouseClientPage />;
 }
diff --git a/frontend/app/manufacturing/kiosk/[machineId]/client.tsx b/frontend/app/manufacturing/kiosk/[machineId]/client.tsx
new file mode 100644
index 0000000..c8e195c
--- /dev/null
+++ b/frontend/app/manufacturing/kiosk/[machineId]/client.tsx
@@ -0,0 +1,253 @@
+'use client';
+
+import { useEffect, useState, useCallback } from 'react';
+import { useRouter } from 'next/navigation';
+import { api } from '@/lib/api';
+import { Home, AlertTriangle, Save, Thermometer, Wrench, Zap, FileText } from 'lucide-react';
+
+interface Machine {
+    id: number;
+    name: string;
+}
+
+interface Mold {
+    id: number;
+    name: string;
+    cavities: number;
+    product_weight: number;
+}
+
+export default function MachineKioskPage({ machineId }: { machineId: string }) {
+
+    const router = useRouter();
+    const [machine, setMachine] = useState<Machine | null>(null);
+    const [loading, setLoading] = useState(true);
+    const [cycleCount, setCycleCount] = useState(0);
+    const [mold, setMold] = useState<Mold | null>(null);
+
+    const loadMachineData = useCallback(async () => {
+        try {
+            const [machineData, lastProd] = await Promise.all([
+                api.fetchWithAuth(`/manufacturing/machines/${machineId}`),
+                api.fetchWithAuth(`/manufacturing/machines/${machineId}/last-mold`)
+            ]);
+            setMachine(machineData);
+            if (lastProd) {
+                setMold(lastProd.mold);
+            }
+        } catch (error) {
+            console.error('Error loading machine data:', error);
+        } finally {
+            setLoading(false);
+        }
+    }, [machineId]);
+
+    useEffect(() => {
+        loadMachineData();
+    }, [loadMachineData]);
+
+    const handleProductionSubmit = async () => {
+        if (!mold) {
+            alert('╪د┘╪▒╪ش╪د╪ة ╪د╪«╪ز┘è╪د╪▒ ╪د┘┘é╪د┘╪ذ ╪ث┘ê┘╪د┘ï (┘à┘ ┘┘ê╪ص╪ر ╪د┘╪ح╪»╪د╪▒╪ر)');
+            return;
+        }
+
+        try {
+            await api.createProduction({
+                machine_id: Number(machineId),
+                mold_id: mold.id,
+                pieces_produced: cycleCount * (mold.cavities || 1),
+                date: new Date().toISOString(),
+                shift: 'A',
+                operator_name: '╪╣╪د┘à┘ ╪د┘┘â╪┤┘â',
+                notes: '╪ز╪│╪ش┘è┘ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è'
+            });
+
+            const btn = document.getElementById('produce-btn');
+            if (btn) {
+                btn.style.transform = 'scale(0.95)';
+                setTimeout(() => btn.style.transform = 'scale(1)', 100);
+            }
+
+            alert(`╪ز┘à ╪ز╪│╪ش┘è┘ ${cycleCount} ╪»┘ê╪▒╪ر (${cycleCount * (mold.cavities || 1)} ┘é╪╖╪╣╪ر) ╪ذ┘╪ش╪د╪ص!`);
+            setCycleCount(0);
+            router.refresh();
+        } catch (error) {
+            console.error('Production error:', error);
+            alert('╪«╪╖╪ث ┘┘è ╪د┘╪ز╪│╪ش┘è┘');
+        }
+    };
+
+    if (loading || !machine) {
+        return <div className="min-h-screen bg-slate-950 flex items-center justify-center text-white text-4xl animate-pulse">╪ش╪د╪▒┘è ┘╪ز╪ص ╪د┘┘╪╕╪د┘à...</div>;
+    }
+
+    return (
+        <div className="min-h-screen bg-slate-950 text-white flex flex-col" dir="rtl">
+            <div className="bg-slate-900 border-b border-white/10 p-6 flex justify-between items-center">
+                <div className="flex items-center gap-6">
+                    <button
+                        onClick={() => router.push('/manufacturing/kiosk')}
+                        className="w-16 h-16 bg-slate-800 rounded-2xl text-3xl flex items-center justify-center border border-white/20 hover:bg-slate-700 transition"
+                    >
+                        <Home />
+                    </button>
+                    <div>
+                        <h1 className="text-4xl font-black text-white">{machine.name}</h1>
+                        <p className="text-xl text-emerald-400 mt-1">
+                            {mold ? `╪د┘┘é╪د┘╪ذ ╪د┘╪ص╪د┘┘è: ${mold.name}` : <span className="flex items-center gap-1"><AlertTriangle /> ┘╪د ┘è┘ê╪ش╪» ┘é╪د┘╪ذ ┘à╪ص╪»╪»</span>}
+                        </p>
+                    </div>
+                </div>
+                <div className="text-left">
+                    <div className="text-2xl font-mono dir-ltr text-blue-300">
+                        {new Date().toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' })}
+                    </div>
+                    <div className="text-slate-400">
+                        {new Date().toLocaleDateString('ar-EG')}
+                    </div>
+                </div>
+            </div>
+
+            <div className="flex-1 p-8 grid grid-cols-1 lg:grid-cols-2 gap-8">
+                <div className="bg-slate-900/50 rounded-3xl border border-white/10 p-8 flex flex-col justify-center items-center">
+                    <div className="text-slate-400 text-2xl mb-4">╪╣╪»╪» ╪د┘╪»┘ê╪▒╪د╪ز (╪د┘╪╢╪▒╪ذ╪د╪ز)</div>
+                    <div className="flex items-center gap-8 mb-12">
+                        <button
+                            onClick={() => setCycleCount(Math.max(0, cycleCount - 1))}
+                            className="w-24 h-24 bg-rose-500/20 hover:bg-rose-500/30 border-2 border-rose-500/50 rounded-full text-4xl text-rose-500 transition"
+                        >
+                            -
+                        </button>
+                        <div className="text-9xl font-mono font-bold w-48 text-center">
+                            {cycleCount}
+                        </div>
+                        <button
+                            onClick={() => setCycleCount(cycleCount + 1)}
+                            className="w-24 h-24 bg-emerald-500/20 hover:bg-emerald-500/30 border-2 border-emerald-500/50 rounded-full text-4xl text-emerald-500 transition"
+                        >
+                            +
+                        </button>
+                    </div>
+
+                    <div className="grid grid-cols-3 gap-6 w-full mb-12">
+                        {[1, 5, 10, 50, 100].map(num => (
+                            <button
+                                key={num}
+                                onClick={() => setCycleCount(cycleCount + num)}
+                                className="py-4 bg-slate-800 hover:bg-slate-700 rounded-xl text-xl font-bold border border-white/10 transition"
+                            >
+                                +{num}
+                            </button>
+                        ))}
+                        <button
+                            onClick={() => setCycleCount(0)}
+                            className="py-4 bg-rose-900/30 hover:bg-rose-900/50 text-rose-400 rounded-xl text-xl font-bold border border-rose-500/20 transition"
+                        >
+                            ╪ز╪╡┘┘è╪▒
+                        </button>
+                    </div>
+
+                    <button
+                        id="produce-btn"
+                        onClick={handleProductionSubmit}
+                        disabled={cycleCount === 0}
+                        className={`
+                            w-full py-8 rounded-3xl text-4xl font-black shadow-2xl transition-all duration-200
+                            ${cycleCount > 0
+                                ? 'bg-gradient-to-r from-blue-600 to-indigo-600 hover:scale-[1.02] shadow-blue-900/50 text-white cursor-pointer'
+                                : 'bg-slate-800 text-slate-500 cursor-not-allowed'
+                            }
+                        `}
+                    >
+                        <Save /> ╪ز╪│╪ش┘è┘ ╪د┘╪ح┘╪ز╪د╪ش
+                    </button>
+                </div>
+
+                <div className="space-y-8">
+                    <div className="bg-slate-900/50 rounded-3xl border border-white/10 p-8">
+                        <h3 className="text-2xl font-bold mb-6 border-b border-white/10 pb-4">╪ح╪ص╪╡╪د╪خ┘è╪د╪ز ┘┘ê╪▒┘è╪ر</h3>
+                        <div className="space-y-6">
+                            <div className="flex justify-between items-center">
+                                <span className="text-xl text-slate-400">╪د┘┘à┘╪ز╪ش ╪د┘┘à╪ز┘ê┘é╪╣</span>
+                                <span className="text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-pink-400">
+                                    {(cycleCount * (mold?.cavities || 1)).toLocaleString()} ┘é╪╖╪╣╪ر
+                                </span>
+                            </div>
+                            <div className="flex justify-between items-center">
+                                <span className="text-xl text-slate-400">╪د┘┘ê╪▓┘ ╪د┘┘à┘é╪»╪▒</span>
+                                <span className="text-2xl font-bold text-white">
+                                    {((cycleCount * (mold?.cavities || 1) * (mold?.product_weight || 0)) / 1000).toFixed(2)} ┘â╪ش┘à
+                                </span>
+                            </div>
+                        </div>
+                    </div>
+
+                    <div className="bg-slate-900/50 rounded-3xl border border-white/10 p-8">
+                        <h3 className="text-2xl font-bold mb-6 border-b border-white/10 pb-4 text-rose-400 flex items-center gap-2"><AlertTriangle /> ╪ز┘╪ذ┘è┘ç ╪╣╪╖┘</h3>
+                        <div className="grid grid-cols-2 gap-4">
+                            <button
+                                onClick={async () => {
+                                    try {
+                                        await api.fetchWithAuth('/manufacturing/maintenance', {
+                                            method: 'POST',
+                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'HIGH_TEMP', date: new Date().toISOString(), description: '╪ص╪▒╪د╪▒╪ر ╪╣╪د┘┘è╪ر', cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
+                                        });
+                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪ذ┘╪د╪║ ╪ص╪▒╪د╪▒╪ر ╪╣╪د┘┘è╪ر');
+                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
+                                }}
+                                className="p-6 bg-rose-500/10 hover:bg-rose-500/20 border border-rose-500/30 rounded-2xl text-rose-400 font-bold transition"
+                            >
+                                <Thermometer /> ╪ص╪▒╪د╪▒╪ر ╪╣╪د┘┘è╪ر
+                            </button>
+                            <button
+                                onClick={async () => {
+                                    try {
+                                        await api.fetchWithAuth('/manufacturing/maintenance', {
+                                            method: 'POST',
+                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'MECHANICAL', date: new Date().toISOString(), description: '╪╣╪╖┘ ┘à┘è┘â╪د┘┘è┘â┘è', cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
+                                        });
+                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪ذ┘╪د╪║ ╪╣╪╖┘ ┘à┘è┘â╪د┘┘è┘â┘è');
+                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
+                                }}
+                                className="p-6 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/30 rounded-2xl text-amber-400 font-bold transition"
+                            >
+                                <Wrench /> ╪╣╪╖┘ ┘à┘è┘â╪د┘┘è┘â┘è
+                            </button>
+                            <button
+                                onClick={async () => {
+                                    try {
+                                        await api.fetchWithAuth('/manufacturing/maintenance', {
+                                            method: 'POST',
+                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'ELECTRICAL', date: new Date().toISOString(), description: '┘à╪┤┘â┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة', cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
+                                        });
+                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪ذ┘╪د╪║ ┘à╪┤┘â┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة');
+                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
+                                }}
+                                className="p-6 bg-orange-500/10 hover:bg-orange-500/20 border border-orange-500/30 rounded-2xl text-orange-400 font-bold transition"
+                            >
+                                <Zap /> ┘à╪┤┘â┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة
+                            </button>
+                            <button
+                                onClick={async () => {
+                                    const desc = prompt('┘ê╪╡┘ ╪د┘╪╣╪╖┘:');
+                                    if (!desc) return;
+                                    try {
+                                        await api.fetchWithAuth('/manufacturing/maintenance', {
+                                            method: 'POST',
+                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'OTHER', date: new Date().toISOString(), description: desc, cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
+                                        });
+                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║');
+                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
+                                }}
+                                className="p-6 bg-slate-800 hover:bg-slate-700 border border-white/10 rounded-2xl text-white font-bold transition"
+                            >
+                                <FileText /> ╪╣╪╖┘ ╪ت╪«╪▒
+                            </button>
+                        </div>
+                    </div>
+                </div>
+            </div>
+        </div>
+    );
+}
diff --git a/frontend/app/manufacturing/kiosk/[machineId]/page.tsx b/frontend/app/manufacturing/kiosk/[machineId]/page.tsx
index 6914ab8..f2877db 100644
--- a/frontend/app/manufacturing/kiosk/[machineId]/page.tsx
+++ b/frontend/app/manufacturing/kiosk/[machineId]/page.tsx
@@ -1,265 +1,10 @@
-'use client';
+import KioskClientPage from './client';
 
-import { useEffect, useState, use, useCallback } from 'react';
-import { useRouter } from 'next/navigation';
-import { api } from '@/lib/api';
-import { Home, AlertTriangle, Save, Thermometer, Wrench, Zap, FileText } from 'lucide-react';
-
-interface Machine {
-    id: number;
-    name: string;
-}
-
-interface Mold {
-    id: number;
-    name: string;
-    cavities: number;
-    product_weight: number;
+export function generateStaticParams(): { machineId: string }[] {
+  return [{ machineId: 'placeholder' }];
 }
 
-export default function MachineKioskPage({ params }: { params: Promise<{ machineId: string }> }) {
-    // Unwrap params using use() hook for Next.js 15+
-    const resolvedParams = use(params);
-    const machineId = resolvedParams.machineId;
-
-    const router = useRouter();
-    const [machine, setMachine] = useState<Machine | null>(null);
-    const [loading, setLoading] = useState(true);
-    const [cycleCount, setCycleCount] = useState(0);
-    const [mold, setMold] = useState<Mold | null>(null);
-
-    const loadMachineData = useCallback(async () => {
-        try {
-            const [machineData, lastProd] = await Promise.all([
-                api.fetchWithAuth(`/manufacturing/machines/${machineId}`),
-                api.fetchWithAuth(`/manufacturing/machines/${machineId}/last-mold`)
-            ]);
-            setMachine(machineData);
-            if (lastProd) {
-                setMold(lastProd.mold);
-            }
-        } catch (error) {
-            console.error('Error loading machine data:', error);
-        } finally {
-            setLoading(false);
-        }
-    }, [machineId]);
-
-    useEffect(() => {
-        loadMachineData();
-    }, [loadMachineData]);
-
-    const handleProductionSubmit = async () => {
-        if (!mold) {
-            alert('╪د┘╪▒╪ش╪د╪ة ╪د╪«╪ز┘è╪د╪▒ ╪د┘┘é╪د┘╪ذ ╪ث┘ê┘╪د┘ï (┘à┘ ┘┘ê╪ص╪ر ╪د┘╪ح╪»╪د╪▒╪ر)');
-            return;
-        }
-
-        try {
-            // Register production cycle
-            await api.createProduction({
-                machine_id: Number(machineId),
-                mold_id: mold.id,
-                pieces_produced: cycleCount * (mold.cavities || 1), // Total pieces
-                date: new Date().toISOString(),
-                shift: 'A', // Default shift A for simplicity, logic can be enhanced
-                operator_name: '╪╣╪د┘à┘ ╪د┘┘â╪┤┘â', // In real app, prompt for PIN or select user
-                notes: '╪ز╪│╪ش┘è┘ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è'
-            });
-
-            // Visual feedback
-            const btn = document.getElementById('produce-btn');
-            if (btn) {
-                btn.style.transform = 'scale(0.95)';
-                setTimeout(() => btn.style.transform = 'scale(1)', 100);
-            }
-
-            // Reset count or just notify
-            alert(`╪ز┘à ╪ز╪│╪ش┘è┘ ${cycleCount} ╪»┘ê╪▒╪ر (${cycleCount * (mold.cavities || 1)} ┘é╪╖╪╣╪ر) ╪ذ┘╪ش╪د╪ص!`);
-            setCycleCount(0);
-            router.refresh();
-        } catch (error) {
-            console.error('Production error:', error);
-            alert('╪«╪╖╪ث ┘┘è ╪د┘╪ز╪│╪ش┘è┘');
-        }
-    };
-
-    if (loading || !machine) {
-        return <div className="min-h-screen bg-slate-950 flex items-center justify-center text-white text-4xl animate-pulse">╪ش╪د╪▒┘è ┘╪ز╪ص ╪د┘┘╪╕╪د┘à...</div>;
-    }
-
-    return (
-        <div className="min-h-screen bg-slate-950 text-white flex flex-col" dir="rtl">
-            {/* Header */}
-            <div className="bg-slate-900 border-b border-white/10 p-6 flex justify-between items-center">
-                <div className="flex items-center gap-6">
-                    <button
-                        onClick={() => router.push('/manufacturing/kiosk')}
-                        className="w-16 h-16 bg-slate-800 rounded-2xl text-3xl flex items-center justify-center border border-white/20 hover:bg-slate-700 transition"
-                    >
-                        <Home />
-                    </button>
-                    <div>
-                        <h1 className="text-4xl font-black text-white">{machine.name}</h1>
-                        <p className="text-xl text-emerald-400 mt-1">
-                            {mold ? `╪د┘┘é╪د┘╪ذ ╪د┘╪ص╪د┘┘è: ${mold.name}` : <span className="flex items-center gap-1"><AlertTriangle /> ┘╪د ┘è┘ê╪ش╪» ┘é╪د┘╪ذ ┘à╪ص╪»╪»</span>}
-                        </p>
-                    </div>
-                </div>
-                <div className="text-left">
-                    <div className="text-2xl font-mono dir-ltr text-blue-300">
-                        {new Date().toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' })}
-                    </div>
-                    <div className="text-slate-400">
-                        {new Date().toLocaleDateString('ar-EG')}
-                    </div>
-                </div>
-            </div>
-
-            {/* Main Control */}
-            <div className="flex-1 p-8 grid grid-cols-1 lg:grid-cols-2 gap-8">
-                {/* Production Controls */}
-                <div className="bg-slate-900/50 rounded-3xl border border-white/10 p-8 flex flex-col justify-center items-center">
-                    <div className="text-slate-400 text-2xl mb-4">╪╣╪»╪» ╪د┘╪»┘ê╪▒╪د╪ز (╪د┘╪╢╪▒╪ذ╪د╪ز)</div>
-                    <div className="flex items-center gap-8 mb-12">
-                        <button
-                            onClick={() => setCycleCount(Math.max(0, cycleCount - 1))}
-                            className="w-24 h-24 bg-rose-500/20 hover:bg-rose-500/30 border-2 border-rose-500/50 rounded-full text-4xl text-rose-500 transition"
-                        >
-                            -
-                        </button>
-                        <div className="text-9xl font-mono font-bold w-48 text-center">
-                            {cycleCount}
-                        </div>
-                        <button
-                            onClick={() => setCycleCount(cycleCount + 1)}
-                            className="w-24 h-24 bg-emerald-500/20 hover:bg-emerald-500/30 border-2 border-emerald-500/50 rounded-full text-4xl text-emerald-500 transition"
-                        >
-                            +
-                        </button>
-                    </div>
-
-                    <div className="grid grid-cols-3 gap-6 w-full mb-12">
-                        {[1, 5, 10, 50, 100].map(num => (
-                            <button
-                                key={num}
-                                onClick={() => setCycleCount(cycleCount + num)}
-                                className="py-4 bg-slate-800 hover:bg-slate-700 rounded-xl text-xl font-bold border border-white/10 transition"
-                            >
-                                +{num}
-                            </button>
-                        ))}
-                        <button
-                            onClick={() => setCycleCount(0)}
-                            className="py-4 bg-rose-900/30 hover:bg-rose-900/50 text-rose-400 rounded-xl text-xl font-bold border border-rose-500/20 transition"
-                        >
-                            ╪ز╪╡┘┘è╪▒
-                        </button>
-                    </div>
-
-                    <button
-                        id="produce-btn"
-                        onClick={handleProductionSubmit}
-                        disabled={cycleCount === 0}
-                        className={`
-                            w-full py-8 rounded-3xl text-4xl font-black shadow-2xl transition-all duration-200
-                            ${cycleCount > 0
-                                ? 'bg-gradient-to-r from-blue-600 to-indigo-600 hover:scale-[1.02] shadow-blue-900/50 text-white cursor-pointer'
-                                : 'bg-slate-800 text-slate-500 cursor-not-allowed'
-                            }
-                        `}
-                    >
-                        <Save /> ╪ز╪│╪ش┘è┘ ╪د┘╪ح┘╪ز╪د╪ش
-                    </button>
-                </div>
-
-                {/* Info Panel */}
-                <div className="space-y-8">
-                    {/* Current Stats */}
-                    <div className="bg-slate-900/50 rounded-3xl border border-white/10 p-8">
-                        <h3 className="text-2xl font-bold mb-6 border-b border-white/10 pb-4">╪ح╪ص╪╡╪د╪خ┘è╪د╪ز ┘┘ê╪▒┘è╪ر</h3>
-                        <div className="space-y-6">
-                            <div className="flex justify-between items-center">
-                                <span className="text-xl text-slate-400">╪د┘┘à┘╪ز╪ش ╪د┘┘à╪ز┘ê┘é╪╣</span>
-                                <span className="text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-pink-400">
-                                    {(cycleCount * (mold?.cavities || 1)).toLocaleString()} ┘é╪╖╪╣╪ر
-                                </span>
-                            </div>
-                            <div className="flex justify-between items-center">
-                                <span className="text-xl text-slate-400">╪د┘┘ê╪▓┘ ╪د┘┘à┘é╪»╪▒</span>
-                                <span className="text-2xl font-bold text-white">
-                                    {((cycleCount * (mold?.cavities || 1) * (mold?.product_weight || 0)) / 1000).toFixed(2)} ┘â╪ش┘à
-                                </span>
-                            </div>
-                        </div>
-                    </div>
-
-                    {/* Quick Issues */}
-                    <div className="bg-slate-900/50 rounded-3xl border border-white/10 p-8">
-                        <h3 className="text-2xl font-bold mb-6 border-b border-white/10 pb-4 text-rose-400 flex items-center gap-2"><AlertTriangle /> ╪ز┘╪ذ┘è┘ç ╪╣╪╖┘</h3>
-                        <div className="grid grid-cols-2 gap-4">
-                            <button
-                                onClick={async () => {
-                                    try {
-                                        await api.fetchWithAuth('/manufacturing/maintenance', {
-                                            method: 'POST',
-                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'HIGH_TEMP', date: new Date().toISOString(), description: '╪ص╪▒╪د╪▒╪ر ╪╣╪د┘┘è╪ر', cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
-                                        });
-                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪ذ┘╪د╪║ ╪ص╪▒╪د╪▒╪ر ╪╣╪د┘┘è╪ر');
-                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
-                                }}
-                                className="p-6 bg-rose-500/10 hover:bg-rose-500/20 border border-rose-500/30 rounded-2xl text-rose-400 font-bold transition"
-                            >
-                                <Thermometer /> ╪ص╪▒╪د╪▒╪ر ╪╣╪د┘┘è╪ر
-                            </button>
-                            <button
-                                onClick={async () => {
-                                    try {
-                                        await api.fetchWithAuth('/manufacturing/maintenance', {
-                                            method: 'POST',
-                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'MECHANICAL', date: new Date().toISOString(), description: '╪╣╪╖┘ ┘à┘è┘â╪د┘┘è┘â┘è', cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
-                                        });
-                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪ذ┘╪د╪║ ╪╣╪╖┘ ┘à┘è┘â╪د┘┘è┘â┘è');
-                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
-                                }}
-                                className="p-6 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/30 rounded-2xl text-amber-400 font-bold transition"
-                            >
-                                <Wrench /> ╪╣╪╖┘ ┘à┘è┘â╪د┘┘è┘â┘è
-                            </button>
-                            <button
-                                onClick={async () => {
-                                    try {
-                                        await api.fetchWithAuth('/manufacturing/maintenance', {
-                                            method: 'POST',
-                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'ELECTRICAL', date: new Date().toISOString(), description: '┘à╪┤┘â┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة', cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
-                                        });
-                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪ذ┘╪د╪║ ┘à╪┤┘â┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة');
-                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
-                                }}
-                                className="p-6 bg-orange-500/10 hover:bg-orange-500/20 border border-orange-500/30 rounded-2xl text-orange-400 font-bold transition"
-                            >
-                                <Zap /> ┘à╪┤┘â┘╪ر ┘â┘ç╪▒╪ذ╪د╪ة
-                            </button>
-                            <button
-                                onClick={async () => {
-                                    const desc = prompt('┘ê╪╡┘ ╪د┘╪╣╪╖┘:');
-                                    if (!desc) return;
-                                    try {
-                                        await api.fetchWithAuth('/manufacturing/maintenance', {
-                                            method: 'POST',
-                                            body: JSON.stringify({ machine_id: Number(machineId), type: 'OTHER', date: new Date().toISOString(), description: desc, cost: 0, status: 'OPEN', notes: '╪ز┘à ╪د┘╪ح╪ذ┘╪د╪║ ┘à┘ ╪د┘┘â╪┤┘â ╪د┘╪░┘â┘è' }),
-                                        });
-                                         alert('╪ز┘à ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║');
-                                     } catch { alert('┘╪┤┘ ╪ز╪│╪ش┘è┘ ╪د┘╪ذ┘╪د╪║'); }
-                                }}
-                                className="p-6 bg-slate-800 hover:bg-slate-700 border border-white/10 rounded-2xl text-white font-bold transition"
-                            >
-                                <FileText /> ╪╣╪╖┘ ╪ت╪«╪▒
-                            </button>
-                        </div>
-                    </div>
-                </div>
-            </div>
-        </div>
-    );
+export default async function Page({ params }: { params: Promise<{ machineId: string }> }) {
+  const { machineId } = await params;
+  return <KioskClientPage machineId={machineId} />;
 }
diff --git a/frontend/app/manufacturing/machines/[id]/client.tsx b/frontend/app/manufacturing/machines/[id]/client.tsx
new file mode 100644
index 0000000..2c06377
--- /dev/null
+++ b/frontend/app/manufacturing/machines/[id]/client.tsx
@@ -0,0 +1,94 @@
+'use client';
+
+import { useEffect, useState, useCallback } from 'react';
+import { useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+import { useSetBackButton, GO_BACK } from '@/components/BackButton';
+
+interface MachineHistory {
+    mold: { name: string };
+    startDate: string;
+    endDate: string;
+    pieces: number;
+    totalCost: number;
+}
+
+export default function MachineDetailsPage() {
+    useSetBackButton(GO_BACK);
+    const params = useParams();
+    const [history, setHistory] = useState<MachineHistory[]>([]);
+    const [loading, setLoading] = useState(true);
+
+    const loadData = useCallback(async () => {
+        try {
+            const data = await api.fetchWithAuth(`/manufacturing/machines/${params.id}/history`);
+            setHistory(Array.isArray(data) ? data : []);
+        } catch (error) {
+            console.error('Error loading history:', error);
+        } finally {
+            setLoading(false);
+        }
+    }, [params.id]);
+
+    useEffect(() => {
+        if (params.id) {
+            loadData();
+        }
+    }, [params.id, loadData]);
+
+    if (loading) {
+        return (
+            <div className="min-h-screen flex items-center justify-center bg-slate-900">
+                <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
+            </div>
+        );
+    }
+
+    return (
+        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
+            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
+                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
+                    <h1 className="text-2xl font-bold text-white">╪│╪ش┘ ╪د┘┘à╪د┘â┘è┘╪ر</h1>
+                </div>
+            </header>
+
+            <main className="container mx-auto px-6 py-8">
+                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
+                    <table className="w-full">
+                        <thead className="bg-white/5">
+                            <tr>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ح╪│╪╖┘à╪ذ╪ر</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪▒┘â┘è╪ذ</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪║┘è┘è╪▒</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪ح╪ش┘à╪د┘┘è ╪د┘╪ح┘╪ز╪د╪ش</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر</th>
+                            </tr>
+                        </thead>
+                        <tbody>
+                            {history.map((record, index) => (
+                                <tr key={index} className="border-t border-white/10 hover:bg-white/5">
+                                    <td className="px-6 py-4 text-gray-200">{record.mold?.name}</td>
+                                    <td className="px-6 py-4 text-gray-300">{new Date(record.startDate).toLocaleDateString('ar-EG')}</td>
+                                    <td className="px-6 py-4 text-gray-300">{new Date(record.endDate).toLocaleDateString('ar-EG')}</td>
+                                    <td className="px-6 py-4 text-gray-300">{record.pieces} ┘é╪╖╪╣╪ر</td>
+                                    <td className="px-6 py-4 text-gray-300">
+                                        {record.pieces > 0
+                                            ? (record.totalCost / record.pieces).toFixed(2)
+                                            : 0} ╪ش┘┘è┘ç
+                                    </td>
+                                </tr>
+                            ))}
+                            {history.length === 0 && (
+                                <tr>
+                                    <td colSpan={5} className="px-6 py-8 text-center text-gray-400">
+                                        ┘╪د ┘è┘ê╪ش╪» ╪│╪ش┘ ╪ح┘╪ز╪د╪ش ┘┘ç╪░┘ç ╪د┘┘à╪د┘â┘è┘╪ر.
+                                    </td>
+                                </tr>
+                            )}
+                        </tbody>
+                    </table>
+                </div>
+            </main>
+        </div>
+    );
+}
diff --git a/frontend/app/manufacturing/machines/[id]/maintenance/client.tsx b/frontend/app/manufacturing/machines/[id]/maintenance/client.tsx
new file mode 100644
index 0000000..b03ce14
--- /dev/null
+++ b/frontend/app/manufacturing/machines/[id]/maintenance/client.tsx
@@ -0,0 +1,231 @@
+'use client';
+
+import { useEffect, useState, useCallback } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+import { Wrench } from 'lucide-react';
+
+interface Maintenance {
+    id: number;
+    date: string;
+    type: string;
+    description: string;
+    cost: number;
+    status: string;
+}
+
+interface Machine {
+    id: number;
+    name: string;
+}
+
+export default function MachineMaintenancePage() {
+    const router = useRouter();
+    const params = useParams();
+    const [maintenance, setMaintenance] = useState<Maintenance[]>([]);
+    const [machine, setMachine] = useState<Machine | null>(null);
+    const [loading, setLoading] = useState(true);
+    const [showModal, setShowModal] = useState(false);
+
+    const loadData = useCallback(async () => {
+        try {
+            const [maintenanceData, machineData] = await Promise.all([
+                api.fetchWithAuth(`/manufacturing/maintenance?machine_id=${params.id}`),
+                api.fetchWithAuth(`/manufacturing/machines`),
+            ]);
+
+            const currentMachine = (machineData as Machine[]).find((m) => m.id === Number(params.id));
+            setMachine(currentMachine || null);
+            setMaintenance(Array.isArray(maintenanceData) ? maintenanceData : []);
+        } catch (error) {
+            console.error('Error loading data:', error);
+        } finally {
+            setLoading(false);
+        }
+    }, [params.id]);
+
+    useEffect(() => {
+        loadData();
+    }, [loadData]);
+
+    const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) => {
+        e.preventDefault();
+        const formData = new FormData(e.currentTarget);
+        const data = {
+            machine_id: Number(params.id),
+            type: formData.get('type'),
+            date: formData.get('date'),
+            description: formData.get('description'),
+            cost: formData.get('cost'),
+            status: formData.get('status'),
+            notes: formData.get('notes'),
+        };
+
+        try {
+            await api.fetchWithAuth('/manufacturing/maintenance', {
+                method: 'POST',
+                body: JSON.stringify(data),
+            });
+            setShowModal(false);
+            loadData();
+        } catch (error) {
+            console.error('Error saving maintenance:', error);
+        }
+    };
+
+    if (loading) {
+        return (
+            <div className="min-h-screen flex items-center justify-center bg-slate-900">
+                <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
+            </div>
+        );
+    }
+
+    return (
+        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
+            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
+                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
+                    <div>
+                        <h1 className="text-2xl font-bold text-white flex items-center gap-2"><Wrench /> ╪│╪ش┘ ╪د┘╪╡┘è╪د┘╪ر</h1>
+                        <p className="text-gray-400 text-sm mt-1">╪د┘┘à╪د┘â┘è┘╪ر: {machine?.name}</p>
+                    </div>
+                    <button
+                        onClick={() => router.push('/manufacturing/machines')}
+                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
+                    >
+                        ╪╣┘ê╪»╪ر ┘┘┘à╪د┘â┘è┘╪د╪ز
+                    </button>
+                </div>
+            </header>
+
+            <main className="container mx-auto px-6 py-8">
+                <div className="mb-6">
+                    <button
+                        onClick={() => setShowModal(true)}
+                        className="px-6 py-3 bg-gradient-to-r from-yellow-600 to-orange-600 text-white rounded-lg font-semibold hover:from-yellow-700 hover:to-orange-700 transition"
+                    >
+                        + ╪ز╪│╪ش┘è┘ ╪╡┘è╪د┘╪ر/╪╣╪╖┘
+                    </button>
+                </div>
+
+                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
+                    <table className="w-full">
+                        <thead className="bg-white/5">
+                            <tr>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ز╪د╪▒┘è╪«</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘┘┘ê╪╣</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘┘ê╪╡┘</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ز┘â┘┘╪ر</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ص╪د┘╪ر</th>
+                            </tr>
+                        </thead>
+                        <tbody>
+                            {maintenance.map((item) => (
+                                <tr key={item.id} className="border-t border-white/10 hover:bg-white/5">
+                                    <td className="px-6 py-4 text-gray-200">{new Date(item.date).toLocaleDateString('ar-EG')}</td>
+                                    <td className="px-6 py-4">
+                                        <span className={`px-2 py-1 rounded text-sm ${item.type === 'BREAKDOWN' ? 'bg-red-500/20 text-red-200' : 'bg-blue-500/20 text-blue-200'
+                                            }`}>
+                                            {item.type === 'BREAKDOWN' ? '╪╣╪╖┘' : '╪╡┘è╪د┘╪ر ╪»┘ê╪▒┘è╪ر'}
+                                        </span>
+                                    </td>
+                                    <td className="px-6 py-4 text-gray-300">{item.description}</td>
+                                    <td className="px-6 py-4 text-gray-300">{item.cost} ╪ش┘┘è┘ç</td>
+                                    <td className="px-6 py-4">
+                                        <span className={`px-2 py-1 rounded text-sm ${item.status === 'COMPLETED' ? 'bg-green-500/20 text-green-200' : 'bg-yellow-500/20 text-yellow-200'
+                                            }`}>
+                                            {item.status === 'COMPLETED' ? '╪ز┘à ╪د┘╪ح╪╡┘╪د╪ص' : '┘é┘è╪» ╪د┘╪╣┘à┘'}
+                                        </span>
+                                    </td>
+                                </tr>
+                            ))}
+                            {maintenance.length === 0 && (
+                                <tr>
+                                    <td colSpan={5} className="px-6 py-8 text-center text-gray-400">
+                                        ┘╪د ╪ز┘ê╪ش╪» ╪│╪ش┘╪د╪ز ╪╡┘è╪د┘╪ر ┘┘ç╪░┘ç ╪د┘┘à╪د┘â┘è┘╪ر
+                                    </td>
+                                </tr>
+                            )}
+                        </tbody>
+                    </table>
+                </div>
+            </main>
+
+            {showModal && (
+                <div className="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50" onClick={() => setShowModal(false)}>
+                    <div className="bg-slate-800 p-8 rounded-2xl w-full max-w-md border border-white/20" onClick={(e) => e.stopPropagation()}>
+                        <h2 className="text-2xl font-bold text-white mb-6">╪ز╪│╪ش┘è┘ ╪╡┘è╪د┘╪ر ╪ش╪»┘è╪»╪ر</h2>
+                        <form onSubmit={handleSubmit} className="space-y-4">
+                            <div>
+                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘╪ز╪د╪▒┘è╪«</label>
+                                <input
+                                    name="date"
+                                    type="date"
+                                    required
+                                    defaultValue={new Date().toISOString().split('T')[0]}
+                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
+                                />
+                            </div>
+                            <div>
+                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘┘┘ê╪╣</label>
+                                <select
+                                    name="type"
+                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
+                                >
+                                    <option value="BREAKDOWN">╪╣╪╖┘ ┘à┘╪د╪ش╪خ</option>
+                                    <option value="SCHEDULED">╪╡┘è╪د┘╪ر ╪»┘ê╪▒┘è╪ر</option>
+                                    <option value="PREVENTIVE">╪╡┘è╪د┘╪ر ┘ê┘é╪د╪خ┘è╪ر</option>
+                                </select>
+                            </div>
+                            <div>
+                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘┘ê╪╡┘</label>
+                                <textarea
+                                    name="description"
+                                    required
+                                    rows={3}
+                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
+                                />
+                            </div>
+                            <div>
+                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘╪ز┘â┘┘╪ر (╪ش┘┘è┘ç)</label>
+                                <input
+                                    name="cost"
+                                    type="number"
+                                    step="0.01"
+                                    defaultValue="0"
+                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
+                                />
+                            </div>
+                            <div>
+                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘╪ص╪د┘╪ر</label>
+                                <select
+                                    name="status"
+                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
+                                >
+                                    <option value="PENDING">┘é┘è╪» ╪د┘╪د┘╪ز╪╕╪د╪▒</option>
+                                    <option value="IN_PROGRESS">╪ش╪د╪▒┘è ╪د┘╪╣┘à┘</option>
+                                    <option value="COMPLETED">╪ز┘à ╪د┘╪ح╪╡┘╪د╪ص</option>
+                                </select>
+                            </div>
+                            <div className="flex gap-4 justify-end">
+                                <button
+                                    type="button"
+                                    onClick={() => setShowModal(false)}
+                                    className="px-6 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg"
+                                >
+                                    ╪ح┘╪║╪د╪ة
+                                </button>
+                                <button
+                                    type="submit"
+                                    className="px-6 py-2 bg-gradient-to-r from-yellow-600 to-orange-600 text-white rounded-lg hover:from-yellow-700 hover:to-orange-700"
+                                >
+                                    ╪ص┘╪╕
+                                </button>
+                            </div>
+                        </form>
+                    </div>
+                </div>
+            )}
+        </div>
+    );
+}
diff --git a/frontend/app/manufacturing/machines/[id]/maintenance/page.tsx b/frontend/app/manufacturing/machines/[id]/maintenance/page.tsx
index b03ce14..4a36ea9 100644
--- a/frontend/app/manufacturing/machines/[id]/maintenance/page.tsx
+++ b/frontend/app/manufacturing/machines/[id]/maintenance/page.tsx
@@ -1,231 +1,9 @@
-'use client';
+import MaintenanceClientPage from './client';
 
-import { useEffect, useState, useCallback } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-import { Wrench } from 'lucide-react';
-
-interface Maintenance {
-    id: number;
-    date: string;
-    type: string;
-    description: string;
-    cost: number;
-    status: string;
-}
-
-interface Machine {
-    id: number;
-    name: string;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function MachineMaintenancePage() {
-    const router = useRouter();
-    const params = useParams();
-    const [maintenance, setMaintenance] = useState<Maintenance[]>([]);
-    const [machine, setMachine] = useState<Machine | null>(null);
-    const [loading, setLoading] = useState(true);
-    const [showModal, setShowModal] = useState(false);
-
-    const loadData = useCallback(async () => {
-        try {
-            const [maintenanceData, machineData] = await Promise.all([
-                api.fetchWithAuth(`/manufacturing/maintenance?machine_id=${params.id}`),
-                api.fetchWithAuth(`/manufacturing/machines`),
-            ]);
-
-            const currentMachine = (machineData as Machine[]).find((m) => m.id === Number(params.id));
-            setMachine(currentMachine || null);
-            setMaintenance(Array.isArray(maintenanceData) ? maintenanceData : []);
-        } catch (error) {
-            console.error('Error loading data:', error);
-        } finally {
-            setLoading(false);
-        }
-    }, [params.id]);
-
-    useEffect(() => {
-        loadData();
-    }, [loadData]);
-
-    const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) => {
-        e.preventDefault();
-        const formData = new FormData(e.currentTarget);
-        const data = {
-            machine_id: Number(params.id),
-            type: formData.get('type'),
-            date: formData.get('date'),
-            description: formData.get('description'),
-            cost: formData.get('cost'),
-            status: formData.get('status'),
-            notes: formData.get('notes'),
-        };
-
-        try {
-            await api.fetchWithAuth('/manufacturing/maintenance', {
-                method: 'POST',
-                body: JSON.stringify(data),
-            });
-            setShowModal(false);
-            loadData();
-        } catch (error) {
-            console.error('Error saving maintenance:', error);
-        }
-    };
-
-    if (loading) {
-        return (
-            <div className="min-h-screen flex items-center justify-center bg-slate-900">
-                <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
-            </div>
-        );
-    }
-
-    return (
-        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
-            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
-                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
-                    <div>
-                        <h1 className="text-2xl font-bold text-white flex items-center gap-2"><Wrench /> ╪│╪ش┘ ╪د┘╪╡┘è╪د┘╪ر</h1>
-                        <p className="text-gray-400 text-sm mt-1">╪د┘┘à╪د┘â┘è┘╪ر: {machine?.name}</p>
-                    </div>
-                    <button
-                        onClick={() => router.push('/manufacturing/machines')}
-                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
-                    >
-                        ╪╣┘ê╪»╪ر ┘┘┘à╪د┘â┘è┘╪د╪ز
-                    </button>
-                </div>
-            </header>
-
-            <main className="container mx-auto px-6 py-8">
-                <div className="mb-6">
-                    <button
-                        onClick={() => setShowModal(true)}
-                        className="px-6 py-3 bg-gradient-to-r from-yellow-600 to-orange-600 text-white rounded-lg font-semibold hover:from-yellow-700 hover:to-orange-700 transition"
-                    >
-                        + ╪ز╪│╪ش┘è┘ ╪╡┘è╪د┘╪ر/╪╣╪╖┘
-                    </button>
-                </div>
-
-                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
-                    <table className="w-full">
-                        <thead className="bg-white/5">
-                            <tr>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ز╪د╪▒┘è╪«</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘┘┘ê╪╣</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘┘ê╪╡┘</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ز┘â┘┘╪ر</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ص╪د┘╪ر</th>
-                            </tr>
-                        </thead>
-                        <tbody>
-                            {maintenance.map((item) => (
-                                <tr key={item.id} className="border-t border-white/10 hover:bg-white/5">
-                                    <td className="px-6 py-4 text-gray-200">{new Date(item.date).toLocaleDateString('ar-EG')}</td>
-                                    <td className="px-6 py-4">
-                                        <span className={`px-2 py-1 rounded text-sm ${item.type === 'BREAKDOWN' ? 'bg-red-500/20 text-red-200' : 'bg-blue-500/20 text-blue-200'
-                                            }`}>
-                                            {item.type === 'BREAKDOWN' ? '╪╣╪╖┘' : '╪╡┘è╪د┘╪ر ╪»┘ê╪▒┘è╪ر'}
-                                        </span>
-                                    </td>
-                                    <td className="px-6 py-4 text-gray-300">{item.description}</td>
-                                    <td className="px-6 py-4 text-gray-300">{item.cost} ╪ش┘┘è┘ç</td>
-                                    <td className="px-6 py-4">
-                                        <span className={`px-2 py-1 rounded text-sm ${item.status === 'COMPLETED' ? 'bg-green-500/20 text-green-200' : 'bg-yellow-500/20 text-yellow-200'
-                                            }`}>
-                                            {item.status === 'COMPLETED' ? '╪ز┘à ╪د┘╪ح╪╡┘╪د╪ص' : '┘é┘è╪» ╪د┘╪╣┘à┘'}
-                                        </span>
-                                    </td>
-                                </tr>
-                            ))}
-                            {maintenance.length === 0 && (
-                                <tr>
-                                    <td colSpan={5} className="px-6 py-8 text-center text-gray-400">
-                                        ┘╪د ╪ز┘ê╪ش╪» ╪│╪ش┘╪د╪ز ╪╡┘è╪د┘╪ر ┘┘ç╪░┘ç ╪د┘┘à╪د┘â┘è┘╪ر
-                                    </td>
-                                </tr>
-                            )}
-                        </tbody>
-                    </table>
-                </div>
-            </main>
-
-            {showModal && (
-                <div className="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50" onClick={() => setShowModal(false)}>
-                    <div className="bg-slate-800 p-8 rounded-2xl w-full max-w-md border border-white/20" onClick={(e) => e.stopPropagation()}>
-                        <h2 className="text-2xl font-bold text-white mb-6">╪ز╪│╪ش┘è┘ ╪╡┘è╪د┘╪ر ╪ش╪»┘è╪»╪ر</h2>
-                        <form onSubmit={handleSubmit} className="space-y-4">
-                            <div>
-                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘╪ز╪د╪▒┘è╪«</label>
-                                <input
-                                    name="date"
-                                    type="date"
-                                    required
-                                    defaultValue={new Date().toISOString().split('T')[0]}
-                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
-                                />
-                            </div>
-                            <div>
-                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘┘┘ê╪╣</label>
-                                <select
-                                    name="type"
-                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
-                                >
-                                    <option value="BREAKDOWN">╪╣╪╖┘ ┘à┘╪د╪ش╪خ</option>
-                                    <option value="SCHEDULED">╪╡┘è╪د┘╪ر ╪»┘ê╪▒┘è╪ر</option>
-                                    <option value="PREVENTIVE">╪╡┘è╪د┘╪ر ┘ê┘é╪د╪خ┘è╪ر</option>
-                                </select>
-                            </div>
-                            <div>
-                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘┘ê╪╡┘</label>
-                                <textarea
-                                    name="description"
-                                    required
-                                    rows={3}
-                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
-                                />
-                            </div>
-                            <div>
-                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘╪ز┘â┘┘╪ر (╪ش┘┘è┘ç)</label>
-                                <input
-                                    name="cost"
-                                    type="number"
-                                    step="0.01"
-                                    defaultValue="0"
-                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
-                                />
-                            </div>
-                            <div>
-                                <label className="block text-sm font-medium text-gray-200 mb-2">╪د┘╪ص╪د┘╪ر</label>
-                                <select
-                                    name="status"
-                                    className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg text-white"
-                                >
-                                    <option value="PENDING">┘é┘è╪» ╪د┘╪د┘╪ز╪╕╪د╪▒</option>
-                                    <option value="IN_PROGRESS">╪ش╪د╪▒┘è ╪د┘╪╣┘à┘</option>
-                                    <option value="COMPLETED">╪ز┘à ╪د┘╪ح╪╡┘╪د╪ص</option>
-                                </select>
-                            </div>
-                            <div className="flex gap-4 justify-end">
-                                <button
-                                    type="button"
-                                    onClick={() => setShowModal(false)}
-                                    className="px-6 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg"
-                                >
-                                    ╪ح┘╪║╪د╪ة
-                                </button>
-                                <button
-                                    type="submit"
-                                    className="px-6 py-2 bg-gradient-to-r from-yellow-600 to-orange-600 text-white rounded-lg hover:from-yellow-700 hover:to-orange-700"
-                                >
-                                    ╪ص┘╪╕
-                                </button>
-                            </div>
-                        </form>
-                    </div>
-                </div>
-            )}
-        </div>
-    );
+export default function Page() {
+  return <MaintenanceClientPage />;
 }
diff --git a/frontend/app/manufacturing/machines/[id]/page.tsx b/frontend/app/manufacturing/machines/[id]/page.tsx
index 2c06377..998620d 100644
--- a/frontend/app/manufacturing/machines/[id]/page.tsx
+++ b/frontend/app/manufacturing/machines/[id]/page.tsx
@@ -1,94 +1,9 @@
-'use client';
+import MachineClientPage from './client';
 
-import { useEffect, useState, useCallback } from 'react';
-import { useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-import { useSetBackButton, GO_BACK } from '@/components/BackButton';
-
-interface MachineHistory {
-    mold: { name: string };
-    startDate: string;
-    endDate: string;
-    pieces: number;
-    totalCost: number;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function MachineDetailsPage() {
-    useSetBackButton(GO_BACK);
-    const params = useParams();
-    const [history, setHistory] = useState<MachineHistory[]>([]);
-    const [loading, setLoading] = useState(true);
-
-    const loadData = useCallback(async () => {
-        try {
-            const data = await api.fetchWithAuth(`/manufacturing/machines/${params.id}/history`);
-            setHistory(Array.isArray(data) ? data : []);
-        } catch (error) {
-            console.error('Error loading history:', error);
-        } finally {
-            setLoading(false);
-        }
-    }, [params.id]);
-
-    useEffect(() => {
-        if (params.id) {
-            loadData();
-        }
-    }, [params.id, loadData]);
-
-    if (loading) {
-        return (
-            <div className="min-h-screen flex items-center justify-center bg-slate-900">
-                <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
-            </div>
-        );
-    }
-
-    return (
-        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
-            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
-                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
-                    <h1 className="text-2xl font-bold text-white">╪│╪ش┘ ╪د┘┘à╪د┘â┘è┘╪ر</h1>
-                </div>
-            </header>
-
-            <main className="container mx-auto px-6 py-8">
-                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
-                    <table className="w-full">
-                        <thead className="bg-white/5">
-                            <tr>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘╪ح╪│╪╖┘à╪ذ╪ر</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪▒┘â┘è╪ذ</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪║┘è┘è╪▒</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪ح╪ش┘à╪د┘┘è ╪د┘╪ح┘╪ز╪د╪ش</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر</th>
-                            </tr>
-                        </thead>
-                        <tbody>
-                            {history.map((record, index) => (
-                                <tr key={index} className="border-t border-white/10 hover:bg-white/5">
-                                    <td className="px-6 py-4 text-gray-200">{record.mold?.name}</td>
-                                    <td className="px-6 py-4 text-gray-300">{new Date(record.startDate).toLocaleDateString('ar-EG')}</td>
-                                    <td className="px-6 py-4 text-gray-300">{new Date(record.endDate).toLocaleDateString('ar-EG')}</td>
-                                    <td className="px-6 py-4 text-gray-300">{record.pieces} ┘é╪╖╪╣╪ر</td>
-                                    <td className="px-6 py-4 text-gray-300">
-                                        {record.pieces > 0
-                                            ? (record.totalCost / record.pieces).toFixed(2)
-                                            : 0} ╪ش┘┘è┘ç
-                                    </td>
-                                </tr>
-                            ))}
-                            {history.length === 0 && (
-                                <tr>
-                                    <td colSpan={5} className="px-6 py-8 text-center text-gray-400">
-                                        ┘╪د ┘è┘ê╪ش╪» ╪│╪ش┘ ╪ح┘╪ز╪د╪ش ┘┘ç╪░┘ç ╪د┘┘à╪د┘â┘è┘╪ر.
-                                    </td>
-                                </tr>
-                            )}
-                        </tbody>
-                    </table>
-                </div>
-            </main>
-        </div>
-    );
+export default function Page() {
+  return <MachineClientPage />;
 }
diff --git a/frontend/app/manufacturing/molds/[id]/client.tsx b/frontend/app/manufacturing/molds/[id]/client.tsx
new file mode 100644
index 0000000..bcfefa4
--- /dev/null
+++ b/frontend/app/manufacturing/molds/[id]/client.tsx
@@ -0,0 +1,99 @@
+'use client';
+
+import { useEffect, useState, useCallback } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+
+interface MoldHistory {
+    machine: { name: string };
+    startDate: string;
+    endDate: string;
+    pieces: number;
+    totalCost: number;
+}
+
+export default function MoldDetailsPage() {
+    const router = useRouter();
+    const params = useParams();
+    const [history, setHistory] = useState<MoldHistory[]>([]);
+    const [loading, setLoading] = useState(true);
+
+    const loadData = useCallback(async () => {
+        try {
+            const data = await api.fetchWithAuth(`/manufacturing/molds/${params.id}/history`);
+            setHistory(Array.isArray(data) ? data : []);
+        } catch (error) {
+            console.error('Error loading history:', error);
+        } finally {
+            setLoading(false);
+        }
+    }, [params.id]);
+
+    useEffect(() => {
+        if (params.id) {
+            loadData();
+        }
+    }, [params.id, loadData]);
+
+    if (loading) {
+        return (
+            <div className="min-h-screen flex items-center justify-center bg-slate-900">
+                <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
+            </div>
+        );
+    }
+
+    return (
+        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
+            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
+                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
+                    <h1 className="text-2xl font-bold text-white">╪│╪ش┘ ╪د┘╪ح╪│╪╖┘à╪ذ╪ر</h1>
+                    <button
+                        onClick={() => router.back()}
+                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
+                    >
+                        ╪╣┘ê╪»╪ر
+                    </button>
+                </div>
+            </header>
+
+            <main className="container mx-auto px-6 py-8">
+                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
+                    <table className="w-full">
+                        <thead className="bg-white/5">
+                            <tr>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘┘à╪د┘â┘è┘╪ر</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪▒┘â┘è╪ذ</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪║┘è┘è╪▒</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">╪ح╪ش┘à╪د┘┘è ╪د┘╪ح┘╪ز╪د╪ش</th>
+                                <th className="px-6 py-4 text-right text-white font-semibold">┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر</th>
+                            </tr>
+                        </thead>
+                        <tbody>
+                            {history.map((record, index) => (
+                                <tr key={index} className="border-t border-white/10 hover:bg-white/5">
+                                    <td className="px-6 py-4 text-gray-200">{record.machine?.name}</td>
+                                    <td className="px-6 py-4 text-gray-300">{new Date(record.startDate).toLocaleDateString('ar-EG')}</td>
+                                    <td className="px-6 py-4 text-gray-300">{new Date(record.endDate).toLocaleDateString('ar-EG')}</td>
+                                    <td className="px-6 py-4 text-gray-300">{record.pieces} ┘é╪╖╪╣╪ر</td>
+                                    <td className="px-6 py-4 text-gray-300">
+                                        {record.pieces > 0
+                                            ? (record.totalCost / record.pieces).toFixed(2)
+                                            : 0} ╪ش┘┘è┘ç
+                                    </td>
+                                </tr>
+                            ))}
+                            {history.length === 0 && (
+                                <tr>
+                                    <td colSpan={5} className="px-6 py-8 text-center text-gray-400">
+                                        ┘╪د ┘è┘ê╪ش╪» ╪│╪ش┘ ╪د╪│╪ز╪«╪»╪د┘à ┘┘ç╪░┘ç ╪د┘╪ح╪│╪╖┘à╪ذ╪ر.
+                                    </td>
+                                </tr>
+                            )}
+                        </tbody>
+                    </table>
+                </div>
+            </main>
+        </div>
+    );
+}
diff --git a/frontend/app/manufacturing/molds/[id]/page.tsx b/frontend/app/manufacturing/molds/[id]/page.tsx
index bcfefa4..d7ceef8 100644
--- a/frontend/app/manufacturing/molds/[id]/page.tsx
+++ b/frontend/app/manufacturing/molds/[id]/page.tsx
@@ -1,99 +1,9 @@
-'use client';
+import MoldClientPage from './client';
 
-import { useEffect, useState, useCallback } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-
-interface MoldHistory {
-    machine: { name: string };
-    startDate: string;
-    endDate: string;
-    pieces: number;
-    totalCost: number;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function MoldDetailsPage() {
-    const router = useRouter();
-    const params = useParams();
-    const [history, setHistory] = useState<MoldHistory[]>([]);
-    const [loading, setLoading] = useState(true);
-
-    const loadData = useCallback(async () => {
-        try {
-            const data = await api.fetchWithAuth(`/manufacturing/molds/${params.id}/history`);
-            setHistory(Array.isArray(data) ? data : []);
-        } catch (error) {
-            console.error('Error loading history:', error);
-        } finally {
-            setLoading(false);
-        }
-    }, [params.id]);
-
-    useEffect(() => {
-        if (params.id) {
-            loadData();
-        }
-    }, [params.id, loadData]);
-
-    if (loading) {
-        return (
-            <div className="min-h-screen flex items-center justify-center bg-slate-900">
-                <div className="text-white text-xl">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>
-            </div>
-        );
-    }
-
-    return (
-        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
-            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
-                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
-                    <h1 className="text-2xl font-bold text-white">╪│╪ش┘ ╪د┘╪ح╪│╪╖┘à╪ذ╪ر</h1>
-                    <button
-                        onClick={() => router.back()}
-                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
-                    >
-                        ╪╣┘ê╪»╪ر
-                    </button>
-                </div>
-            </header>
-
-            <main className="container mx-auto px-6 py-8">
-                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
-                    <table className="w-full">
-                        <thead className="bg-white/5">
-                            <tr>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪د┘┘à╪د┘â┘è┘╪ر</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪▒┘â┘è╪ذ</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪ز╪د╪▒┘è╪« ╪د┘╪ز╪║┘è┘è╪▒</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">╪ح╪ش┘à╪د┘┘è ╪د┘╪ح┘╪ز╪د╪ش</th>
-                                <th className="px-6 py-4 text-right text-white font-semibold">┘à╪ز┘ê╪│╪╖ ╪د┘╪ز┘â┘┘╪ر</th>
-                            </tr>
-                        </thead>
-                        <tbody>
-                            {history.map((record, index) => (
-                                <tr key={index} className="border-t border-white/10 hover:bg-white/5">
-                                    <td className="px-6 py-4 text-gray-200">{record.machine?.name}</td>
-                                    <td className="px-6 py-4 text-gray-300">{new Date(record.startDate).toLocaleDateString('ar-EG')}</td>
-                                    <td className="px-6 py-4 text-gray-300">{new Date(record.endDate).toLocaleDateString('ar-EG')}</td>
-                                    <td className="px-6 py-4 text-gray-300">{record.pieces} ┘é╪╖╪╣╪ر</td>
-                                    <td className="px-6 py-4 text-gray-300">
-                                        {record.pieces > 0
-                                            ? (record.totalCost / record.pieces).toFixed(2)
-                                            : 0} ╪ش┘┘è┘ç
-                                    </td>
-                                </tr>
-                            ))}
-                            {history.length === 0 && (
-                                <tr>
-                                    <td colSpan={5} className="px-6 py-8 text-center text-gray-400">
-                                        ┘╪د ┘è┘ê╪ش╪» ╪│╪ش┘ ╪د╪│╪ز╪«╪»╪د┘à ┘┘ç╪░┘ç ╪د┘╪ح╪│╪╖┘à╪ذ╪ر.
-                                    </td>
-                                </tr>
-                            )}
-                        </tbody>
-                    </table>
-                </div>
-            </main>
-        </div>
-    );
+export default function Page() {
+  return <MoldClientPage />;
 }
diff --git a/frontend/app/manufacturing/raw-materials/[id]/add-stock/client.tsx b/frontend/app/manufacturing/raw-materials/[id]/add-stock/client.tsx
new file mode 100644
index 0000000..bb80147
--- /dev/null
+++ b/frontend/app/manufacturing/raw-materials/[id]/add-stock/client.tsx
@@ -0,0 +1,194 @@
+'use client';
+
+import { useState, useEffect, useCallback } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '@/lib/api';
+import { sortAlphabetically } from '@/lib/sort-utils';
+import { Plus, Save } from 'lucide-react';
+
+interface RawMaterialDetail {
+    id: number;
+    product: { name: string; unit: string };
+}
+
+interface Supplier {
+    id: number;
+    name: string;
+}
+
+export default function AddRawMaterialStockPage() {
+    const router = useRouter();
+    const params = useParams();
+    const id = params.id as string;
+
+    const [loading, setLoading] = useState(true);
+    const [rawMaterial, setRawMaterial] = useState<RawMaterialDetail | null>(null);
+    const [suppliers, setSuppliers] = useState<Supplier[]>([]);
+
+    const [quantity, setQuantity] = useState('');
+    const [price, setPrice] = useState('');
+    const [dataDate, setDate] = useState(new Date().toISOString().split('T')[0]);
+    const [supplierId, setSupplierId] = useState('');
+    const [notes, setNotes] = useState('');
+
+    const fetchData = useCallback(async () => {
+        try {
+            const rmData = await api.fetchWithAuth(`/manufacturing/raw-materials/${id}`);
+            const supData = await api.fetchWithAuth(`/purchases/suppliers`);
+
+            if (rmData && supData) {
+                setRawMaterial(rmData);
+                setSuppliers(sortAlphabetically(Array.isArray(supData) ? supData : [], 'name'));
+            } else {
+                alert('┘╪┤┘ ┘┘è ╪ز╪ص┘à┘è┘ ╪د┘╪ذ┘è╪د┘╪د╪ز');
+                router.back();
+            }
+        } catch (error) {
+            console.error('Error fetching data:', error);
+            alert('┘╪┤┘ ┘┘è ╪ز╪ص┘à┘è┘ ╪د┘╪ذ┘è╪د┘╪د╪ز');
+            router.back();
+        } finally {
+            setLoading(false);
+        }
+    }, [id, router]);
+
+    useEffect(() => {
+        fetchData();
+    }, [fetchData]);
+
+    const handleSubmit = async (e: React.FormEvent) => {
+        e.preventDefault();
+        try {
+            await api.fetchWithAuth(`/manufacturing/raw-materials/${id}/purchase`, {
+                method: 'POST',
+                body: JSON.stringify({
+                    quantity: parseFloat(quantity),
+                    price: parseFloat(price),
+                    date: dataDate,
+                    supplier_id: supplierId ? parseInt(supplierId) : null,
+                    notes: notes
+                }),
+            });
+
+            alert('╪ز┘à ╪ح╪╢╪د┘╪ر ╪د┘╪▒╪╡┘è╪» ╪ذ┘╪ش╪د╪ص!');
+            router.push('/manufacturing/raw-materials');
+
+        } catch (error: unknown) {
+            console.error('Error adding stock:', error);
+            const message = error instanceof Error ? error.message : '┘╪┤┘ ╪╣┘à┘┘è╪ر ╪د┘╪ح╪╢╪د┘╪ر';
+            alert(`╪«╪╖╪ث: ${message}`);
+        }
+    };
+
+    if (loading) return <div className="text-white text-center p-10">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
+    if (!rawMaterial) return <div className="text-white text-center p-10">╪د┘┘à╪د╪»╪ر ╪د┘╪«╪د┘à ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»╪ر</div>;
+
+    return (
+        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
+            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
+                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
+                    <h1 className="text-2xl font-bold text-white flex items-center gap-3">
+                        <span className="text-3xl"><Plus /></span>
+                        ╪ح╪╢╪د┘╪ر ╪▒╪╡┘è╪»: {rawMaterial.product.name}
+                    </h1>
+                    <button
+                        onClick={() => router.back()}
+                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
+                    >
+                        ╪╣┘ê╪»╪ر
+                    </button>
+                </div>
+            </header>
+
+            <main className="container mx-auto px-6 py-8">
+                <div className="max-w-2xl mx-auto bg-white/10 backdrop-blur-lg p-8 rounded-2xl border border-white/20">
+                    <form onSubmit={handleSubmit} className="space-y-6">
+
+                        <div className="grid grid-cols-2 gap-6">
+                            <div>
+                                <label className="block text-gray-300 text-sm font-semibold mb-2">
+                                    ╪د┘┘â┘à┘è╪ر ({rawMaterial.product.unit}) *
+                                </label>
+                                <input
+                                    type="number"
+                                    step="0.01"
+                                    required
+                                    dir="ltr"
+                                    value={quantity}
+                                    onChange={(e) => setQuantity(e.target.value)}
+                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500 text-right"
+                                    placeholder="0.00"
+                                />
+                            </div>
+                            <div>
+                                <label className="block text-gray-300 text-sm font-semibold mb-2">
+                                    ╪│╪╣╪▒ ╪د┘┘ê╪ص╪»╪ر ({price ? (parseFloat(price) / parseFloat(quantity || '1')).toFixed(2) : '0.00'}) *
+                                </label>
+                                <input
+                                    type="number"
+                                    step="0.01"
+                                    required
+                                    dir="ltr"
+                                    value={price}
+                                    onChange={(e) => setPrice(e.target.value)}
+                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500 text-right"
+                                    placeholder="0.00"
+                                />
+                                <p className="text-xs text-gray-400 mt-1">╪ث╪»╪«┘ ╪│╪╣╪▒ ╪د┘╪┤╪▒╪د╪ة ┘┘┘ê╪ص╪»╪ر ╪د┘┘ê╪د╪ص╪»╪ر</p>
+                            </div>
+                        </div>
+
+                        <div className="grid grid-cols-2 gap-6">
+                            <div>
+                                <label className="block text-gray-300 text-sm font-semibold mb-2">
+                                    ╪د┘╪ز╪د╪▒┘è╪« *
+                                </label>
+                                <input
+                                    type="date"
+                                    required
+                                    value={dataDate}
+                                    onChange={(e) => setDate(e.target.value)}
+                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500"
+                                />
+                            </div>
+                            <div>
+                                <label className="block text-gray-300 text-sm font-semibold mb-2">
+                                    ╪د┘┘à┘ê╪▒╪» (╪د╪«╪ز┘è╪د╪▒┘è)
+                                </label>
+                                <select
+                                    value={supplierId}
+                                    onChange={(e) => setSupplierId(e.target.value)}
+                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500"
+                                >
+                                    <option value="">╪د╪«╪ز╪▒ ┘à┘ê╪▒╪»...</option>
+                                    {suppliers.map(s => (
+                                        <option key={s.id} value={s.id}>{s.name}</option>
+                                    ))}
+                                </select>
+                            </div>
+                        </div>
+
+                        <div>
+                            <label className="block text-gray-300 text-sm font-semibold mb-2">
+                                ┘à┘╪د╪ص╪╕╪د╪ز
+                            </label>
+                            <textarea
+                                value={notes}
+                                onChange={(e) => setNotes(e.target.value)}
+                                rows={3}
+                                className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500"
+                            />
+                        </div>
+
+                        <button
+                            type="submit"
+                            className="w-full py-4 bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-bold rounded-xl shadow-lg transition transform hover:scale-[1.02]"
+                        >
+                            <Save /> ╪ص┘╪╕ ┘ê╪ح╪╢╪د┘╪ر ┘┘╪▒╪╡┘è╪»
+                        </button>
+                    </form>
+                </div>
+            </main>
+        </div>
+    );
+}
diff --git a/frontend/app/manufacturing/raw-materials/[id]/add-stock/page.tsx b/frontend/app/manufacturing/raw-materials/[id]/add-stock/page.tsx
index 5dd53ae..1f67faa 100644
--- a/frontend/app/manufacturing/raw-materials/[id]/add-stock/page.tsx
+++ b/frontend/app/manufacturing/raw-materials/[id]/add-stock/page.tsx
@@ -1,198 +1,9 @@
-'use client';
+import AddStockClientPage from './client';
 
-import { useState, useEffect, useCallback } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '@/lib/api';
-import { sortAlphabetically } from '@/lib/sort-utils';
-import { Plus, Save } from 'lucide-react';
-
-interface RawMaterialDetail {
-    id: number;
-    product: { name: string; unit: string };
-}
-
-interface Supplier {
-    id: number;
-    name: string;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function AddRawMaterialStockPage() {
-    const router = useRouter();
-    const params = useParams();
-    const id = params.id as string;
-
-    const [loading, setLoading] = useState(true);
-    const [rawMaterial, setRawMaterial] = useState<RawMaterialDetail | null>(null);
-    const [suppliers, setSuppliers] = useState<Supplier[]>([]);
-
-    // Form Data
-    const [quantity, setQuantity] = useState('');
-    const [price, setPrice] = useState('');
-    const [dataDate, setDate] = useState(new Date().toISOString().split('T')[0]);
-    const [supplierId, setSupplierId] = useState('');
-    const [notes, setNotes] = useState('');
-
-    const fetchData = useCallback(async () => {
-        try {
-            // Fetch Raw Material Details
-            const rmData = await api.fetchWithAuth(`/manufacturing/raw-materials/${id}`);
-
-            // Fetch Suppliers
-            const supData = await api.fetchWithAuth(`/purchases/suppliers`);
-
-            if (rmData && supData) {
-                setRawMaterial(rmData);
-                setSuppliers(sortAlphabetically(Array.isArray(supData) ? supData : [], 'name'));
-            } else {
-                alert('┘╪┤┘ ┘┘è ╪ز╪ص┘à┘è┘ ╪د┘╪ذ┘è╪د┘╪د╪ز');
-                router.back();
-            }
-        } catch (error) {
-            console.error('Error fetching data:', error);
-            alert('┘╪┤┘ ┘┘è ╪ز╪ص┘à┘è┘ ╪د┘╪ذ┘è╪د┘╪د╪ز');
-            router.back();
-        } finally {
-            setLoading(false);
-        }
-    }, [id, router]);
-
-    useEffect(() => {
-        fetchData();
-    }, [fetchData]);
-
-    const handleSubmit = async (e: React.FormEvent) => {
-        e.preventDefault();
-        try {
-            await api.fetchWithAuth(`/manufacturing/raw-materials/${id}/purchase`, {
-                method: 'POST',
-                body: JSON.stringify({
-                    quantity: parseFloat(quantity),
-                    price: parseFloat(price),
-                    date: dataDate,
-                    supplier_id: supplierId ? parseInt(supplierId) : null,
-                    notes: notes
-                }),
-            });
-
-            alert('╪ز┘à ╪ح╪╢╪د┘╪ر ╪د┘╪▒╪╡┘è╪» ╪ذ┘╪ش╪د╪ص!');
-            router.push('/manufacturing/raw-materials');
-
-        } catch (error: unknown) {
-            console.error('Error adding stock:', error);
-            const message = error instanceof Error ? error.message : '┘╪┤┘ ╪╣┘à┘┘è╪ر ╪د┘╪ح╪╢╪د┘╪ر';
-            alert(`╪«╪╖╪ث: ${message}`);
-        }
-    };
-
-    if (loading) return <div className="text-white text-center p-10">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
-    if (!rawMaterial) return <div className="text-white text-center p-10">╪د┘┘à╪د╪»╪ر ╪د┘╪«╪د┘à ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»╪ر</div>;
-
-    return (
-        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
-            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
-                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
-                    <h1 className="text-2xl font-bold text-white flex items-center gap-3">
-                        <span className="text-3xl"><Plus /></span>
-                        ╪ح╪╢╪د┘╪ر ╪▒╪╡┘è╪»: {rawMaterial.product.name}
-                    </h1>
-                    <button
-                        onClick={() => router.back()}
-                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
-                    >
-                        ╪╣┘ê╪»╪ر
-                    </button>
-                </div>
-            </header>
-
-            <main className="container mx-auto px-6 py-8">
-                <div className="max-w-2xl mx-auto bg-white/10 backdrop-blur-lg p-8 rounded-2xl border border-white/20">
-                    <form onSubmit={handleSubmit} className="space-y-6">
-
-                        <div className="grid grid-cols-2 gap-6">
-                            <div>
-                                <label className="block text-gray-300 text-sm font-semibold mb-2">
-                                    ╪د┘┘â┘à┘è╪ر ({rawMaterial.product.unit}) *
-                                </label>
-                                <input
-                                    type="number"
-                                    step="0.01"
-                                    required
-                                    dir="ltr"
-                                    value={quantity}
-                                    onChange={(e) => setQuantity(e.target.value)}
-                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500 text-right"
-                                    placeholder="0.00"
-                                />
-                            </div>
-                            <div>
-                                <label className="block text-gray-300 text-sm font-semibold mb-2">
-                                    ╪│╪╣╪▒ ╪د┘┘ê╪ص╪»╪ر ({price ? (parseFloat(price) / parseFloat(quantity || '1')).toFixed(2) : '0.00'}) *
-                                </label>
-                                <input
-                                    type="number"
-                                    step="0.01"
-                                    required
-                                    dir="ltr"
-                                    value={price}
-                                    onChange={(e) => setPrice(e.target.value)}
-                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500 text-right"
-                                    placeholder="0.00"
-                                />
-                                <p className="text-xs text-gray-400 mt-1">╪ث╪»╪«┘ ╪│╪╣╪▒ ╪د┘╪┤╪▒╪د╪ة ┘┘┘ê╪ص╪»╪ر ╪د┘┘ê╪د╪ص╪»╪ر</p>
-                            </div>
-                        </div>
-
-                        <div className="grid grid-cols-2 gap-6">
-                            <div>
-                                <label className="block text-gray-300 text-sm font-semibold mb-2">
-                                    ╪د┘╪ز╪د╪▒┘è╪« *
-                                </label>
-                                <input
-                                    type="date"
-                                    required
-                                    value={dataDate}
-                                    onChange={(e) => setDate(e.target.value)}
-                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500"
-                                />
-                            </div>
-                            <div>
-                                <label className="block text-gray-300 text-sm font-semibold mb-2">
-                                    ╪د┘┘à┘ê╪▒╪» (╪د╪«╪ز┘è╪د╪▒┘è)
-                                </label>
-                                <select
-                                    value={supplierId}
-                                    onChange={(e) => setSupplierId(e.target.value)}
-                                    className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500"
-                                >
-                                    <option value="">╪د╪«╪ز╪▒ ┘à┘ê╪▒╪»...</option>
-                                    {suppliers.map(s => (
-                                        <option key={s.id} value={s.id}>{s.name}</option>
-                                    ))}
-                                </select>
-                            </div>
-                        </div>
-
-                        <div>
-                            <label className="block text-gray-300 text-sm font-semibold mb-2">
-                                ┘à┘╪د╪ص╪╕╪د╪ز
-                            </label>
-                            <textarea
-                                value={notes}
-                                onChange={(e) => setNotes(e.target.value)}
-                                rows={3}
-                                className="w-full px-4 py-3 bg-white/5 border border-white/20 rounded-lg text-white focus:outline-none focus:border-blue-500"
-                            />
-                        </div>
-
-                        <button
-                            type="submit"
-                            className="w-full py-4 bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-bold rounded-xl shadow-lg transition transform hover:scale-[1.02]"
-                        >
-                            <Save /> ╪ص┘╪╕ ┘ê╪ح╪╢╪د┘╪ر ┘┘╪▒╪╡┘è╪»
-                        </button>
-                    </form>
-                </div>
-            </main>
-        </div>
-    );
+export default function Page() {
+  return <AddStockClientPage />;
 }
diff --git a/frontend/app/manufacturing/raw-materials/[id]/client.tsx b/frontend/app/manufacturing/raw-materials/[id]/client.tsx
new file mode 100644
index 0000000..2d2b350
--- /dev/null
+++ b/frontend/app/manufacturing/raw-materials/[id]/client.tsx
@@ -0,0 +1,514 @@
+'use client';
+
+import { useState, useEffect, useCallback } from 'react';
+import { useRouter, useParams } from 'next/navigation';
+import { api } from '../../../../lib/api';
+import { FileText, BarChart3, Plus, Pencil, Trash2, RefreshCw } from 'lucide-react';
+
+interface RawMaterial {
+    id: number;
+    current_stock: number;
+    last_purchase_price?: number;
+    reorder_point: number;
+    stock_status: string;
+    product: {
+        id: number;
+        name: string;
+        unit: string;
+        cost_price: number;
+    };
+}
+
+interface StockMovement {
+    id: number;
+    date: string;
+    type: string;
+    quantity: number;
+    price?: number;
+    reference?: string;
+    notes?: string;
+}
+
+export default function RawMaterialDetailsPage() {
+    const router = useRouter();
+    const params = useParams();
+    const id = params.id as string;
+
+    const [loading, setLoading] = useState(true);
+    const [rawMaterial, setRawMaterial] = useState<RawMaterial | null>(null);
+    const [movements, setMovements] = useState<StockMovement[]>([]);
+
+    const [showEditDialog, setShowEditDialog] = useState(false);
+    const [editingMovement, setEditingMovement] = useState<StockMovement | null>(null);
+    const [editForm, setEditForm] = useState({
+        quantity: '',
+        price: '',
+        date: '',
+        notes: ''
+    });
+
+    const [showAddDialog, setShowAddDialog] = useState(false);
+    const [addForm, setAddForm] = useState({
+        type: 'IN',
+        quantity: '',
+        price: '',
+        date: new Date().toISOString().split('T')[0],
+        reference: '',
+        notes: ''
+    });
+
+    const [filterPeriod, setFilterPeriod] = useState('ALL');
+    const [filteredMovements, setFilteredMovements] = useState<StockMovement[]>([]);
+
+    const filterData = useCallback(() => {
+        if (filterPeriod === 'ALL') {
+            setFilteredMovements(movements);
+            return;
+        }
+
+        const now = new Date();
+        const startDate = new Date();
+
+        switch (filterPeriod) {
+            case 'DAY': startDate.setDate(now.getDate() - 1); break;
+            case 'MONTH': startDate.setMonth(now.getMonth() - 1); break;
+            case '3MONTHS': startDate.setMonth(now.getMonth() - 3); break;
+            case '6MONTHS': startDate.setMonth(now.getMonth() - 6); break;
+            case 'YEAR': startDate.setFullYear(now.getFullYear() - 1); break;
+        }
+
+        const filtered = movements.filter(m => new Date(m.date) >= startDate);
+        setFilteredMovements(filtered);
+    }, [movements, filterPeriod]);
+
+    const fetchData = useCallback(async () => {
+        try {
+            const raw = await api.getRawMaterial(+id);
+            const movementsData = await api.getRawMaterialMovements(+id);
+            setRawMaterial(raw);
+            setMovements(movementsData);
+        } catch (error: unknown) {
+            console.error('Error fetching details:', error);
+            if (error && typeof error === 'object' && 'status' in error && error.status === 404) {
+                setRawMaterial(null);
+            }
+        } finally {
+            setLoading(false);
+        }
+    }, [id]);
+
+    useEffect(() => {
+        fetchData();
+    }, [fetchData]);
+
+    useEffect(() => {
+        filterData();
+    }, [filterData]);
+
+    const handleDeleteMovement = async (movId: number) => {
+        if (!confirm('┘ç┘ ╪ث┘╪ز ┘à╪ز╪ث┘â╪» ┘à┘ ╪ص╪░┘ ┘ç╪░╪د ╪د┘╪│╪ش┘╪ا\n╪│┘è╪ز┘à ╪╣┘â╪│ ╪ز╪ث╪س┘è╪▒┘ç ╪╣┘┘ë ╪د┘┘à╪«╪▓┘ê┘!')) return;
+
+        try {
+            await api.deleteStockMovement(movId);
+            alert('╪ز┘à ╪د┘╪ص╪░┘ ╪ذ┘╪ش╪د╪ص');
+            fetchData();
+        } catch (error) {
+            console.error('Error deleting movement:', error);
+            alert('╪ص╪»╪س ╪«╪╖╪ث');
+        }
+    };
+
+    const handleEditMovement = (mov: StockMovement) => {
+        setEditingMovement(mov);
+        setEditForm({
+            quantity: String(mov.quantity),
+            price: String(mov.price ?? ''),
+            date: mov.date?.split('T')[0] ?? '',
+            notes: mov.notes || ''
+        });
+        setShowEditDialog(true);
+    };
+
+    const handleSaveEdit = async (e: React.FormEvent) => {
+        e.preventDefault();
+        if (!editingMovement) return;
+
+        try {
+            await api.updateStockMovement(editingMovement.id, {
+                quantity: parseFloat(editForm.quantity),
+                price: parseFloat(editForm.price) || 0,
+                date: editForm.date,
+                notes: editForm.notes
+            });
+            setShowEditDialog(false);
+            setEditingMovement(null);
+            fetchData();
+            alert('╪ز┘à ╪ز╪ص╪»┘è╪س ╪د┘╪│╪ش┘ ╪ذ┘╪ش╪د╪ص');
+        } catch (error) {
+            console.error('Error updating movement:', error);
+            alert('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪د┘╪ز╪ص╪»┘è╪س');
+        }
+    };
+
+    const handleAddMovement = async (e: React.FormEvent) => {
+        e.preventDefault();
+        try {
+            if (addForm.type === 'IN') {
+                await api.addRawMaterialStock(+id, {
+                    quantity: parseFloat(addForm.quantity),
+                    price: parseFloat(addForm.price) || undefined,
+                    date: addForm.date,
+                    notes: addForm.notes
+                });
+            } else {
+                await api.createStockMovement({
+                    productId: +id,
+                    type: addForm.type as 'IN' | 'OUT',
+                    quantity: parseFloat(addForm.quantity),
+                    price: parseFloat(addForm.price) || 0,
+                    date: addForm.date,
+                    reference: addForm.reference,
+                    notes: addForm.notes
+                });
+            }
+            setShowAddDialog(false);
+            setAddForm({
+                type: 'IN',
+                quantity: '',
+                price: '',
+                date: new Date().toISOString().split('T')[0],
+                reference: '',
+                notes: ''
+            });
+            fetchData();
+            alert('╪ز┘à ╪ح╪╢╪د┘╪ر ╪د┘╪ص╪▒┘â╪ر ╪ذ┘╪ش╪د╪ص');
+        } catch (error) {
+            console.error('Error adding movement:', error);
+            alert('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪د┘╪ح╪╢╪د┘╪ر');
+        }
+    };
+
+    if (loading) return <div className="text-white text-center p-10">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
+    if (!rawMaterial) return <div className="text-white text-center p-10">╪د┘┘à╪د╪»╪ر ╪د┘╪«╪د┘à ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»╪ر</div>;
+
+    return (
+        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
+            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
+                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
+                    <h1 className="text-2xl font-bold text-white flex items-center gap-3">
+                        <span className="text-3xl"><FileText /></span>
+                        ╪ز┘╪د╪╡┘è┘: {rawMaterial.product.name}
+                    </h1>
+                    <button
+                        onClick={() => router.push('/manufacturing/raw-materials')}
+                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
+                    >
+                        ╪╣┘ê╪»╪ر ┘┘┘é╪د╪خ┘à╪ر
+                    </button>
+                </div>
+            </header>
+
+            <main className="container mx-auto px-6 py-8">
+                <div className="bg-white/10 backdrop-blur-lg p-6 rounded-2xl border border-white/20 mb-8">
+                    <div className="grid grid-cols-1 md:grid-cols-4 gap-6 text-center">
+                        <div className="relative group">
+                            <p className="text-gray-400 text-sm">╪د┘┘à╪«╪▓┘ê┘ ╪د┘╪ص╪د┘┘è</p>
+                            <div className="flex items-center justify-center gap-2 mt-1">
+                                <p className="text-3xl font-bold text-white">
+                                    {rawMaterial.current_stock} <span className="text-sm font-normal">{rawMaterial.product.unit}</span>
+                                </p>
+                                <button
+                                    onClick={async () => {
+                                        if (!confirm('┘ç┘ ╪ز╪▒┘è╪» ╪ح╪╣╪د╪»╪ر ╪ص╪│╪د╪ذ ╪د┘┘à╪«╪▓┘ê┘ ╪ذ┘╪د╪ة┘ï ╪╣┘┘ë ╪│╪ش┘ ╪د┘╪ص╪▒┘â╪د╪ز╪ا\n╪│┘è╪ز┘à ╪ز╪╣╪»┘è┘ ╪د┘╪▒┘é┘à ╪د┘╪ص╪د┘┘è ┘┘è╪╖╪د╪ذ┘é ┘à╪ش┘à┘ê╪╣ ╪د┘╪╣┘à┘┘è╪د╪ز ╪د┘┘à╪│╪ش┘╪ر.')) return;
+                                        try {
+                                            const data = await api.recalculateRawMaterialStock(+id);
+                                            alert(`╪ز┘à ╪د┘╪ز╪ص╪»┘è╪س ╪ذ┘╪ش╪د╪ص\n╪د┘╪▒╪╡┘è╪» ╪د┘┘à╪ص╪│┘ê╪ذ: ${data.calculated_stock}`);
+                                            fetchData();
+                                        } catch (err) {
+                                            console.error(err);
+                                            alert('╪ص╪»╪س ╪«╪╖╪ث');
+                                        }
+                                    }}
+                                    className="opacity-0 group-hover:opacity-100 transition p-1 bg-blue-500/20 hover:bg-blue-500/40 text-blue-300 rounded text-xs"
+                                    title="╪ح╪╣╪د╪»╪ر ╪د╪ص╪ز╪│╪د╪ذ ╪د┘╪▒╪╡┘è╪» ┘à┘ ╪د┘╪│╪ش┘"
+                                >
+                                    <RefreshCw /> ╪ز╪╡╪ص┘è╪ص
+                                </button>
+                            </div>
+                        </div>
+                        <div>
+                            <p className="text-gray-400 text-sm">╪ت╪«╪▒ ╪│╪╣╪▒ ╪┤╪▒╪د╪ة</p>
+                            <p className="text-2xl font-bold text-green-400 mt-1">
+                                {Number(rawMaterial.last_purchase_price ?? rawMaterial.product?.cost_price ?? 0).toFixed(2)} ╪ش.┘à
+                            </p>
+                        </div>
+                        <div>
+                            <p className="text-gray-400 text-sm">╪د┘╪ص╪» ╪د┘╪ث╪»┘┘ë</p>
+                            <p className="text-2xl font-bold text-yellow-400 mt-1">
+                                {rawMaterial.reorder_point}
+                            </p>
+                        </div>
+                        <div>
+                            <p className="text-gray-400 text-sm">╪د┘╪ص╪د┘╪ر</p>
+                            <span className={`inline-block mt-2 px-3 py-1 rounded-full text-sm font-bold 
+                                ${rawMaterial.stock_status === 'NORMAL' ? 'bg-green-500/20 text-green-400' :
+                                    rawMaterial.stock_status === 'LOW_STOCK' ? 'bg-yellow-500/20 text-yellow-400' : 'bg-red-500/20 text-red-400'}`}>
+                                {rawMaterial.stock_status === 'NORMAL' ? '╪╣╪د╪»┘è' : rawMaterial.stock_status === 'LOW_STOCK' ? '┘à┘╪«┘╪╢' : '┘┘╪░'}
+                            </span>
+                        </div>
+                    </div>
+                </div>
+
+                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
+                    <div className="p-6 border-b border-white/10 flex flex-col md:flex-row justify-between items-center gap-4">
+                        <h3 className="text-xl font-bold text-white flex items-center gap-2"><BarChart3 /> ╪│╪ش┘ ╪د┘╪ص╪▒┘â╪ر ╪د┘┘è┘ê┘à┘è</h3>
+                        <div className="flex items-center gap-2">
+                            <div className="flex bg-white/5 rounded-lg p-1 gap-1 overflow-x-auto">
+                                {[
+                                    { label: '╪د┘┘â┘', value: 'ALL' },
+                                    { label: '┘è┘ê┘à', value: 'DAY' },
+                                    { label: '╪┤┘ç╪▒', value: 'MONTH' },
+                                    { label: '3 ╪┤┘ç┘ê╪▒', value: '3MONTHS' },
+                                    { label: '6 ╪┤┘ç┘ê╪▒', value: '6MONTHS' },
+                                    { label: '╪│┘╪ر', value: 'YEAR' },
+                                ].map((opt) => (
+                                    <button
+                                        key={opt.value}
+                                        onClick={() => setFilterPeriod(opt.value)}
+                                        className={`px-3 py-1.5 rounded-md text-sm transition ${filterPeriod === opt.value
+                                            ? 'bg-blue-600/80 text-white font-bold'
+                                            : 'text-gray-400 hover:text-white hover:bg-white/10'
+                                            }`}
+                                    >
+                                        {opt.label}
+                                    </button>
+                                ))}
+                            </div>
+                            <button
+                                onClick={() => setShowAddDialog(true)}
+                                className="px-3 py-1.5 bg-green-600/80 text-white rounded-md text-sm hover:bg-green-700"
+                            >
+                                <Plus /> ╪ح╪╢╪د┘╪ر ╪ص╪▒┘â╪ر
+                            </button>
+                        </div>
+                    </div>
+                    <div className="overflow-x-auto">
+                        <table className="w-full">
+                            <thead className="bg-white/5">
+                                <tr>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘╪ز╪د╪▒┘è╪«</th>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘┘┘ê╪╣</th>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘┘â┘à┘è╪ر</th>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘╪│╪╣╪▒</th>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘┘à┘ê╪▒╪» / ╪د┘┘à╪▒╪ش╪╣</th>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">┘à┘╪د╪ص╪╕╪د╪ز</th>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪ز╪╣╪»┘è┘</th>
+                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪ص╪░┘</th>
+                                </tr>
+                            </thead>
+                            <tbody className="divide-y divide-white/10">
+                                {filteredMovements.length === 0 ? (
+                                    <tr>
+                                        <td colSpan={8} className="px-6 py-12 text-center text-gray-400">
+                                            ┘╪د ╪ز┘ê╪ش╪» ╪ص╪▒┘â╪د╪ز ┘à╪│╪ش┘╪ر
+                                        </td>
+                                    </tr>
+                                ) : (
+                                    filteredMovements.map((mov, idx) => (
+                                        <tr key={idx} className="hover:bg-white/5 transition">
+                                            <td className="px-6 py-4 text-gray-300">
+                                                {mov.date ? new Date(mov.date).toLocaleDateString('ar-EG') : '-'}
+                                            </td>
+                                            <td className="px-6 py-4">
+                                                <span className={`px-2 py-1 rounded text-xs font-bold ${mov.type === 'IN' ? 'bg-green-500/20 text-green-400' : 'bg-red-500/20 text-red-400'
+                                                    }`}>
+                                                    {mov.type === 'IN' ? '╪┤╪▒╪د╪ة / ╪ح╪╢╪د┘╪ر' : '╪د╪│╪ز┘ç┘╪د┘â / ╪╡╪▒┘'}
+                                                </span>
+                                            </td>
+                                            <td className="px-6 py-4 text-white font-mono">{mov.quantity}</td>
+                                            <td className="px-6 py-4 text-gray-300">{mov.price ? `${Number(mov.price).toFixed(2)} ╪ش.┘à` : '-'}</td>
+                                            <td className="px-6 py-4 text-gray-300">{mov.reference}</td>
+                                            <td className="px-6 py-4 text-gray-400 text-sm">{mov.notes || '-'}</td>
+                                            <td className="px-6 py-4 flex gap-2 justify-end">
+                                                <button
+                                                    onClick={() => handleEditMovement(mov)}
+                                                    className="p-1 px-3 bg-yellow-500/10 hover:bg-yellow-500/20 text-yellow-500 rounded text-sm transition"
+                                                >
+                                                    <Pencil />
+                                                </button>
+                                                <button
+                                                    onClick={() => handleDeleteMovement(mov.id)}
+                                                    className="p-1 px-3 bg-red-500/10 hover:bg-red-500/20 text-red-500 rounded text-sm transition"
+                                                >
+                                                    <Trash2 />
+                                                </button>
+                                            </td>
+                                        </tr>
+                                    ))
+                                )}
+                            </tbody>
+                        </table>
+                    </div>
+                </div>
+
+                {showEditDialog && (
+                    <div className="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50">
+                        <div className="bg-slate-800 rounded-2xl p-8 max-w-md w-full mx-4 border border-white/20">
+                            <h2 className="text-xl font-bold text-white mb-6 flex items-center gap-2"><Pencil /> ╪ز╪╣╪»┘è┘ ╪د┘╪│╪ش┘</h2>
+                            <form onSubmit={handleSaveEdit} className="space-y-4">
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪ز╪د╪▒┘è╪«</label>
+                                    <input
+                                        type="date"
+                                        required
+                                        value={editForm.date}
+                                        onChange={(e) => setEditForm({ ...editForm, date: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
+                                    />
+                                </div>
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘â┘à┘è╪ر</label>
+                                    <input
+                                        type="number"
+                                        step="0.01"
+                                        required
+                                        dir="ltr"
+                                        value={editForm.quantity}
+                                        onChange={(e) => setEditForm({ ...editForm, quantity: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
+                                    />
+                                </div>
+                                {editingMovement?.type === 'IN' && (
+                                    <div>
+                                        <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪│╪╣╪▒</label>
+                                        <input
+                                            type="number"
+                                            step="0.01"
+                                            dir="ltr"
+                                            value={editForm.price}
+                                            onChange={(e) => setEditForm({ ...editForm, price: e.target.value })}
+                                            className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
+                                        />
+                                    </div>
+                                )}
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">┘à┘╪د╪ص╪╕╪د╪ز</label>
+                                    <textarea
+                                        value={editForm.notes}
+                                        onChange={(e) => setEditForm({ ...editForm, notes: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
+                                        rows={3}
+                                    />
+                                </div>
+                                <div className="flex gap-4 pt-4">
+                                    <button
+                                        type="submit"
+                                        className="flex-1 px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition"
+                                    >
+                                        ╪ص┘╪╕ ╪د┘╪ز╪╣╪»┘è┘╪د╪ز
+                                    </button>
+                                    <button
+                                        type="button"
+                                        onClick={() => setShowEditDialog(false)}
+                                        className="flex-1 px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white rounded-lg font-semibold transition"
+                                    >
+                                        ╪ح┘╪║╪د╪ة
+                                    </button>
+                                </div>
+                            </form>
+                        </div>
+                    </div>
+                )}
+
+                {showAddDialog && (
+                    <div className="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50">
+                        <div className="bg-slate-800 rounded-2xl p-8 max-w-md w-full mx-4 border border-white/20">
+                            <h2 className="text-xl font-bold text-white mb-6 flex items-center gap-2"><Plus /> ╪ح╪╢╪د┘╪ر ╪ص╪▒┘â╪ر</h2>
+                            <form onSubmit={handleAddMovement} className="space-y-4">
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘┘ê╪╣</label>
+                                    <select
+                                        value={addForm.type}
+                                        onChange={(e) => setAddForm({ ...addForm, type: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
+                                    >
+                                        <option value="IN">╪┤╪▒╪د╪ة / ╪ح╪╢╪د┘╪ر</option>
+                                        <option value="OUT">╪د╪│╪ز┘ç┘╪د┘â / ╪╡╪▒┘</option>
+                                    </select>
+                                </div>
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪ز╪د╪▒┘è╪«</label>
+                                    <input
+                                        type="date"
+                                        required
+                                        value={addForm.date}
+                                        onChange={(e) => setAddForm({ ...addForm, date: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
+                                    />
+                                </div>
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘â┘à┘è╪ر</label>
+                                    <input
+                                        type="number"
+                                        step="0.01"
+                                        required
+                                        dir="ltr"
+                                        value={addForm.quantity}
+                                        onChange={(e) => setAddForm({ ...addForm, quantity: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
+                                    />
+                                </div>
+                                {addForm.type === 'IN' && (
+                                    <div>
+                                        <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪│╪╣╪▒</label>
+                                        <input
+                                            type="number"
+                                            step="0.01"
+                                            dir="ltr"
+                                            value={addForm.price}
+                                            onChange={(e) => setAddForm({ ...addForm, price: e.target.value })}
+                                            className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
+                                        />
+                                    </div>
+                                )}
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘à┘ê╪▒╪» / ╪د┘┘à╪▒╪ش╪╣</label>
+                                    <input
+                                        type="text"
+                                        value={addForm.reference}
+                                        onChange={(e) => setAddForm({ ...addForm, reference: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
+                                    />
+                                </div>
+                                <div>
+                                    <label className="block text-gray-300 text-sm font-semibold mb-2">┘à┘╪د╪ص╪╕╪د╪ز</label>
+                                    <textarea
+                                        value={addForm.notes}
+                                        onChange={(e) => setAddForm({ ...addForm, notes: e.target.value })}
+                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
+                                        rows={3}
+                                    />
+                                </div>
+                                <div className="flex gap-4 pt-4">
+                                    <button
+                                        type="submit"
+                                        className="flex-1 px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg font-semibold transition"
+                                    >
+                                        ╪ح╪╢╪د┘╪ر ╪د┘╪ص╪▒┘â╪ر
+                                    </button>
+                                    <button
+                                        type="button"
+                                        onClick={() => setShowAddDialog(false)}
+                                        className="flex-1 px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white rounded-lg font-semibold transition"
+                                    >
+                                        ╪ح┘╪║╪د╪ة
+                                    </button>
+                                </div>
+                            </form>
+                        </div>
+                    </div>
+                )}
+            </main>
+        </div>
+    );
+}
diff --git a/frontend/app/manufacturing/raw-materials/[id]/page.tsx b/frontend/app/manufacturing/raw-materials/[id]/page.tsx
index a137526..a3a5f5a 100644
--- a/frontend/app/manufacturing/raw-materials/[id]/page.tsx
+++ b/frontend/app/manufacturing/raw-materials/[id]/page.tsx
@@ -1,522 +1,9 @@
-'use client';
+import RawMaterialClientPage from './client';
 
-import { useState, useEffect, useCallback } from 'react';
-import { useRouter, useParams } from 'next/navigation';
-import { api } from '../../../../lib/api';
-import { FileText, BarChart3, Plus, Pencil, Trash2, RefreshCw } from 'lucide-react';
-
-interface RawMaterial {
-    id: number;
-    current_stock: number;
-    last_purchase_price?: number;
-    reorder_point: number;
-    stock_status: string;
-    product: {
-        id: number;
-        name: string;
-        unit: string;
-        cost_price: number;
-    };
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-interface StockMovement {
-    id: number;
-    date: string;
-    type: string;
-    quantity: number;
-    price?: number;
-    reference?: string;
-    notes?: string;
+export default function Page() {
+  return <RawMaterialClientPage />;
 }
-
-export default function RawMaterialDetailsPage() {
-    const router = useRouter();
-    const params = useParams();
-    const id = params.id as string;
-
-    const [loading, setLoading] = useState(true);
-    const [rawMaterial, setRawMaterial] = useState<RawMaterial | null>(null);
-    const [movements, setMovements] = useState<StockMovement[]>([]);
-
-    const [showEditDialog, setShowEditDialog] = useState(false);
-    const [editingMovement, setEditingMovement] = useState<StockMovement | null>(null);
-    const [editForm, setEditForm] = useState({
-        quantity: '',
-        price: '',
-        date: '',
-        notes: ''
-    });
-
-    // Add Movement states
-    const [showAddDialog, setShowAddDialog] = useState(false);
-    const [addForm, setAddForm] = useState({
-        type: 'IN',
-        quantity: '',
-        price: '',
-        date: new Date().toISOString().split('T')[0],
-        reference: '',
-        notes: ''
-    });
-
-    const [filterPeriod, setFilterPeriod] = useState('ALL');
-    const [filteredMovements, setFilteredMovements] = useState<StockMovement[]>([]);
-
-    const filterData = useCallback(() => {
-        if (filterPeriod === 'ALL') {
-            setFilteredMovements(movements);
-            return;
-        }
-
-        const now = new Date();
-        const startDate = new Date();
-
-        switch (filterPeriod) {
-            case 'DAY': startDate.setDate(now.getDate() - 1); break;
-            case 'MONTH': startDate.setMonth(now.getMonth() - 1); break;
-            case '3MONTHS': startDate.setMonth(now.getMonth() - 3); break;
-            case '6MONTHS': startDate.setMonth(now.getMonth() - 6); break;
-            case 'YEAR': startDate.setFullYear(now.getFullYear() - 1); break;
-        }
-
-        const filtered = movements.filter(m => new Date(m.date) >= startDate);
-        setFilteredMovements(filtered);
-    }, [movements, filterPeriod]);
-
-    const fetchData = useCallback(async () => {
-        try {
-            const raw = await api.getRawMaterial(+id);
-            const movementsData = await api.getRawMaterialMovements(+id);
-            setRawMaterial(raw);
-            setMovements(movementsData);
-        } catch (error: unknown) {
-            console.error('Error fetching details:', error);
-            if (error && typeof error === 'object' && 'status' in error && error.status === 404) {
-                setRawMaterial(null);
-            }
-        } finally {
-            setLoading(false);
-        }
-    }, [id]);
-
-    useEffect(() => {
-        fetchData();
-    }, [fetchData]);
-
-    useEffect(() => {
-        filterData();
-    }, [filterData]);
-
-    const handleDeleteMovement = async (movId: number) => {
-        if (!confirm('┘ç┘ ╪ث┘╪ز ┘à╪ز╪ث┘â╪» ┘à┘ ╪ص╪░┘ ┘ç╪░╪د ╪د┘╪│╪ش┘╪ا\n╪│┘è╪ز┘à ╪╣┘â╪│ ╪ز╪ث╪س┘è╪▒┘ç ╪╣┘┘ë ╪د┘┘à╪«╪▓┘ê┘!')) return;
-
-        try {
-            await api.deleteStockMovement(movId);
-            alert('╪ز┘à ╪د┘╪ص╪░┘ ╪ذ┘╪ش╪د╪ص');
-            fetchData();
-        } catch (error) {
-            console.error('Error deleting movement:', error);
-            alert('╪ص╪»╪س ╪«╪╖╪ث');
-        }
-    };
-
-    const handleEditMovement = (mov: StockMovement) => {
-        setEditingMovement(mov);
-        setEditForm({
-            quantity: String(mov.quantity),
-            price: String(mov.price ?? ''),
-            date: mov.date?.split('T')[0] ?? '',
-            notes: mov.notes || ''
-        });
-        setShowEditDialog(true);
-    };
-
-    const handleSaveEdit = async (e: React.FormEvent) => {
-        e.preventDefault();
-        if (!editingMovement) return;
-
-        try {
-            await api.updateStockMovement(editingMovement.id, {
-                quantity: parseFloat(editForm.quantity),
-                price: parseFloat(editForm.price) || 0,
-                date: editForm.date,
-                notes: editForm.notes
-            });
-            setShowEditDialog(false);
-            setEditingMovement(null);
-            fetchData();
-            alert('╪ز┘à ╪ز╪ص╪»┘è╪س ╪د┘╪│╪ش┘ ╪ذ┘╪ش╪د╪ص');
-        } catch (error) {
-            console.error('Error updating movement:', error);
-            alert('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪د┘╪ز╪ص╪»┘è╪س');
-        }
-    };
-
-    const handleAddMovement = async (e: React.FormEvent) => {
-        e.preventDefault();
-        try {
-            // Use addRawMaterialStock for IN movements (purchases)
-            if (addForm.type === 'IN') {
-                await api.addRawMaterialStock(+id, {
-                    quantity: parseFloat(addForm.quantity),
-                    price: parseFloat(addForm.price) || undefined,
-                    date: addForm.date,
-                    notes: addForm.notes
-                });
-            } else {
-                // For OUT movements, use createStockMovement
-                await api.createStockMovement({
-                    productId: +id,
-                    type: addForm.type as 'IN' | 'OUT',
-                    quantity: parseFloat(addForm.quantity),
-                    price: parseFloat(addForm.price) || 0,
-                    date: addForm.date,
-                    reference: addForm.reference,
-                    notes: addForm.notes
-                });
-            }
-            setShowAddDialog(false);
-            setAddForm({
-                type: 'IN',
-                quantity: '',
-                price: '',
-                date: new Date().toISOString().split('T')[0],
-                reference: '',
-                notes: ''
-            });
-            fetchData();
-            alert('╪ز┘à ╪ح╪╢╪د┘╪ر ╪د┘╪ص╪▒┘â╪ر ╪ذ┘╪ش╪د╪ص');
-        } catch (error) {
-            console.error('Error adding movement:', error);
-            alert('╪ص╪»╪س ╪«╪╖╪ث ╪ث╪س┘╪د╪ة ╪د┘╪ح╪╢╪د┘╪ر');
-        }
-    };
-
-    if (loading) return <div className="text-white text-center p-10">╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
-    if (!rawMaterial) return <div className="text-white text-center p-10">╪د┘┘à╪د╪»╪ر ╪د┘╪«╪د┘à ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»╪ر</div>;
-
-    return (
-        <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900" dir="rtl">
-            <header className="bg-white/10 backdrop-blur-lg border-b border-white/20 sticky top-0 z-50">
-                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
-                    <h1 className="text-2xl font-bold text-white flex items-center gap-3">
-                        <span className="text-3xl"><FileText /></span>
-                        ╪ز┘╪د╪╡┘è┘: {rawMaterial.product.name}
-                    </h1>
-                    <button
-                        onClick={() => router.push('/manufacturing/raw-materials')}
-                        className="px-4 py-2 bg-gray-500/20 hover:bg-gray-500/30 text-gray-200 rounded-lg transition"
-                    >
-                        ╪╣┘ê╪»╪ر ┘┘┘é╪د╪خ┘à╪ر
-                    </button>
-                </div>
-            </header>
-
-            <main className="container mx-auto px-6 py-8">
-                {/* Info Card */}
-                <div className="bg-white/10 backdrop-blur-lg p-6 rounded-2xl border border-white/20 mb-8">
-                    <div className="grid grid-cols-1 md:grid-cols-4 gap-6 text-center">
-                        <div className="relative group">
-                            <p className="text-gray-400 text-sm">╪د┘┘à╪«╪▓┘ê┘ ╪د┘╪ص╪د┘┘è</p>
-                            <div className="flex items-center justify-center gap-2 mt-1">
-                                <p className="text-3xl font-bold text-white">
-                                    {rawMaterial.current_stock} <span className="text-sm font-normal">{rawMaterial.product.unit}</span>
-                                </p>
-                                <button
-                                    onClick={async () => {
-                                        if (!confirm('┘ç┘ ╪ز╪▒┘è╪» ╪ح╪╣╪د╪»╪ر ╪ص╪│╪د╪ذ ╪د┘┘à╪«╪▓┘ê┘ ╪ذ┘╪د╪ة┘ï ╪╣┘┘ë ╪│╪ش┘ ╪د┘╪ص╪▒┘â╪د╪ز╪ا\n╪│┘è╪ز┘à ╪ز╪╣╪»┘è┘ ╪د┘╪▒┘é┘à ╪د┘╪ص╪د┘┘è ┘┘è╪╖╪د╪ذ┘é ┘à╪ش┘à┘ê╪╣ ╪د┘╪╣┘à┘┘è╪د╪ز ╪د┘┘à╪│╪ش┘╪ر.')) return;
-                                        try {
-                                            const data = await api.recalculateRawMaterialStock(+id);
-                                            alert(`╪ز┘à ╪د┘╪ز╪ص╪»┘è╪س ╪ذ┘╪ش╪د╪ص\n╪د┘╪▒╪╡┘è╪» ╪د┘┘à╪ص╪│┘ê╪ذ: ${data.calculated_stock}`);
-                                            fetchData();
-                                        } catch (err) {
-                                            console.error(err);
-                                            alert('╪ص╪»╪س ╪«╪╖╪ث');
-                                        }
-                                    }}
-                                    className="opacity-0 group-hover:opacity-100 transition p-1 bg-blue-500/20 hover:bg-blue-500/40 text-blue-300 rounded text-xs"
-                                    title="╪ح╪╣╪د╪»╪ر ╪د╪ص╪ز╪│╪د╪ذ ╪د┘╪▒╪╡┘è╪» ┘à┘ ╪د┘╪│╪ش┘"
-                                >
-                                    <RefreshCw /> ╪ز╪╡╪ص┘è╪ص
-                                </button>
-                            </div>
-                        </div>
-                        <div>
-                            <p className="text-gray-400 text-sm">╪ت╪«╪▒ ╪│╪╣╪▒ ╪┤╪▒╪د╪ة</p>
-                            <p className="text-2xl font-bold text-green-400 mt-1">
-                                {Number(rawMaterial.last_purchase_price ?? rawMaterial.product?.cost_price ?? 0).toFixed(2)} ╪ش.┘à
-                            </p>
-                        </div>
-                        <div>
-                            <p className="text-gray-400 text-sm">╪د┘╪ص╪» ╪د┘╪ث╪»┘┘ë</p>
-                            <p className="text-2xl font-bold text-yellow-400 mt-1">
-                                {rawMaterial.reorder_point}
-                            </p>
-                        </div>
-                        <div>
-                            <p className="text-gray-400 text-sm">╪د┘╪ص╪د┘╪ر</p>
-                            <span className={`inline-block mt-2 px-3 py-1 rounded-full text-sm font-bold 
-                                ${rawMaterial.stock_status === 'NORMAL' ? 'bg-green-500/20 text-green-400' :
-                                    rawMaterial.stock_status === 'LOW_STOCK' ? 'bg-yellow-500/20 text-yellow-400' : 'bg-red-500/20 text-red-400'}`}>
-                                {rawMaterial.stock_status === 'NORMAL' ? '╪╣╪د╪»┘è' : rawMaterial.stock_status === 'LOW_STOCK' ? '┘à┘╪«┘╪╢' : '┘┘╪░'}
-                            </span>
-                        </div>
-                    </div>
-                </div>
-
-                {/* Movements Log */}
-                <div className="bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 overflow-hidden">
-                    <div className="p-6 border-b border-white/10 flex flex-col md:flex-row justify-between items-center gap-4">
-                        <h3 className="text-xl font-bold text-white flex items-center gap-2"><BarChart3 /> ╪│╪ش┘ ╪د┘╪ص╪▒┘â╪ر ╪د┘┘è┘ê┘à┘è</h3>
-                        <div className="flex items-center gap-2">
-                            <div className="flex bg-white/5 rounded-lg p-1 gap-1 overflow-x-auto">
-                                {[
-                                    { label: '╪د┘┘â┘', value: 'ALL' },
-                                    { label: '┘è┘ê┘à', value: 'DAY' },
-                                    { label: '╪┤┘ç╪▒', value: 'MONTH' },
-                                    { label: '3 ╪┤┘ç┘ê╪▒', value: '3MONTHS' },
-                                    { label: '6 ╪┤┘ç┘ê╪▒', value: '6MONTHS' },
-                                    { label: '╪│┘╪ر', value: 'YEAR' },
-                                ].map((opt) => (
-                                    <button
-                                        key={opt.value}
-                                        onClick={() => setFilterPeriod(opt.value)}
-                                        className={`px-3 py-1.5 rounded-md text-sm transition ${filterPeriod === opt.value
-                                            ? 'bg-blue-600/80 text-white font-bold'
-                                            : 'text-gray-400 hover:text-white hover:bg-white/10'
-                                            }`}
-                                    >
-                                        {opt.label}
-                                    </button>
-                                ))}
-                            </div>
-                            {/* Add Movement Button */}
-                            <button
-                                onClick={() => setShowAddDialog(true)}
-                                className="px-3 py-1.5 bg-green-600/80 text-white rounded-md text-sm hover:bg-green-700"
-                            >
-                                <Plus /> ╪ح╪╢╪د┘╪ر ╪ص╪▒┘â╪ر
-                            </button>
-                        </div>
-                    </div>
-                    <div className="overflow-x-auto">
-                        <table className="w-full">
-                            <thead className="bg-white/5">
-                                <tr>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘╪ز╪د╪▒┘è╪«</th>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘┘┘ê╪╣</th>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘┘â┘à┘è╪ر</th>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘╪│╪╣╪▒</th>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪د┘┘à┘ê╪▒╪» / ╪د┘┘à╪▒╪ش╪╣</th>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">┘à┘╪د╪ص╪╕╪د╪ز</th>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪ز╪╣╪»┘è┘</th>
-                                    <th className="px-6 py-4 text-right text-sm font-semibold text-gray-300">╪ص╪░┘</th>
-                                </tr>
-                            </thead>
-                            <tbody className="divide-y divide-white/10">
-                                {filteredMovements.length === 0 ? (
-                                    <tr>
-                                        <td colSpan={8} className="px-6 py-12 text-center text-gray-400">
-                                            ┘╪د ╪ز┘ê╪ش╪» ╪ص╪▒┘â╪د╪ز ┘à╪│╪ش┘╪ر
-                                        </td>
-                                    </tr>
-                                ) : (
-                                    filteredMovements.map((mov, idx) => (
-                                        <tr key={idx} className="hover:bg-white/5 transition">
-                                            <td className="px-6 py-4 text-gray-300">
-                                                {mov.date ? new Date(mov.date).toLocaleDateString('ar-EG') : '-'}
-                                            </td>
-                                            <td className="px-6 py-4">
-                                                <span className={`px-2 py-1 rounded text-xs font-bold ${mov.type === 'IN' ? 'bg-green-500/20 text-green-400' : 'bg-red-500/20 text-red-400'
-                                                    }`}>
-                                                    {mov.type === 'IN' ? '╪┤╪▒╪د╪ة / ╪ح╪╢╪د┘╪ر' : '╪د╪│╪ز┘ç┘╪د┘â / ╪╡╪▒┘'}
-                                                </span>
-                                            </td>
-                                            <td className="px-6 py-4 text-white font-mono">{mov.quantity}</td>
-                                            <td className="px-6 py-4 text-gray-300">{mov.price ? `${Number(mov.price).toFixed(2)} ╪ش.┘à` : '-'}</td>
-                                            <td className="px-6 py-4 text-gray-300">{mov.reference}</td>
-                                            <td className="px-6 py-4 text-gray-400 text-sm">{mov.notes || '-'}</td>
-                                            <td className="px-6 py-4 flex gap-2 justify-end">
-                                                <button
-                                                    onClick={() => handleEditMovement(mov)}
-                                                    className="p-1 px-3 bg-yellow-500/10 hover:bg-yellow-500/20 text-yellow-500 rounded text-sm transition"
-                                                >
-                                                    <Pencil />
-                                                </button>
-                                                <button
-                                                    onClick={() => handleDeleteMovement(mov.id)}
-                                                    className="p-1 px-3 bg-red-500/10 hover:bg-red-500/20 text-red-500 rounded text-sm transition"
-                                                >
-                                                    <Trash2 />
-                                                </button>
-                                            </td>
-                                        </tr>
-                                    ))
-                                )}
-                            </tbody>
-                        </table>
-                    </div>
-                </div>
-
-                {/* Edit Dialog */}
-                {showEditDialog && (
-                    <div className="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50">
-                        <div className="bg-slate-800 rounded-2xl p-8 max-w-md w-full mx-4 border border-white/20">
-                            <h2 className="text-xl font-bold text-white mb-6 flex items-center gap-2"><Pencil /> ╪ز╪╣╪»┘è┘ ╪د┘╪│╪ش┘</h2>
-                            <form onSubmit={handleSaveEdit} className="space-y-4">
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪ز╪د╪▒┘è╪«</label>
-                                    <input
-                                        type="date"
-                                        required
-                                        value={editForm.date}
-                                        onChange={(e) => setEditForm({ ...editForm, date: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
-                                    />
-                                </div>
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘â┘à┘è╪ر</label>
-                                    <input
-                                        type="number"
-                                        step="0.01"
-                                        required
-                                        dir="ltr"
-                                        value={editForm.quantity}
-                                        onChange={(e) => setEditForm({ ...editForm, quantity: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
-                                    />
-                                </div>
-                                {editingMovement?.type === 'IN' && (
-                                    <div>
-                                        <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪│╪╣╪▒</label>
-                                        <input
-                                            type="number"
-                                            step="0.01"
-                                            dir="ltr"
-                                            value={editForm.price}
-                                            onChange={(e) => setEditForm({ ...editForm, price: e.target.value })}
-                                            className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
-                                        />
-                                    </div>
-                                )}
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">┘à┘╪د╪ص╪╕╪د╪ز</label>
-                                    <textarea
-                                        value={editForm.notes}
-                                        onChange={(e) => setEditForm({ ...editForm, notes: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
-                                        rows={3}
-                                    />
-                                </div>
-                                <div className="flex gap-4 pt-4">
-                                    <button
-                                        type="submit"
-                                        className="flex-1 px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition"
-                                    >
-                                        ╪ص┘╪╕ ╪د┘╪ز╪╣╪»┘è┘╪د╪ز
-                                    </button>
-                                    <button
-                                        type="button"
-                                        onClick={() => setShowEditDialog(false)}
-                                        className="flex-1 px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white rounded-lg font-semibold transition"
-                                    >
-                                        ╪ح┘╪║╪د╪ة
-                                    </button>
-                                </div>
-                            </form>
-                        </div>
-                    </div>
-                )}
-
-                {/* Add Movement Dialog */}
-                {showAddDialog && (
-                    <div className="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50">
-                        <div className="bg-slate-800 rounded-2xl p-8 max-w-md w-full mx-4 border border-white/20">
-                            <h2 className="text-xl font-bold text-white mb-6 flex items-center gap-2"><Plus /> ╪ح╪╢╪د┘╪ر ╪ص╪▒┘â╪ر</h2>
-                            <form onSubmit={handleAddMovement} className="space-y-4">
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘┘ê╪╣</label>
-                                    <select
-                                        value={addForm.type}
-                                        onChange={(e) => setAddForm({ ...addForm, type: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
-                                    >
-                                        <option value="IN">╪┤╪▒╪د╪ة / ╪ح╪╢╪د┘╪ر</option>
-                                        <option value="OUT">╪د╪│╪ز┘ç┘╪د┘â / ╪╡╪▒┘</option>
-                                    </select>
-                                </div>
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪ز╪د╪▒┘è╪«</label>
-                                    <input
-                                        type="date"
-                                        required
-                                        value={addForm.date}
-                                        onChange={(e) => setAddForm({ ...addForm, date: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
-                                    />
-                                </div>
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘â┘à┘è╪ر</label>
-                                    <input
-                                        type="number"
-                                        step="0.01"
-                                        required
-                                        dir="ltr"
-                                        value={addForm.quantity}
-                                        onChange={(e) => setAddForm({ ...addForm, quantity: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
-                                    />
-                                </div>
-                                {addForm.type === 'IN' && (
-                                    <div>
-                                        <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘╪│╪╣╪▒</label>
-                                        <input
-                                            type="number"
-                                            step="0.01"
-                                            dir="ltr"
-                                            value={addForm.price}
-                                            onChange={(e) => setAddForm({ ...addForm, price: e.target.value })}
-                                            className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
-                                        />
-                                    </div>
-                                )}
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">╪د┘┘à┘ê╪▒╪» / ╪د┘┘à╪▒╪ش╪╣</label>
-                                    <input
-                                        type="text"
-                                        value={addForm.reference}
-                                        onChange={(e) => setAddForm({ ...addForm, reference: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white text-right"
-                                    />
-                                </div>
-                                <div>
-                                    <label className="block text-gray-300 text-sm font-semibold mb-2">┘à┘╪د╪ص╪╕╪د╪ز</label>
-                                    <textarea
-                                        value={addForm.notes}
-                                        onChange={(e) => setAddForm({ ...addForm, notes: e.target.value })}
-                                        className="w-full px-4 py-2 bg-white/5 border border-white/10 rounded-lg text-white"
-                                        rows={3}
-                                    />
-                                </div>
-                                <div className="flex gap-4 pt-4">
-                                    <button
-                                        type="submit"
-                                        className="flex-1 px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg font-semibold transition"
-                                    >
-                                        ╪ح╪╢╪د┘╪ر ╪د┘╪ص╪▒┘â╪ر
-                                    </button>
-                                    <button
-                                        type="button"
-                                        onClick={() => setShowAddDialog(false)}
-                                        className="flex-1 px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white rounded-lg font-semibold transition"
-                                    >
-                                        ╪ح┘╪║╪د╪ة
-                                    </button>
-                                </div>
-                            </form>
-                        </div>
-                    </div>
-                )}
-            </main>
-        </div>
-    );
-}
\ No newline at end of file
diff --git a/frontend/app/manufacturing/traceability/[id]/client.tsx b/frontend/app/manufacturing/traceability/[id]/client.tsx
new file mode 100644
index 0000000..cbdc223
--- /dev/null
+++ b/frontend/app/manufacturing/traceability/[id]/client.tsx
@@ -0,0 +1,242 @@
+'use client';
+
+import { useEffect, useState } from 'react';
+import { useParams, useRouter } from 'next/navigation';
+import { api } from '@/lib/api';
+import { TestTube, Cog, Check, Factory, FileText } from 'lucide-react';
+
+interface BatchComponent {
+    id: number;
+    raw_material?: { id: number; product?: { name: string; unit: string } };
+    accessory?: { id: number; product?: { name: string; unit: string } };
+    supplier_batch_number?: string;
+    quantity_used: number;
+    unit: string;
+    cost_per_unit: number;
+    total_cost: number;
+}
+
+interface Batch {
+    id: number;
+    batch_number: string;
+    product: { id: number; name: string; unit: string };
+    production_date: string;
+    expiry_date?: string;
+    quantity: number;
+    unit: string;
+    status: string;
+    notes?: string;
+    production_id?: number;
+    created_at: string;
+    components: BatchComponent[];
+}
+
+const STATUS_MAP: Record<string, { label: string; color: string }> = {
+    PENDING: { label: '┘à╪╣┘┘é', color: 'bg-yellow-500/20 text-yellow-300 border-yellow-500/30' },
+    RELEASED: { label: '┘à┘╪┤┘ê╪▒', color: 'bg-emerald-500/20 text-emerald-300 border-emerald-500/30' },
+    ON_HOLD: { label: '┘à╪╣┘┘é ┘à╪ج┘é╪ز╪د┘ï', color: 'bg-orange-500/20 text-orange-300 border-orange-500/30' },
+    RECALLED: { label: '┘à╪│╪ص┘ê╪ذ', color: 'bg-red-500/20 text-red-300 border-red-500/30' },
+    EXPIRED: { label: '┘à┘╪ز┘ç┘è ╪د┘╪╡┘╪د╪ص┘è╪ر', color: 'bg-slate-500/20 text-slate-300 border-slate-500/30' },
+};
+
+export default function BatchDetailPage() {
+    const { id } = useParams();
+    const router = useRouter();
+    const [batch, setBatch] = useState<Batch | null>(null);
+    const [loading, setLoading] = useState(true);
+    const [backwardResult, setBackwardResult] = useState<BatchComponent[] | null>(null);
+
+    useEffect(() => {
+        if (!id) return;
+        (async () => {
+            try {
+                const data = await api.getBatch(Number(id));
+                setBatch(data);
+                const trace = await api.backwardTrace(Number(id));
+                setBackwardResult(trace.components || []);
+            } catch (e) {
+                console.error('Error:', e);
+            } finally {
+                setLoading(false);
+            }
+        })();
+    }, [id]);
+
+    const handleStatusChange = async (status: string) => {
+        try {
+            const updated = await api.updateBatchStatus(batch!.id, status);
+            setBatch(updated);
+        } catch (e) {
+            console.error('Status update error:', e);
+        }
+    };
+
+    const handleRecall = async () => {
+        const reason = prompt('╪│╪ذ╪ذ ╪د┘╪│╪ص╪ذ:');
+        try {
+            const updated = await api.recallBatch(batch!.id, reason || undefined);
+            setBatch(updated);
+        } catch (e) {
+            console.error('Recall error:', e);
+        }
+    };
+
+    if (loading) return (
+        <div className="min-h-screen bg-slate-900 flex items-center justify-center">
+            <div className="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-blue-500"></div>
+        </div>
+    );
+
+    if (!batch) return (
+        <div className="min-h-screen bg-slate-900 flex items-center justify-center text-slate-400">
+            ╪د┘╪»┘╪╣╪ر ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»╪ر
+        </div>
+    );
+
+    const statusInfo = STATUS_MAP[batch.status] || { label: batch.status, color: 'bg-white/5 text-slate-300' };
+
+    return (
+        <div className="min-h-screen bg-slate-900 text-white" dir="rtl">
+            <header className="glass border-b border-white/5 bg-slate-900/50 backdrop-blur-lg sticky top-0 z-10">
+                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
+                    <div className="flex items-center gap-3">
+                        <button onClick={() => router.push('/manufacturing/traceability')} className="text-slate-400 hover:text-white transition text-xl">ظْ</button>
+                        <h1 className="text-2xl font-bold font-mono">{batch.batch_number}</h1>
+                        <span className={`px-3 py-1 rounded-full text-xs font-bold border ${statusInfo.color}`}>{statusInfo.label}</span>
+                    </div>
+                    <div className="flex gap-3">
+                        {batch.status === 'PENDING' && (
+                            <button onClick={() => handleStatusChange('RELEASED')} className="px-4 py-2 bg-emerald-600 hover:bg-emerald-500 rounded-xl transition text-sm font-bold">
+                                ┘╪┤╪▒ ╪د┘╪»┘╪╣╪ر
+                            </button>
+                        )}
+                        {(batch.status === 'RELEASED' || batch.status === 'ON_HOLD') && (
+                            <button onClick={handleRecall} className="px-4 py-2 bg-red-600 hover:bg-red-500 rounded-xl transition text-sm font-bold">
+                                ╪│╪ص╪ذ ╪د┘╪»┘╪╣╪ر
+                            </button>
+                        )}
+                        {batch.status === 'PENDING' && (
+                            <button onClick={() => handleStatusChange('ON_HOLD')} className="px-4 py-2 bg-orange-600 hover:bg-orange-500 rounded-xl transition text-sm font-bold">
+                                ╪ز╪╣┘┘è┘é
+                            </button>
+                        )}
+                    </div>
+                </div>
+            </header>
+
+            <main className="container mx-auto px-6 py-8">
+                <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
+                    <div className="lg:col-span-2 space-y-6">
+                        <div className="glass p-6 rounded-2xl border border-white/5">
+                            <h2 className="text-lg font-bold mb-4">┘à╪╣┘┘ê┘à╪د╪ز ╪د┘╪»┘╪╣╪ر</h2>
+                            <div className="grid grid-cols-2 gap-4 text-sm">
+                                <div>
+                                    <span className="text-slate-400">╪د┘┘à┘╪ز╪ش:</span>
+                                    <p className="font-bold">{batch.product?.name || 'ظ¤'}</p>
+                                </div>
+                                <div>
+                                    <span className="text-slate-400">╪د┘┘â┘à┘è╪ر:</span>
+                                    <p className="font-bold">{Number(batch.quantity).toLocaleString()} {batch.unit}</p>
+                                </div>
+                                <div>
+                                    <span className="text-slate-400">╪ز╪د╪▒┘è╪« ╪د┘╪ح┘╪ز╪د╪ش:</span>
+                                    <p>{new Date(batch.production_date).toLocaleDateString('ar-EG')}</p>
+                                </div>
+                                <div>
+                                    <span className="text-slate-400">╪ز╪د╪▒┘è╪« ╪د┘╪د┘╪ز┘ç╪د╪ة:</span>
+                                    <p className={batch.expiry_date && new Date(batch.expiry_date) < new Date() ? 'text-red-400' : ''}>
+                                        {batch.expiry_date ? new Date(batch.expiry_date).toLocaleDateString('ar-EG') : '╪║┘è╪▒ ┘à╪ص╪»╪»'}
+                                    </p>
+                                </div>
+                                {batch.notes && (
+                                    <div className="col-span-2">
+                                        <span className="text-slate-400">┘à┘╪د╪ص╪╕╪د╪ز:</span>
+                                        <p className="mt-1 text-slate-300">{batch.notes}</p>
+                                    </div>
+                                )}
+                            </div>
+                        </div>
+
+                        <div className="glass p-6 rounded-2xl border border-white/5">
+                            <h2 className="text-lg font-bold mb-4 flex items-center gap-2">
+                                <span className="w-1 h-5 bg-purple-500 rounded-full"></span>
+                                ╪د┘┘à┘â┘ê┘╪د╪ز ╪د┘┘à╪│╪ز╪«╪»┘à╪ر (╪ز╪ز╪ذ╪╣ ╪╣┘â╪│┘è)
+                            </h2>
+                            {!backwardResult || backwardResult.length === 0 ? (
+                                <p className="text-slate-500 text-sm">┘╪د ╪ز┘ê╪ش╪» ┘à┘â┘ê┘╪د╪ز ┘à╪│╪ش┘╪ر ┘┘ç╪░┘ç ╪د┘╪»┘╪╣╪ر</p>
+                            ) : (
+                                <div className="space-y-3">
+                                    {backwardResult.map((c) => (
+                                        <div key={c.id} className="flex items-center justify-between p-4 bg-white/5 rounded-xl border border-white/5">
+                                            <div className="flex items-center gap-3">
+                                                <span className="text-xl">{c.raw_material ? <TestTube /> : <Cog />}</span>
+                                                <div>
+                                                    <p className="font-bold text-sm">
+                                                        {c.raw_material?.product?.name || c.accessory?.product?.name || '┘à┘â┘ê┘'}
+                                                    </p>
+                                                    {c.supplier_batch_number && (
+                                                        <p className="text-xs text-purple-300 font-mono">
+                                                            Batch ╪د┘┘à┘ê╪▒╪»: {c.supplier_batch_number}
+                                                        </p>
+                                                    )}
+                                                </div>
+                                            </div>
+                                            <div className="text-left">
+                                                <p className="font-bold">{Number(c.quantity_used).toLocaleString()} {c.unit}</p>
+                                                <p className="text-xs text-slate-500">{Number(c.total_cost).toLocaleString()} ╪ش.┘à</p>
+                                            </div>
+                                        </div>
+                                    ))}
+                                </div>
+                            )}
+                        </div>
+                    </div>
+
+                    <div className="space-y-6">
+                        <div className="glass p-6 rounded-2xl border border-white/5">
+                            <h3 className="font-bold mb-4">╪│╪ش┘ ╪د┘╪ص╪د┘╪ر</h3>
+                            <div className="space-y-4">
+                                {['PENDING', 'RELEASED', 'ON_HOLD', 'RECALLED', 'EXPIRED'].map((s) => (
+                                    <button
+                                        key={s}
+                                        onClick={() => handleStatusChange(s)}
+                                        disabled={batch.status === s}
+                                        className={`w-full text-right px-4 py-3 rounded-xl text-sm transition border ${
+                                            batch.status === s
+                                                ? `${STATUS_MAP[s]?.color || 'bg-white/10'} border-current`
+                                                : 'bg-white/5 border-white/5 hover:border-white/20'
+                                        }`}
+                                    >
+                                        <span className="font-bold">{STATUS_MAP[s]?.label || s}</span>
+                                        {batch.status === s && <span className="mr-2 text-xs"><Check /></span>}
+                                    </button>
+                                ))}
+                            </div>
+                        </div>
+
+                        <div className="glass p-6 rounded-2xl border border-white/5">
+                            <h3 className="font-bold mb-4">╪ح╪ش╪▒╪د╪ة╪د╪ز</h3>
+                            <div className="space-y-3">
+                                <button
+                                    onClick={() => router.push(`/manufacturing/daily-production/${batch.production_id || ''}`)}
+                                    className="w-full px-4 py-3 bg-white/5 hover:bg-white/10 rounded-xl text-sm transition text-right"
+                                >
+                                    <Factory /> ╪╣╪▒╪╢ ╪د┘╪ح┘╪ز╪د╪ش ╪د┘┘à╪▒╪ز╪ذ╪╖
+                                </button>
+                                <button
+                                    onClick={() => {
+                                        navigator.clipboard.writeText(batch.batch_number);
+                                        alert('╪ز┘à ┘╪│╪« ╪▒┘é┘à ╪د┘╪»┘╪╣╪ر');
+                                    }}
+                                    className="w-full px-4 py-3 bg-white/5 hover:bg-white/10 rounded-xl text-sm transition text-right"
+                                >
+                                    <FileText /> ┘╪│╪« ╪▒┘é┘à ╪د┘╪»┘╪╣╪ر
+                                </button>
+                            </div>
+                        </div>
+                    </div>
+                </div>
+            </main>
+        </div>
+    );
+}
diff --git a/frontend/app/manufacturing/traceability/[id]/page.tsx b/frontend/app/manufacturing/traceability/[id]/page.tsx
index 8718c67..0645d31 100644
--- a/frontend/app/manufacturing/traceability/[id]/page.tsx
+++ b/frontend/app/manufacturing/traceability/[id]/page.tsx
@@ -1,245 +1,9 @@
-'use client';
+import TraceabilityClientPage from './client';
 
-import { useEffect, useState } from 'react';
-import { useParams, useRouter } from 'next/navigation';
-import { api } from '@/lib/api';
-import { TestTube, Cog, Check, Factory, FileText } from 'lucide-react';
-
-interface BatchComponent {
-    id: number;
-    raw_material?: { id: number; product?: { name: string; unit: string } };
-    accessory?: { id: number; product?: { name: string; unit: string } };
-    supplier_batch_number?: string;
-    quantity_used: number;
-    unit: string;
-    cost_per_unit: number;
-    total_cost: number;
-}
-
-interface Batch {
-    id: number;
-    batch_number: string;
-    product: { id: number; name: string; unit: string };
-    production_date: string;
-    expiry_date?: string;
-    quantity: number;
-    unit: string;
-    status: string;
-    notes?: string;
-    production_id?: number;
-    created_at: string;
-    components: BatchComponent[];
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-const STATUS_MAP: Record<string, { label: string; color: string }> = {
-    PENDING: { label: '┘à╪╣┘┘é', color: 'bg-yellow-500/20 text-yellow-300 border-yellow-500/30' },
-    RELEASED: { label: '┘à┘╪┤┘ê╪▒', color: 'bg-emerald-500/20 text-emerald-300 border-emerald-500/30' },
-    ON_HOLD: { label: '┘à╪╣┘┘é ┘à╪ج┘é╪ز╪د┘ï', color: 'bg-orange-500/20 text-orange-300 border-orange-500/30' },
-    RECALLED: { label: '┘à╪│╪ص┘ê╪ذ', color: 'bg-red-500/20 text-red-300 border-red-500/30' },
-    EXPIRED: { label: '┘à┘╪ز┘ç┘è ╪د┘╪╡┘╪د╪ص┘è╪ر', color: 'bg-slate-500/20 text-slate-300 border-slate-500/30' },
-};
-
-export default function BatchDetailPage() {
-    const { id } = useParams();
-    const router = useRouter();
-    const [batch, setBatch] = useState<Batch | null>(null);
-    const [loading, setLoading] = useState(true);
-    const [backwardResult, setBackwardResult] = useState<BatchComponent[] | null>(null);
-
-    useEffect(() => {
-        if (!id) return;
-        (async () => {
-            try {
-                const data = await api.getBatch(Number(id));
-                setBatch(data);
-                const trace = await api.backwardTrace(Number(id));
-                setBackwardResult(trace.components || []);
-            } catch (e) {
-                console.error('Error:', e);
-            } finally {
-                setLoading(false);
-            }
-        })();
-    }, [id]);
-
-    const handleStatusChange = async (status: string) => {
-        try {
-            const updated = await api.updateBatchStatus(batch!.id, status);
-            setBatch(updated);
-        } catch (e) {
-            console.error('Status update error:', e);
-        }
-    };
-
-    const handleRecall = async () => {
-        const reason = prompt('╪│╪ذ╪ذ ╪د┘╪│╪ص╪ذ:');
-        try {
-            const updated = await api.recallBatch(batch!.id, reason || undefined);
-            setBatch(updated);
-        } catch (e) {
-            console.error('Recall error:', e);
-        }
-    };
-
-    if (loading) return (
-        <div className="min-h-screen bg-slate-900 flex items-center justify-center">
-            <div className="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-blue-500"></div>
-        </div>
-    );
-
-    if (!batch) return (
-        <div className="min-h-screen bg-slate-900 flex items-center justify-center text-slate-400">
-            ╪د┘╪»┘╪╣╪ر ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»╪ر
-        </div>
-    );
-
-    const statusInfo = STATUS_MAP[batch.status] || { label: batch.status, color: 'bg-white/5 text-slate-300' };
-
-    return (
-        <div className="min-h-screen bg-slate-900 text-white" dir="rtl">
-            <header className="glass border-b border-white/5 bg-slate-900/50 backdrop-blur-lg sticky top-0 z-10">
-                <div className="container mx-auto px-6 py-4 flex justify-between items-center">
-                    <div className="flex items-center gap-3">
-                        <button onClick={() => router.push('/manufacturing/traceability')} className="text-slate-400 hover:text-white transition text-xl">ظْ</button>
-                        <h1 className="text-2xl font-bold font-mono">{batch.batch_number}</h1>
-                        <span className={`px-3 py-1 rounded-full text-xs font-bold border ${statusInfo.color}`}>{statusInfo.label}</span>
-                    </div>
-                    <div className="flex gap-3">
-                        {batch.status === 'PENDING' && (
-                            <button onClick={() => handleStatusChange('RELEASED')} className="px-4 py-2 bg-emerald-600 hover:bg-emerald-500 rounded-xl transition text-sm font-bold">
-                                ┘╪┤╪▒ ╪د┘╪»┘╪╣╪ر
-                            </button>
-                        )}
-                        {(batch.status === 'RELEASED' || batch.status === 'ON_HOLD') && (
-                            <button onClick={handleRecall} className="px-4 py-2 bg-red-600 hover:bg-red-500 rounded-xl transition text-sm font-bold">
-                                ╪│╪ص╪ذ ╪د┘╪»┘╪╣╪ر
-                            </button>
-                        )}
-                        {batch.status === 'PENDING' && (
-                            <button onClick={() => handleStatusChange('ON_HOLD')} className="px-4 py-2 bg-orange-600 hover:bg-orange-500 rounded-xl transition text-sm font-bold">
-                                ╪ز╪╣┘┘è┘é
-                            </button>
-                        )}
-                    </div>
-                </div>
-            </header>
-
-            <main className="container mx-auto px-6 py-8">
-                <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
-                    {/* Left: Info */}
-                    <div className="lg:col-span-2 space-y-6">
-                        <div className="glass p-6 rounded-2xl border border-white/5">
-                            <h2 className="text-lg font-bold mb-4">┘à╪╣┘┘ê┘à╪د╪ز ╪د┘╪»┘╪╣╪ر</h2>
-                            <div className="grid grid-cols-2 gap-4 text-sm">
-                                <div>
-                                    <span className="text-slate-400">╪د┘┘à┘╪ز╪ش:</span>
-                                    <p className="font-bold">{batch.product?.name || 'ظ¤'}</p>
-                                </div>
-                                <div>
-                                    <span className="text-slate-400">╪د┘┘â┘à┘è╪ر:</span>
-                                    <p className="font-bold">{Number(batch.quantity).toLocaleString()} {batch.unit}</p>
-                                </div>
-                                <div>
-                                    <span className="text-slate-400">╪ز╪د╪▒┘è╪« ╪د┘╪ح┘╪ز╪د╪ش:</span>
-                                    <p>{new Date(batch.production_date).toLocaleDateString('ar-EG')}</p>
-                                </div>
-                                <div>
-                                    <span className="text-slate-400">╪ز╪د╪▒┘è╪« ╪د┘╪د┘╪ز┘ç╪د╪ة:</span>
-                                    <p className={batch.expiry_date && new Date(batch.expiry_date) < new Date() ? 'text-red-400' : ''}>
-                                        {batch.expiry_date ? new Date(batch.expiry_date).toLocaleDateString('ar-EG') : '╪║┘è╪▒ ┘à╪ص╪»╪»'}
-                                    </p>
-                                </div>
-                                {batch.notes && (
-                                    <div className="col-span-2">
-                                        <span className="text-slate-400">┘à┘╪د╪ص╪╕╪د╪ز:</span>
-                                        <p className="mt-1 text-slate-300">{batch.notes}</p>
-                                    </div>
-                                )}
-                            </div>
-                        </div>
-
-                        {/* Components (Backward Trace) */}
-                        <div className="glass p-6 rounded-2xl border border-white/5">
-                            <h2 className="text-lg font-bold mb-4 flex items-center gap-2">
-                                <span className="w-1 h-5 bg-purple-500 rounded-full"></span>
-                                ╪د┘┘à┘â┘ê┘╪د╪ز ╪د┘┘à╪│╪ز╪«╪»┘à╪ر (╪ز╪ز╪ذ╪╣ ╪╣┘â╪│┘è)
-                            </h2>
-                            {!backwardResult || backwardResult.length === 0 ? (
-                                <p className="text-slate-500 text-sm">┘╪د ╪ز┘ê╪ش╪» ┘à┘â┘ê┘╪د╪ز ┘à╪│╪ش┘╪ر ┘┘ç╪░┘ç ╪د┘╪»┘╪╣╪ر</p>
-                            ) : (
-                                <div className="space-y-3">
-                                    {backwardResult.map((c) => (
-                                        <div key={c.id} className="flex items-center justify-between p-4 bg-white/5 rounded-xl border border-white/5">
-                                            <div className="flex items-center gap-3">
-                                                <span className="text-xl">{c.raw_material ? <TestTube /> : <Cog />}</span>
-                                                <div>
-                                                    <p className="font-bold text-sm">
-                                                        {c.raw_material?.product?.name || c.accessory?.product?.name || '┘à┘â┘ê┘'}
-                                                    </p>
-                                                    {c.supplier_batch_number && (
-                                                        <p className="text-xs text-purple-300 font-mono">
-                                                            Batch ╪د┘┘à┘ê╪▒╪»: {c.supplier_batch_number}
-                                                        </p>
-                                                    )}
-                                                </div>
-                                            </div>
-                                            <div className="text-left">
-                                                <p className="font-bold">{Number(c.quantity_used).toLocaleString()} {c.unit}</p>
-                                                <p className="text-xs text-slate-500">{Number(c.total_cost).toLocaleString()} ╪ش.┘à</p>
-                                            </div>
-                                        </div>
-                                    ))}
-                                </div>
-                            )}
-                        </div>
-                    </div>
-
-                    {/* Right: Actions & Timeline */}
-                    <div className="space-y-6">
-                        <div className="glass p-6 rounded-2xl border border-white/5">
-                            <h3 className="font-bold mb-4">╪│╪ش┘ ╪د┘╪ص╪د┘╪ر</h3>
-                            <div className="space-y-4">
-                                {['PENDING', 'RELEASED', 'ON_HOLD', 'RECALLED', 'EXPIRED'].map((s) => (
-                                    <button
-                                        key={s}
-                                        onClick={() => handleStatusChange(s)}
-                                        disabled={batch.status === s}
-                                        className={`w-full text-right px-4 py-3 rounded-xl text-sm transition border ${
-                                            batch.status === s
-                                                ? `${STATUS_MAP[s]?.color || 'bg-white/10'} border-current`
-                                                : 'bg-white/5 border-white/5 hover:border-white/20'
-                                        }`}
-                                    >
-                                        <span className="font-bold">{STATUS_MAP[s]?.label || s}</span>
-                                        {batch.status === s && <span className="mr-2 text-xs"><Check /></span>}
-                                    </button>
-                                ))}
-                            </div>
-                        </div>
-
-                        <div className="glass p-6 rounded-2xl border border-white/5">
-                            <h3 className="font-bold mb-4">╪ح╪ش╪▒╪د╪ة╪د╪ز</h3>
-                            <div className="space-y-3">
-                                <button
-                                    onClick={() => router.push(`/manufacturing/daily-production/${batch.production_id || ''}`)}
-                                    className="w-full px-4 py-3 bg-white/5 hover:bg-white/10 rounded-xl text-sm transition text-right"
-                                >
-                                    <Factory /> ╪╣╪▒╪╢ ╪د┘╪ح┘╪ز╪د╪ش ╪د┘┘à╪▒╪ز╪ذ╪╖
-                                </button>
-                                <button
-                                    onClick={() => {
-                                        navigator.clipboard.writeText(batch.batch_number);
-                                        alert('╪ز┘à ┘╪│╪« ╪▒┘é┘à ╪د┘╪»┘╪╣╪ر');
-                                    }}
-                                    className="w-full px-4 py-3 bg-white/5 hover:bg-white/10 rounded-xl text-sm transition text-right"
-                                >
-                                    <FileText /> ┘╪│╪« ╪▒┘é┘à ╪د┘╪»┘╪╣╪ر
-                                </button>
-                            </div>
-                        </div>
-                    </div>
-                </div>
-            </main>
-        </div>
-    );
+export default function Page() {
+  return <TraceabilityClientPage />;
 }
diff --git a/frontend/app/purchases/suppliers/statement/[id]/client.tsx b/frontend/app/purchases/suppliers/statement/[id]/client.tsx
new file mode 100644
index 0000000..b0c6182
--- /dev/null
+++ b/frontend/app/purchases/suppliers/statement/[id]/client.tsx
@@ -0,0 +1,145 @@
+'use client';
+
+import { Printer } from 'lucide-react';
+import { api } from '@/lib/api';
+import { useParams } from 'next/navigation';
+import { useState, useEffect, useCallback } from 'react';
+
+interface Supplier {
+    id: number;
+    name: string;
+    phone?: string;
+    balance: number | string;
+}
+
+interface Transaction {
+    id: number;
+    type: 'INVOICE' | 'OPENING_BALANCE' | 'PAYMENT' | 'RETURN';
+    amount: number | string;
+    date: string;
+    description?: string;
+}
+
+export default function SupplierStatementPage() {
+    const { id } = useParams();
+    const [supplier, setSupplier] = useState<Supplier | null>(null);
+    const [transactions, setTransactions] = useState<Transaction[]>([]);
+    const [loading, setLoading] = useState(true);
+
+    const loadData = useCallback(async () => {
+        try {
+            const suppliers = await api.fetchWithAuth('/purchases/suppliers');
+            const supp = suppliers.find((s: Supplier) => s.id === Number(id));
+            setSupplier(supp);
+
+            if (supp) {
+                const trans = await api.fetchWithAuth(`/purchases/suppliers/${id}/statement`);
+                setTransactions((trans || []).map((t: any) => ({
+                  id: t.ref || t.id,
+                  type: t.type === 'ORDER' ? 'INVOICE' : t.type,
+                  amount: Number(t.debit || t.credit),
+                  date: t.date,
+                  description: t.description || '',
+                })));
+            }
+        } catch (error) {
+            console.error('Error loading data', error);
+        } finally {
+            setLoading(false);
+        }
+    }, [id]);
+
+    useEffect(() => {
+        loadData();
+    }, [loadData]);
+
+    if (loading) return <div>╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
+    if (!supplier) return <div>╪د┘┘à┘ê╪▒╪» ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>;
+
+    const totalPurchases = transactions
+        .filter(t => t.type === 'INVOICE' || t.type === 'OPENING_BALANCE')
+        .reduce((sum, t) => sum + Number(t.amount), 0);
+
+    const totalPayments = transactions
+        .filter(t => t.type === 'PAYMENT' || t.type === 'RETURN')
+        .reduce((sum, t) => sum + Number(t.amount), 0);
+
+    const balance = totalPurchases - totalPayments;
+
+    return (
+        <div className="min-h-screen bg-white text-black p-8 font-serif" dir="rtl">
+            <div className="flex justify-between items-center border-b-2 border-black pb-4 mb-6">
+                <div>
+                    <h1 className="text-3xl font-bold mb-2">┘â╪┤┘ ╪ص╪│╪د╪ذ ┘à┘ê╪▒╪»</h1>
+                    <p className="text-sm">╪د┘╪ز╪د╪▒┘è╪«: {new Date().toLocaleDateString('ar-EG')}</p>
+                </div>
+                <div className="text-left">
+                    <h2 className="text-xl font-bold">╪د┘┘à╪╡┘╪╣ ╪د┘╪ص╪»┘è╪س ┘┘╪ذ┘╪د╪│╪ز┘è┘â</h2>
+                    <p>╪د┘╪╣┘┘ê╪د┘: ╪د┘┘à┘╪╖┘é╪ر ╪د┘╪╡┘╪د╪╣┘è╪ر</p>
+                </div>
+            </div>
+
+            <div className="bg-gray-100 p-4 rounded mb-6 border border-gray-300">
+                <div className="grid grid-cols-2 gap-4">
+                    <div>
+                        <span className="font-bold">╪د╪│┘à ╪د┘┘à┘ê╪▒╪»:</span> {supplier.name}
+                    </div>
+                    <div>
+                        <span className="font-bold">╪▒┘é┘à ╪د┘┘ç╪د╪ز┘:</span> {supplier.phone}
+                    </div>
+                </div>
+            </div>
+
+            <table className="w-full border-collapse border border-black mb-6 text-sm">
+                <thead>
+                    <tr className="bg-gray-200">
+                        <th className="border border-black p-2">╪د┘╪ز╪د╪▒┘è╪«</th>
+                        <th className="border border-black p-2">╪د┘╪╣┘à┘┘è╪ر</th>
+                        <th className="border border-black p-2">╪د┘┘ê╪╡┘</th>
+                        <th className="border border-black p-2">┘à╪┤╪ز╪▒┘è╪د╪ز (╪╣┘┘è┘╪د)</th>
+                        <th className="border border-black p-2">╪│╪»╪د╪» (┘à┘╪د)</th>
+                        <th className="border border-black p-2">╪د┘╪▒╪╡┘è╪»</th>
+                    </tr>
+                </thead>
+                <tbody>
+                    {transactions.map((t, index) => {
+                        const credit = (t.type === 'INVOICE' || t.type === 'OPENING_BALANCE') ? Number(t.amount) : 0;
+                        const debit = (t.type === 'PAYMENT' || t.type === 'RETURN') ? Number(t.amount) : 0;
+
+                        return (
+                            <tr key={index}>
+                                <td className="border border-black p-2">{new Date(t.date).toLocaleDateString('ar-EG')}</td>
+                                <td className="border border-black p-2">
+                                    {t.type === 'INVOICE' ? '┘╪د╪ز┘ê╪▒╪ر ╪┤╪▒╪د╪ة' :
+                                        t.type === 'PAYMENT' ? '╪│╪»╪د╪» ┘┘é╪»┘è╪ر' :
+                                            t.type === 'RETURN' ? '┘à╪▒╪ز╪ش╪╣' : '╪▒╪╡┘è╪» ╪د┘╪ز╪ز╪د╪ص┘è'}
+                                </td>
+                                <td className="border border-black p-2">{t.description || '-'}</td>
+                                <td className="border border-black p-2">{credit > 0 ? credit.toLocaleString() : '-'}</td>
+                                <td className="border border-black p-2">{debit > 0 ? debit.toLocaleString() : '-'}</td>
+                                <td className="border border-black p-2 bg-gray-50"></td>
+                            </tr>
+                        );
+                    })}
+                </tbody>
+                <tfoot>
+                    <tr className="bg-gray-300 font-bold">
+                        <td className="border border-black p-2" colSpan={3}>╪د┘╪ح╪ش┘à╪د┘┘è</td>
+                        <td className="border border-black p-2">{totalPurchases.toLocaleString()}</td>
+                        <td className="border border-black p-2">{totalPayments.toLocaleString()}</td>
+                        <td className="border border-black p-2">{balance.toLocaleString()}</td>
+                    </tr>
+                </tfoot>
+            </table>
+
+            <div className="fixed bottom-8 left-8 print:hidden">
+                <button
+                    onClick={() => window.print()}
+                    className="bg-purple-600 text-white px-6 py-3 rounded-full shadow-lg hover:bg-purple-700 font-bold flex items-center gap-2"
+                >
+                    <Printer className="w-5 h-5" /> ╪╖╪ذ╪د╪╣╪ر
+                </button>
+            </div>
+        </div>
+    );
+}
diff --git a/frontend/app/purchases/suppliers/statement/[id]/page.tsx b/frontend/app/purchases/suppliers/statement/[id]/page.tsx
index 8c15cbb..76cf170 100644
--- a/frontend/app/purchases/suppliers/statement/[id]/page.tsx
+++ b/frontend/app/purchases/suppliers/statement/[id]/page.tsx
@@ -1,154 +1,9 @@
-'use client';
+import SupplierStatementClientPage from './client';
 
-import { Printer } from 'lucide-react';
-import { api } from '@/lib/api';
-import { useParams } from 'next/navigation';
-import { useState, useEffect, useCallback } from 'react';
-
-interface Supplier {
-    id: number;
-    name: string;
-    phone?: string;
-    balance: number | string;
-}
-
-interface Transaction {
-    id: number;
-    type: 'INVOICE' | 'OPENING_BALANCE' | 'PAYMENT' | 'RETURN';
-    amount: number | string;
-    date: string;
-    description?: string;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function SupplierStatementPage() {
-    const { id } = useParams();
-    const [supplier, setSupplier] = useState<Supplier | null>(null);
-    const [transactions, setTransactions] = useState<Transaction[]>([]);
-    const [loading, setLoading] = useState(true);
-
-    const loadData = useCallback(async () => {
-        try {
-            const suppliers = await api.fetchWithAuth('/purchases/suppliers');
-            const supp = suppliers.find((s: Supplier) => s.id === Number(id));
-            setSupplier(supp);
-
-            if (supp) {
-                const trans = await api.fetchWithAuth(`/purchases/suppliers/${id}/statement`);
-                setTransactions((trans || []).map((t: any) => ({
-                  id: t.ref || t.id,
-                  type: t.type === 'ORDER' ? 'INVOICE' : t.type,
-                  amount: Number(t.debit || t.credit),
-                  date: t.date,
-                  description: t.description || '',
-                })));
-            }
-        } catch (error) {
-            console.error('Error loading data', error);
-        } finally {
-            setLoading(false);
-        }
-    }, [id]);
-
-    useEffect(() => {
-        loadData();
-    }, [loadData]);
-
-    if (loading) return <div>╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
-    if (!supplier) return <div>╪د┘┘à┘ê╪▒╪» ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>;
-
-    // For suppliers: Invoice = Credit (we owe), Payment = Debit (we paid)
-    // Actually, usually:
-    // Invoice -> Increases Balance (Credit)
-    // Payment -> Decreases Balance (Debit)
-
-    const totalPurchases = transactions
-        .filter(t => t.type === 'INVOICE' || t.type === 'OPENING_BALANCE')
-        .reduce((sum, t) => sum + Number(t.amount), 0);
-
-    const totalPayments = transactions
-        .filter(t => t.type === 'PAYMENT' || t.type === 'RETURN')
-        .reduce((sum, t) => sum + Number(t.amount), 0);
-
-    const balance = totalPurchases - totalPayments;
-
-    return (
-        <div className="min-h-screen bg-white text-black p-8 font-serif" dir="rtl">
-            {/* Header */}
-            <div className="flex justify-between items-center border-b-2 border-black pb-4 mb-6">
-                <div>
-                    <h1 className="text-3xl font-bold mb-2">┘â╪┤┘ ╪ص╪│╪د╪ذ ┘à┘ê╪▒╪»</h1>
-                    <p className="text-sm">╪د┘╪ز╪د╪▒┘è╪«: {new Date().toLocaleDateString('ar-EG')}</p>
-                </div>
-                <div className="text-left">
-                    <h2 className="text-xl font-bold">╪د┘┘à╪╡┘╪╣ ╪د┘╪ص╪»┘è╪س ┘┘╪ذ┘╪د╪│╪ز┘è┘â</h2>
-                    <p>╪د┘╪╣┘┘ê╪د┘: ╪د┘┘à┘╪╖┘é╪ر ╪د┘╪╡┘╪د╪╣┘è╪ر</p>
-                </div>
-            </div>
-
-            {/* Supplier Info */}
-            <div className="bg-gray-100 p-4 rounded mb-6 border border-gray-300">
-                <div className="grid grid-cols-2 gap-4">
-                    <div>
-                        <span className="font-bold">╪د╪│┘à ╪د┘┘à┘ê╪▒╪»:</span> {supplier.name}
-                    </div>
-                    <div>
-                        <span className="font-bold">╪▒┘é┘à ╪د┘┘ç╪د╪ز┘:</span> {supplier.phone}
-                    </div>
-                </div>
-            </div>
-
-            {/* Transactions Table */}
-            <table className="w-full border-collapse border border-black mb-6 text-sm">
-                <thead>
-                    <tr className="bg-gray-200">
-                        <th className="border border-black p-2">╪د┘╪ز╪د╪▒┘è╪«</th>
-                        <th className="border border-black p-2">╪د┘╪╣┘à┘┘è╪ر</th>
-                        <th className="border border-black p-2">╪د┘┘ê╪╡┘</th>
-                        <th className="border border-black p-2">┘à╪┤╪ز╪▒┘è╪د╪ز (╪╣┘┘è┘╪د)</th>
-                        <th className="border border-black p-2">╪│╪»╪د╪» (┘à┘╪د)</th>
-                        <th className="border border-black p-2">╪د┘╪▒╪╡┘è╪»</th>
-                    </tr>
-                </thead>
-                <tbody>
-                    {transactions.map((t, index) => {
-                        const credit = (t.type === 'INVOICE' || t.type === 'OPENING_BALANCE') ? Number(t.amount) : 0;
-                        const debit = (t.type === 'PAYMENT' || t.type === 'RETURN') ? Number(t.amount) : 0;
-
-                        return (
-                            <tr key={index}>
-                                <td className="border border-black p-2">{new Date(t.date).toLocaleDateString('ar-EG')}</td>
-                                <td className="border border-black p-2">
-                                    {t.type === 'INVOICE' ? '┘╪د╪ز┘ê╪▒╪ر ╪┤╪▒╪د╪ة' :
-                                        t.type === 'PAYMENT' ? '╪│╪»╪د╪» ┘┘é╪»┘è╪ر' :
-                                            t.type === 'RETURN' ? '┘à╪▒╪ز╪ش╪╣' : '╪▒╪╡┘è╪» ╪د┘╪ز╪ز╪د╪ص┘è'}
-                                </td>
-                                <td className="border border-black p-2">{t.description || '-'}</td>
-                                <td className="border border-black p-2">{credit > 0 ? credit.toLocaleString() : '-'}</td>
-                                <td className="border border-black p-2">{debit > 0 ? debit.toLocaleString() : '-'}</td>
-                                <td className="border border-black p-2 bg-gray-50"></td>
-                            </tr>
-                        );
-                    })}
-                </tbody>
-                <tfoot>
-                    <tr className="bg-gray-300 font-bold">
-                        <td className="border border-black p-2" colSpan={3}>╪د┘╪ح╪ش┘à╪د┘┘è</td>
-                        <td className="border border-black p-2">{totalPurchases.toLocaleString()}</td>
-                        <td className="border border-black p-2">{totalPayments.toLocaleString()}</td>
-                        <td className="border border-black p-2">{balance.toLocaleString()}</td>
-                    </tr>
-                </tfoot>
-            </table>
-
-            {/* Print Button */}
-            <div className="fixed bottom-8 left-8 print:hidden">
-                <button
-                    onClick={() => window.print()}
-                    className="bg-purple-600 text-white px-6 py-3 rounded-full shadow-lg hover:bg-purple-700 font-bold flex items-center gap-2"
-                >
-                    <Printer className="w-5 h-5" /> ╪╖╪ذ╪د╪╣╪ر
-                </button>
-            </div>
-        </div>
-    );
+export default function Page() {
+  return <SupplierStatementClientPage />;
 }
diff --git a/frontend/app/sales/customers/statement/[id]/client.tsx b/frontend/app/sales/customers/statement/[id]/client.tsx
new file mode 100644
index 0000000..5248c06
--- /dev/null
+++ b/frontend/app/sales/customers/statement/[id]/client.tsx
@@ -0,0 +1,161 @@
+'use client';
+
+import { Printer } from 'lucide-react';
+import { api } from '@/lib/api';
+import { useParams } from 'next/navigation';
+import { useState, useEffect, useCallback } from 'react';
+
+interface Customer {
+    id: number;
+    name: string;
+    phone?: string;
+    address?: string;
+    balance: number | string;
+}
+
+interface Transaction {
+    id: number;
+    type: 'INVOICE' | 'OPENING_BALANCE' | 'PAYMENT' | 'RETURN';
+    amount: number | string;
+    date: string;
+    description?: string;
+}
+
+export default function CustomerStatementPage() {
+    const { id } = useParams();
+    const [customer, setCustomer] = useState<Customer | null>(null);
+    const [transactions, setTransactions] = useState<Transaction[]>([]);
+    const [loading, setLoading] = useState(true);
+
+    const loadData = useCallback(async () => {
+        try {
+            const customers = await api.fetchWithAuth('/sales/customers');
+            const cust = customers.find((c: Customer) => c.id === Number(id));
+            setCustomer(cust);
+
+            if (cust) {
+                const trans = await api.fetchWithAuth(`/sales/customers/${id}/statement`);
+                setTransactions((trans || []).map((t: any) => ({
+                  id: t.ref || t.id,
+                  type: t.type === 'ORDER' ? 'INVOICE' : t.type,
+                  amount: Number(t.debit || t.credit),
+                  date: t.date,
+                  description: t.description || '',
+                })));
+            }
+        } catch (error) {
+            console.error('Error loading data', error);
+        } finally {
+            setLoading(false);
+        }
+    }, [id]);
+
+    useEffect(() => {
+        loadData();
+    }, [loadData]);
+
+    if (loading) return <div>╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
+    if (!customer) return <div>╪د┘╪╣┘à┘è┘ ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>;
+
+    const totalDebit = transactions
+        .filter(t => t.type === 'INVOICE' || t.type === 'OPENING_BALANCE')
+        .reduce((sum, t) => sum + Number(t.amount), 0);
+
+    const totalCredit = transactions
+        .filter(t => t.type === 'PAYMENT' || t.type === 'RETURN')
+        .reduce((sum, t) => sum + Number(t.amount), 0);
+
+    const balance = totalDebit - totalCredit;
+
+    return (
+        <div className="min-h-screen bg-white text-black p-8 font-serif" dir="rtl">
+            <div className="flex justify-between items-center border-b-2 border-black pb-4 mb-6">
+                <div>
+                    <h1 className="text-3xl font-bold mb-2">┘â╪┤┘ ╪ص╪│╪د╪ذ ╪╣┘à┘è┘</h1>
+                    <p className="text-sm">╪د┘╪ز╪د╪▒┘è╪«: {new Date().toLocaleDateString('ar-EG')}</p>
+                </div>
+                <div className="text-left">
+                    <h2 className="text-xl font-bold">╪د┘┘à╪╡┘╪╣ ╪د┘╪ص╪»┘è╪س ┘┘╪ذ┘╪د╪│╪ز┘è┘â</h2>
+                    <p>╪د┘╪╣┘┘ê╪د┘: ╪د┘┘à┘╪╖┘é╪ر ╪د┘╪╡┘╪د╪╣┘è╪ر</p>
+                    <p>┘ç╪د╪ز┘: 01000000000</p>
+                </div>
+            </div>
+
+            <div className="bg-gray-100 p-4 rounded mb-6 border border-gray-300">
+                <div className="grid grid-cols-2 gap-4">
+                    <div>
+                        <span className="font-bold">╪د╪│┘à ╪د┘╪╣┘à┘è┘:</span> {customer.name}
+                    </div>
+                    <div>
+                        <span className="font-bold">╪▒┘é┘à ╪د┘┘ç╪د╪ز┘:</span> {customer.phone}
+                    </div>
+                    <div>
+                        <span className="font-bold">╪د┘╪╣┘┘ê╪د┘:</span> {customer.address}
+                    </div>
+                </div>
+            </div>
+
+            <table className="w-full border-collapse border border-black mb-6 text-sm">
+                <thead>
+                    <tr className="bg-gray-200">
+                        <th className="border border-black p-2">╪د┘╪ز╪د╪▒┘è╪«</th>
+                        <th className="border border-black p-2">╪د┘╪╣┘à┘┘è╪ر</th>
+                        <th className="border border-black p-2">╪د┘┘ê╪╡┘</th>
+                        <th className="border border-black p-2">┘à╪»┘è┘ (┘┘╪د)</th>
+                        <th className="border border-black p-2">╪»╪د╪خ┘ (╪╣┘┘è┘╪د)</th>
+                        <th className="border border-black p-2">╪د┘╪▒╪╡┘è╪»</th>
+                    </tr>
+                </thead>
+                <tbody>
+                    {transactions.map((t, index) => {
+                        const debit = (t.type === 'INVOICE' || t.type === 'OPENING_BALANCE') ? Number(t.amount) : 0;
+                        const credit = (t.type === 'PAYMENT' || t.type === 'RETURN') ? Number(t.amount) : 0;
+
+                        return (
+                            <tr key={index}>
+                                <td className="border border-black p-2">{new Date(t.date).toLocaleDateString('ar-EG')}</td>
+                                <td className="border border-black p-2">
+                                    {t.type === 'INVOICE' ? '┘╪د╪ز┘ê╪▒╪ر ╪ذ┘è╪╣' :
+                                        t.type === 'PAYMENT' ? '╪»┘╪╣╪ر ┘┘é╪»┘è╪ر' :
+                                            t.type === 'RETURN' ? '┘à╪▒╪ز╪ش╪╣' : '╪▒╪╡┘è╪» ╪د┘╪ز╪ز╪د╪ص┘è'}
+                                </td>
+                                <td className="border border-black p-2">{t.description || '-'}</td>
+                                <td className="border border-black p-2">{debit > 0 ? debit.toLocaleString() : '-'}</td>
+                                <td className="border border-black p-2">{credit > 0 ? credit.toLocaleString() : '-'}</td>
+                                <td className="border border-black p-2 bg-gray-50"></td>
+                            </tr>
+                        );
+                    })}
+                </tbody>
+                <tfoot>
+                    <tr className="bg-gray-300 font-bold">
+                        <td className="border border-black p-2" colSpan={3}>╪د┘╪ح╪ش┘à╪د┘┘è</td>
+                        <td className="border border-black p-2">{totalDebit.toLocaleString()}</td>
+                        <td className="border border-black p-2">{totalCredit.toLocaleString()}</td>
+                        <td className="border border-black p-2">{balance.toLocaleString()}</td>
+                    </tr>
+                </tfoot>
+            </table>
+
+            <div className="flex justify-between mt-12 pt-8 border-t border-black">
+                <div>
+                    <p>╪ز┘ê┘é┘è╪╣ ╪د┘┘à╪ص╪د╪│╪ذ</p>
+                    <p className="mt-8">....................</p>
+                </div>
+                <div>
+                    <p>╪ز┘ê┘é┘è╪╣ ╪د┘╪╣┘à┘è┘</p>
+                    <p className="mt-8">....................</p>
+                </div>
+            </div>
+
+            <div className="fixed bottom-8 left-8 print:hidden">
+                <button
+                    onClick={() => window.print()}
+                    className="bg-blue-600 text-white px-6 py-3 rounded-full shadow-lg hover:bg-blue-700 font-bold flex items-center gap-2"
+                >
+                    <Printer className="w-5 h-5" /> ╪╖╪ذ╪د╪╣╪ر
+                </button>
+            </div>
+        </div>
+    );
+}
diff --git a/frontend/app/sales/customers/statement/[id]/page.tsx b/frontend/app/sales/customers/statement/[id]/page.tsx
index 04aca75..b56bb11 100644
--- a/frontend/app/sales/customers/statement/[id]/page.tsx
+++ b/frontend/app/sales/customers/statement/[id]/page.tsx
@@ -1,168 +1,9 @@
-'use client';
+import StatementClientPage from './client';
 
-import { Printer } from 'lucide-react';
-import { api } from '@/lib/api';
-import { useParams } from 'next/navigation';
-import { useState, useEffect, useCallback } from 'react';
-
-interface Customer {
-    id: number;
-    name: string;
-    phone?: string;
-    address?: string;
-    balance: number | string;
-}
-
-interface Transaction {
-    id: number;
-    type: 'INVOICE' | 'OPENING_BALANCE' | 'PAYMENT' | 'RETURN';
-    amount: number | string;
-    date: string;
-    description?: string;
+export function generateStaticParams(): { id: string }[] {
+  return [{ id: 'placeholder' }];
 }
 
-export default function CustomerStatementPage() {
-    const { id } = useParams();
-    const [customer, setCustomer] = useState<Customer | null>(null);
-    const [transactions, setTransactions] = useState<Transaction[]>([]);
-    const [loading, setLoading] = useState(true);
-
-    const loadData = useCallback(async () => {
-        try {
-            const customers = await api.fetchWithAuth('/sales/customers');
-            const cust = customers.find((c: Customer) => c.id === Number(id));
-            setCustomer(cust);
-
-            if (cust) {
-                const trans = await api.fetchWithAuth(`/sales/customers/${id}/statement`);
-                setTransactions((trans || []).map((t: any) => ({
-                  id: t.ref || t.id,
-                  type: t.type === 'ORDER' ? 'INVOICE' : t.type,
-                  amount: Number(t.debit || t.credit),
-                  date: t.date,
-                  description: t.description || '',
-                })));
-            }
-        } catch (error) {
-            console.error('Error loading data', error);
-        } finally {
-            setLoading(false);
-        }
-    }, [id]);
-
-    useEffect(() => {
-        loadData();
-    }, [loadData]);
-
-    if (loading) return <div>╪ش╪د╪▒┘è ╪د┘╪ز╪ص┘à┘è┘...</div>;
-    if (!customer) return <div>╪د┘╪╣┘à┘è┘ ╪║┘è╪▒ ┘à┘ê╪ش┘ê╪»</div>;
-
-    const totalDebit = transactions
-        .filter(t => t.type === 'INVOICE' || t.type === 'OPENING_BALANCE')
-        .reduce((sum, t) => sum + Number(t.amount), 0);
-
-    const totalCredit = transactions
-        .filter(t => t.type === 'PAYMENT' || t.type === 'RETURN')
-        .reduce((sum, t) => sum + Number(t.amount), 0);
-
-    const balance = totalDebit - totalCredit;
-
-    return (
-        <div className="min-h-screen bg-white text-black p-8 font-serif" dir="rtl">
-            {/* Header */}
-            <div className="flex justify-between items-center border-b-2 border-black pb-4 mb-6">
-                <div>
-                    <h1 className="text-3xl font-bold mb-2">┘â╪┤┘ ╪ص╪│╪د╪ذ ╪╣┘à┘è┘</h1>
-                    <p className="text-sm">╪د┘╪ز╪د╪▒┘è╪«: {new Date().toLocaleDateString('ar-EG')}</p>
-                </div>
-                <div className="text-left">
-                    <h2 className="text-xl font-bold">╪د┘┘à╪╡┘╪╣ ╪د┘╪ص╪»┘è╪س ┘┘╪ذ┘╪د╪│╪ز┘è┘â</h2>
-                    <p>╪د┘╪╣┘┘ê╪د┘: ╪د┘┘à┘╪╖┘é╪ر ╪د┘╪╡┘╪د╪╣┘è╪ر</p>
-                    <p>┘ç╪د╪ز┘: 01000000000</p>
-                </div>
-            </div>
-
-            {/* Customer Info */}
-            <div className="bg-gray-100 p-4 rounded mb-6 border border-gray-300">
-                <div className="grid grid-cols-2 gap-4">
-                    <div>
-                        <span className="font-bold">╪د╪│┘à ╪د┘╪╣┘à┘è┘:</span> {customer.name}
-                    </div>
-                    <div>
-                        <span className="font-bold">╪▒┘é┘à ╪د┘┘ç╪د╪ز┘:</span> {customer.phone}
-                    </div>
-                    <div>
-                        <span className="font-bold">╪د┘╪╣┘┘ê╪د┘:</span> {customer.address}
-                    </div>
-                </div>
-            </div>
-
-            {/* Transactions Table */}
-            <table className="w-full border-collapse border border-black mb-6 text-sm">
-                <thead>
-                    <tr className="bg-gray-200">
-                        <th className="border border-black p-2">╪د┘╪ز╪د╪▒┘è╪«</th>
-                        <th className="border border-black p-2">╪د┘╪╣┘à┘┘è╪ر</th>
-                        <th className="border border-black p-2">╪د┘┘ê╪╡┘</th>
-                        <th className="border border-black p-2">┘à╪»┘è┘ (┘┘╪د)</th>
-                        <th className="border border-black p-2">╪»╪د╪خ┘ (╪╣┘┘è┘╪د)</th>
-                        <th className="border border-black p-2">╪د┘╪▒╪╡┘è╪»</th>
-                    </tr>
-                </thead>
-                <tbody>
-                    {transactions.map((t, index) => {
-                        const debit = (t.type === 'INVOICE' || t.type === 'OPENING_BALANCE') ? Number(t.amount) : 0;
-                        const credit = (t.type === 'PAYMENT' || t.type === 'RETURN') ? Number(t.amount) : 0;
-                        // Incorrect running balance calculation here, need to accumulate.
-                        // But for now just listing. Ideally we calculate running balance.
-
-                        return (
-                            <tr key={index}>
-                                <td className="border border-black p-2">{new Date(t.date).toLocaleDateString('ar-EG')}</td>
-                                <td className="border border-black p-2">
-                                    {t.type === 'INVOICE' ? '┘╪د╪ز┘ê╪▒╪ر ╪ذ┘è╪╣' :
-                                        t.type === 'PAYMENT' ? '╪»┘╪╣╪ر ┘┘é╪»┘è╪ر' :
-                                            t.type === 'RETURN' ? '┘à╪▒╪ز╪ش╪╣' : '╪▒╪╡┘è╪» ╪د┘╪ز╪ز╪د╪ص┘è'}
-                                </td>
-                                <td className="border border-black p-2">{t.description || '-'}</td>
-                                <td className="border border-black p-2">{debit > 0 ? debit.toLocaleString() : '-'}</td>
-                                <td className="border border-black p-2">{credit > 0 ? credit.toLocaleString() : '-'}</td>
-                                <td className="border border-black p-2 bg-gray-50"></td>
-                            </tr>
-                        );
-                    })}
-                </tbody>
-                <tfoot>
-                    <tr className="bg-gray-300 font-bold">
-                        <td className="border border-black p-2" colSpan={3}>╪د┘╪ح╪ش┘à╪د┘┘è</td>
-                        <td className="border border-black p-2">{totalDebit.toLocaleString()}</td>
-                        <td className="border border-black p-2">{totalCredit.toLocaleString()}</td>
-                        <td className="border border-black p-2">{balance.toLocaleString()}</td>
-                    </tr>
-                </tfoot>
-            </table>
-
-            {/* Footer */}
-            <div className="flex justify-between mt-12 pt-8 border-t border-black">
-                <div>
-                    <p>╪ز┘ê┘é┘è╪╣ ╪د┘┘à╪ص╪د╪│╪ذ</p>
-                    <p className="mt-8">....................</p>
-                </div>
-                <div>
-                    <p>╪ز┘ê┘é┘è╪╣ ╪د┘╪╣┘à┘è┘</p>
-                    <p className="mt-8">....................</p>
-                </div>
-            </div>
-
-            {/* Print Button (Hidden when printing) */}
-            <div className="fixed bottom-8 left-8 print:hidden">
-                <button
-                    onClick={() => window.print()}
-                    className="bg-blue-600 text-white px-6 py-3 rounded-full shadow-lg hover:bg-blue-700 font-bold flex items-center gap-2"
-                >
-                    <Printer className="w-5 h-5" /> ╪╖╪ذ╪د╪╣╪ر
-                </button>
-            </div>
-        </div>
-    );
+export default function Page() {
+  return <StatementClientPage />;
 }
diff --git a/frontend/next.config.ts b/frontend/next.config.ts
index 3718742..dd144f1 100644
--- a/frontend/next.config.ts
+++ b/frontend/next.config.ts
@@ -1,30 +1,10 @@
 import type { NextConfig } from 'next';
 
 const nextConfig: NextConfig = {
-  output: 'standalone',
+  output: 'export',
   reactStrictMode: true,
-  turbopack: {
-    root: process.cwd(),
-  },
-  experimental: {
-    turbopackFileSystemCacheForDev: true,
-  },
-  allowedDevOrigins: process.env.ALLOWED_DEV_ORIGINS
-    ? process.env.ALLOWED_DEV_ORIGINS.split(',')
-    : ['localhost'],
-  async rewrites() {
-    return [
-      {
-        source: '/api/:path*',
-        destination: `${process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001'}/api/:path*`,
-      },
-      {
-        source: '/uploads/:path*',
-        destination: `${process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001'}/uploads/:path*`,
-      },
-    ];
-  },
+  trailingSlash: true,
+  images: { unoptimized: true },
 };
 
 export default nextConfig;
-
