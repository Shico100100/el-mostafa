# دليل نشر ونقل النظام (Deployment & Migration Guide)

هذا الدليل يشرح كيفية تشغيل النظام ليعمل على شبكة داخلية (أكثر من جهاز)، وكيفية نقله لجهاز سيرفر آخر.

## 1. التشغيل عبر الشبكة (Multi-Device Access)

النظام جاهز للعمل على أكثر من جهاز بمجرد تشغيله على الجهاز الأساسي (السيرفر).

### الخطوة الأولى: معرفة عنوان الـ IP للسيرفر
1. افتح PowerShell على جهاز السيرفر.
2. اكتب الأمر: `ipconfig`.
3. ابحث عن `IPv4 Address` (غالباً يكون شيء مثل `192.168.1.15`).

### الخطوة الثانية: الوصول من الأجهزة الأخرى
من أي جهاز آخر متصل بنفس الشبكة، افتح المتصفح واكتب:
`http://[SERVER_IP]:3000`
*(استبدل [SERVER_IP] بالعنوان الذي استخرجته).*

---

## 2. نقل السيرفر لجهاز آخر (Migration)

إذا أردت نقل النظام بالكامل لجهاز جديد ليكون هو السيرفر الدائم:

### أ- على الجهاز الجديد:
1. ثبت المتطلبات الأساسية (Node.js و PostgreSQL).
2. انسخ مجلد المشروع `ELMostafa` بالكامل للجهاز الجديد.
3. افتح PowerShell في المجلد الجديد وشغل سكربت التثبيت:
   ```powershell
   .\setup-windows.ps1
   ```
   *(سيقوم بإنشاء قاعدة البيانات وتثبيت المكتبات).*

### ب- نقل البيانات (Database Backup):
إذا كان لديك بيانات قديمة تريد نقلها:
1. **في الجهاز القديم**: استخدم السكربت الموجود في `WINDOWS-README.md` (قسم النسخ الاحتياطي) لعمل ملف `.sql`.
2. **في الجهاز الجديد**: انقل الملف، ثم استخدم الأمر التالي لاسترجاعه:
   ```powershell
   psql -U postgres -d elmostafa_db -f elmostafa_backup.sql
   ```

---

## 3. تحديث البرنامج (Updates)

عند وجود تحديثات جديدة للبرنامج:

1. **إيقاف النظام**: تأكد من إيقاف PM2 أو إغلاق النوافذ.
2. **استلام الملفات**: استبدل الملفات القديمة بالملفات الجديدة (أو استخدم `git pull` إذا كنت تستخدم Git).
3. **تحديث المكتبات**: إذا كان هناك مكتبات جديدة، شغل:
   ```powershell
   cd backend; npm install; cd ..\frontend; npm install; cd ..
   ```
4. **إعادة بناء النسخة الإنتاجية**:
   ```powershell
   .\start-prod-windows.ps1
   ```

---

## ⚠️ نصائح هامة للسيرفر
*   **ثبات الـ IP**: يفضل تثبيت عنوان الـ IP لجهاز السيرفر من إعدادات الراوتر لكي لا يتغير العنوان ويحتاج المستخدمون لتغييره كل مرة.
*   **جدار الحماية (Firewall)**: إذا لم يفتح البرنامج في الأجهزة الأخرى، قد تحتاج للسماح للمنافذ `3000` و `3001` في إعدادات Windows Firewall.
